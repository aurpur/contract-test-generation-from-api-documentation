"""
Runner Agent - Executes Maven tests and analyzes results.

This agent executes generated Java tests using Maven, parses JUnit XML results,
analyzes failures, and provides feedback for test regeneration.

Author: Aurel IKAMA HONEY
"""
import asyncio
import os
import re
import subprocess
import xml.etree.ElementTree as ET
from datetime import datetime
from pathlib import Path
from typing import Any, Dict, List, Optional, Tuple
from uuid import UUID

from agents.base_agent import BaseAgent, AgentConfig
from shared_context import (
    ContextManager,
    GeneratedTest,
    TestExecutionResult,
    AgentType,
)
from orchestration import Task, MessageBuilder
from utils.logging import logger
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class MavenRunner:
    """
    Maven test execution wrapper.
    
    Handles Maven invocation, output parsing, and error handling.
    """
    
    def xǁMavenRunnerǁ__init____mutmut_orig(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_1(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = None
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_2(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(None)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_3(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = None
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_4(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = None
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_5(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(None)
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_6(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" * "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_7(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) * "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_8(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(None) / "bin" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_9(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "XXbinXX" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_10(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "BIN" / "mvn")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_11(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "XXmvnXX")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_12(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "MVN")
        else:
            self.mvn_cmd = "mvn"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_13(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = None
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_14(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "XXmvnXX"
        
        # Verify Maven is available
        self._verify_maven()
    
    def xǁMavenRunnerǁ__init____mutmut_15(self, project_dir: str, maven_home: Optional[str] = None):
        """
        Initialize Maven runner.
        
        Args:
            project_dir: Path to Maven project directory
            maven_home: Optional Maven installation path
        """
        self.project_dir = Path(project_dir)
        self.maven_home = maven_home
        
        # Determine Maven command
        if maven_home:
            self.mvn_cmd = str(Path(maven_home) / "bin" / "mvn")
        else:
            self.mvn_cmd = "MVN"
        
        # Verify Maven is available
        self._verify_maven()
    
    xǁMavenRunnerǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMavenRunnerǁ__init____mutmut_1': xǁMavenRunnerǁ__init____mutmut_1, 
        'xǁMavenRunnerǁ__init____mutmut_2': xǁMavenRunnerǁ__init____mutmut_2, 
        'xǁMavenRunnerǁ__init____mutmut_3': xǁMavenRunnerǁ__init____mutmut_3, 
        'xǁMavenRunnerǁ__init____mutmut_4': xǁMavenRunnerǁ__init____mutmut_4, 
        'xǁMavenRunnerǁ__init____mutmut_5': xǁMavenRunnerǁ__init____mutmut_5, 
        'xǁMavenRunnerǁ__init____mutmut_6': xǁMavenRunnerǁ__init____mutmut_6, 
        'xǁMavenRunnerǁ__init____mutmut_7': xǁMavenRunnerǁ__init____mutmut_7, 
        'xǁMavenRunnerǁ__init____mutmut_8': xǁMavenRunnerǁ__init____mutmut_8, 
        'xǁMavenRunnerǁ__init____mutmut_9': xǁMavenRunnerǁ__init____mutmut_9, 
        'xǁMavenRunnerǁ__init____mutmut_10': xǁMavenRunnerǁ__init____mutmut_10, 
        'xǁMavenRunnerǁ__init____mutmut_11': xǁMavenRunnerǁ__init____mutmut_11, 
        'xǁMavenRunnerǁ__init____mutmut_12': xǁMavenRunnerǁ__init____mutmut_12, 
        'xǁMavenRunnerǁ__init____mutmut_13': xǁMavenRunnerǁ__init____mutmut_13, 
        'xǁMavenRunnerǁ__init____mutmut_14': xǁMavenRunnerǁ__init____mutmut_14, 
        'xǁMavenRunnerǁ__init____mutmut_15': xǁMavenRunnerǁ__init____mutmut_15
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMavenRunnerǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁMavenRunnerǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁMavenRunnerǁ__init____mutmut_orig)
    xǁMavenRunnerǁ__init____mutmut_orig.__name__ = 'xǁMavenRunnerǁ__init__'
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_orig(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_1(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = None
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_2(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                None,
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_3(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=None,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_4(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=None,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_5(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=None,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_6(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_7(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_8(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_9(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_10(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "XX--versionXX"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_11(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--VERSION"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_12(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=False,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_13(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=False,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_14(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=11,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_15(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode == 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_16(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 1:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_17(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(None)
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_18(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(None)
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_19(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[3]}")
        except Exception as e:
            raise RuntimeError(f"Failed to verify Maven: {e}")
    
    def xǁMavenRunnerǁ_verify_maven__mutmut_20(self) -> None:
        """Verify Maven is installed and accessible."""
        try:
            result = subprocess.run(
                [self.mvn_cmd, "--version"],
                capture_output=True,
                text=True,
                timeout=10,
            )
            if result.returncode != 0:
                raise RuntimeError(f"Maven not found: {result.stderr}")
            logger.info(f"Maven verified: {result.stdout.split()[2]}")
        except Exception as e:
            raise RuntimeError(None)
    
    xǁMavenRunnerǁ_verify_maven__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMavenRunnerǁ_verify_maven__mutmut_1': xǁMavenRunnerǁ_verify_maven__mutmut_1, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_2': xǁMavenRunnerǁ_verify_maven__mutmut_2, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_3': xǁMavenRunnerǁ_verify_maven__mutmut_3, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_4': xǁMavenRunnerǁ_verify_maven__mutmut_4, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_5': xǁMavenRunnerǁ_verify_maven__mutmut_5, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_6': xǁMavenRunnerǁ_verify_maven__mutmut_6, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_7': xǁMavenRunnerǁ_verify_maven__mutmut_7, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_8': xǁMavenRunnerǁ_verify_maven__mutmut_8, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_9': xǁMavenRunnerǁ_verify_maven__mutmut_9, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_10': xǁMavenRunnerǁ_verify_maven__mutmut_10, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_11': xǁMavenRunnerǁ_verify_maven__mutmut_11, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_12': xǁMavenRunnerǁ_verify_maven__mutmut_12, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_13': xǁMavenRunnerǁ_verify_maven__mutmut_13, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_14': xǁMavenRunnerǁ_verify_maven__mutmut_14, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_15': xǁMavenRunnerǁ_verify_maven__mutmut_15, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_16': xǁMavenRunnerǁ_verify_maven__mutmut_16, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_17': xǁMavenRunnerǁ_verify_maven__mutmut_17, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_18': xǁMavenRunnerǁ_verify_maven__mutmut_18, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_19': xǁMavenRunnerǁ_verify_maven__mutmut_19, 
        'xǁMavenRunnerǁ_verify_maven__mutmut_20': xǁMavenRunnerǁ_verify_maven__mutmut_20
    }
    
    def _verify_maven(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMavenRunnerǁ_verify_maven__mutmut_orig"), object.__getattribute__(self, "xǁMavenRunnerǁ_verify_maven__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _verify_maven.__signature__ = _mutmut_signature(xǁMavenRunnerǁ_verify_maven__mutmut_orig)
    xǁMavenRunnerǁ_verify_maven__mutmut_orig.__name__ = 'xǁMavenRunnerǁ_verify_maven'
    
    async def xǁMavenRunnerǁrun_tests__mutmut_orig(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_1(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 301,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_2(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = None
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_3(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "XXtestXX"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_4(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "TEST"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_5(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = None
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_6(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(None)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_7(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = "XX,XX".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_8(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(None)
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_9(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" - test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_10(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["XX-Dtest=XX" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_11(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_12(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-DTEST=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_13(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(None)
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_14(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(None)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_15(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {'XX XX'.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_16(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = None
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_17(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=None,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_18(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=None,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_19(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=None,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_20(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_21(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_22(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_23(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_24(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(None),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_25(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = None
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_26(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    None,
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_27(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=None,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_28(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_29(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_30(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(None)
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_31(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = None
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_32(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode(None)
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_33(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('XXutf-8XX')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_34(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('UTF-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_35(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = None
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_36(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode(None)
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_37(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('XXutf-8XX')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_38(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('UTF-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_39(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = None
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_40(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(None)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_41(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = None
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_42(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode != 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_43(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 1
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_44(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = None
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_45(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" - error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_46(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output - "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_47(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "XX\nXX" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_48(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(None)
            return False, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_49(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return True, str(e), {}
    
    async def xǁMavenRunnerǁrun_tests__mutmut_50(
        self,
        test_classes: Optional[List[str]] = None,
        timeout: int = 300,
    ) -> Tuple[bool, str, Dict[str, Any]]:
        """
        Run Maven tests.
        
        Args:
            test_classes: Optional list of test class names to run
            timeout: Execution timeout in seconds
            
        Returns:
            Tuple of (success, output, metrics)
        """
        # Build Maven command
        cmd = [self.mvn_cmd, "test"]
        
        # Add specific test classes if provided
        if test_classes:
            test_pattern = ",".join(test_classes)
            cmd.extend(["-Dtest=" + test_pattern])
        
        logger.info(f"Executing Maven: {' '.join(cmd)}")
        
        try:
            # Run Maven
            process = await asyncio.create_subprocess_exec(
                *cmd,
                cwd=str(self.project_dir),
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
            )
            
            # Wait with timeout
            try:
                stdout, stderr = await asyncio.wait_for(
                    process.communicate(),
                    timeout=timeout,
                )
            except asyncio.TimeoutError:
                process.kill()
                await process.wait()
                raise TimeoutError(f"Maven execution timed out after {timeout}s")
            
            # Decode output
            output = stdout.decode('utf-8')
            error = stderr.decode('utf-8')
            
            # Parse metrics from output
            metrics = self._parse_maven_output(output)
            
            # Check success
            success = process.returncode == 0
            
            full_output = output + "\n" + error if error else output
            
            return success, full_output, metrics
            
        except Exception as e:
            logger.error(f"Maven execution failed: {e}")
            return False, str(None), {}
    
    xǁMavenRunnerǁrun_tests__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMavenRunnerǁrun_tests__mutmut_1': xǁMavenRunnerǁrun_tests__mutmut_1, 
        'xǁMavenRunnerǁrun_tests__mutmut_2': xǁMavenRunnerǁrun_tests__mutmut_2, 
        'xǁMavenRunnerǁrun_tests__mutmut_3': xǁMavenRunnerǁrun_tests__mutmut_3, 
        'xǁMavenRunnerǁrun_tests__mutmut_4': xǁMavenRunnerǁrun_tests__mutmut_4, 
        'xǁMavenRunnerǁrun_tests__mutmut_5': xǁMavenRunnerǁrun_tests__mutmut_5, 
        'xǁMavenRunnerǁrun_tests__mutmut_6': xǁMavenRunnerǁrun_tests__mutmut_6, 
        'xǁMavenRunnerǁrun_tests__mutmut_7': xǁMavenRunnerǁrun_tests__mutmut_7, 
        'xǁMavenRunnerǁrun_tests__mutmut_8': xǁMavenRunnerǁrun_tests__mutmut_8, 
        'xǁMavenRunnerǁrun_tests__mutmut_9': xǁMavenRunnerǁrun_tests__mutmut_9, 
        'xǁMavenRunnerǁrun_tests__mutmut_10': xǁMavenRunnerǁrun_tests__mutmut_10, 
        'xǁMavenRunnerǁrun_tests__mutmut_11': xǁMavenRunnerǁrun_tests__mutmut_11, 
        'xǁMavenRunnerǁrun_tests__mutmut_12': xǁMavenRunnerǁrun_tests__mutmut_12, 
        'xǁMavenRunnerǁrun_tests__mutmut_13': xǁMavenRunnerǁrun_tests__mutmut_13, 
        'xǁMavenRunnerǁrun_tests__mutmut_14': xǁMavenRunnerǁrun_tests__mutmut_14, 
        'xǁMavenRunnerǁrun_tests__mutmut_15': xǁMavenRunnerǁrun_tests__mutmut_15, 
        'xǁMavenRunnerǁrun_tests__mutmut_16': xǁMavenRunnerǁrun_tests__mutmut_16, 
        'xǁMavenRunnerǁrun_tests__mutmut_17': xǁMavenRunnerǁrun_tests__mutmut_17, 
        'xǁMavenRunnerǁrun_tests__mutmut_18': xǁMavenRunnerǁrun_tests__mutmut_18, 
        'xǁMavenRunnerǁrun_tests__mutmut_19': xǁMavenRunnerǁrun_tests__mutmut_19, 
        'xǁMavenRunnerǁrun_tests__mutmut_20': xǁMavenRunnerǁrun_tests__mutmut_20, 
        'xǁMavenRunnerǁrun_tests__mutmut_21': xǁMavenRunnerǁrun_tests__mutmut_21, 
        'xǁMavenRunnerǁrun_tests__mutmut_22': xǁMavenRunnerǁrun_tests__mutmut_22, 
        'xǁMavenRunnerǁrun_tests__mutmut_23': xǁMavenRunnerǁrun_tests__mutmut_23, 
        'xǁMavenRunnerǁrun_tests__mutmut_24': xǁMavenRunnerǁrun_tests__mutmut_24, 
        'xǁMavenRunnerǁrun_tests__mutmut_25': xǁMavenRunnerǁrun_tests__mutmut_25, 
        'xǁMavenRunnerǁrun_tests__mutmut_26': xǁMavenRunnerǁrun_tests__mutmut_26, 
        'xǁMavenRunnerǁrun_tests__mutmut_27': xǁMavenRunnerǁrun_tests__mutmut_27, 
        'xǁMavenRunnerǁrun_tests__mutmut_28': xǁMavenRunnerǁrun_tests__mutmut_28, 
        'xǁMavenRunnerǁrun_tests__mutmut_29': xǁMavenRunnerǁrun_tests__mutmut_29, 
        'xǁMavenRunnerǁrun_tests__mutmut_30': xǁMavenRunnerǁrun_tests__mutmut_30, 
        'xǁMavenRunnerǁrun_tests__mutmut_31': xǁMavenRunnerǁrun_tests__mutmut_31, 
        'xǁMavenRunnerǁrun_tests__mutmut_32': xǁMavenRunnerǁrun_tests__mutmut_32, 
        'xǁMavenRunnerǁrun_tests__mutmut_33': xǁMavenRunnerǁrun_tests__mutmut_33, 
        'xǁMavenRunnerǁrun_tests__mutmut_34': xǁMavenRunnerǁrun_tests__mutmut_34, 
        'xǁMavenRunnerǁrun_tests__mutmut_35': xǁMavenRunnerǁrun_tests__mutmut_35, 
        'xǁMavenRunnerǁrun_tests__mutmut_36': xǁMavenRunnerǁrun_tests__mutmut_36, 
        'xǁMavenRunnerǁrun_tests__mutmut_37': xǁMavenRunnerǁrun_tests__mutmut_37, 
        'xǁMavenRunnerǁrun_tests__mutmut_38': xǁMavenRunnerǁrun_tests__mutmut_38, 
        'xǁMavenRunnerǁrun_tests__mutmut_39': xǁMavenRunnerǁrun_tests__mutmut_39, 
        'xǁMavenRunnerǁrun_tests__mutmut_40': xǁMavenRunnerǁrun_tests__mutmut_40, 
        'xǁMavenRunnerǁrun_tests__mutmut_41': xǁMavenRunnerǁrun_tests__mutmut_41, 
        'xǁMavenRunnerǁrun_tests__mutmut_42': xǁMavenRunnerǁrun_tests__mutmut_42, 
        'xǁMavenRunnerǁrun_tests__mutmut_43': xǁMavenRunnerǁrun_tests__mutmut_43, 
        'xǁMavenRunnerǁrun_tests__mutmut_44': xǁMavenRunnerǁrun_tests__mutmut_44, 
        'xǁMavenRunnerǁrun_tests__mutmut_45': xǁMavenRunnerǁrun_tests__mutmut_45, 
        'xǁMavenRunnerǁrun_tests__mutmut_46': xǁMavenRunnerǁrun_tests__mutmut_46, 
        'xǁMavenRunnerǁrun_tests__mutmut_47': xǁMavenRunnerǁrun_tests__mutmut_47, 
        'xǁMavenRunnerǁrun_tests__mutmut_48': xǁMavenRunnerǁrun_tests__mutmut_48, 
        'xǁMavenRunnerǁrun_tests__mutmut_49': xǁMavenRunnerǁrun_tests__mutmut_49, 
        'xǁMavenRunnerǁrun_tests__mutmut_50': xǁMavenRunnerǁrun_tests__mutmut_50
    }
    
    def run_tests(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMavenRunnerǁrun_tests__mutmut_orig"), object.__getattribute__(self, "xǁMavenRunnerǁrun_tests__mutmut_mutants"), args, kwargs, self)
        return result 
    
    run_tests.__signature__ = _mutmut_signature(xǁMavenRunnerǁrun_tests__mutmut_orig)
    xǁMavenRunnerǁrun_tests__mutmut_orig.__name__ = 'xǁMavenRunnerǁrun_tests'
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_orig(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_1(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = None
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_2(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "XXtests_runXX": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_3(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "TESTS_RUN": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_4(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 1,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_5(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "XXtests_passedXX": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_6(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "TESTS_PASSED": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_7(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 1,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_8(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "XXtests_failedXX": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_9(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "TESTS_FAILED": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_10(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 1,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_11(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "XXtests_skippedXX": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_12(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "TESTS_SKIPPED": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_13(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 1,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_14(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "XXexecution_time_msXX": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_15(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "EXECUTION_TIME_MS": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_16(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 1,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_17(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = None
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_18(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"XXTests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)XX"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_19(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"tests run: (\d+), failures: (\d+), errors: (\d+), skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_20(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"TESTS RUN: (\d+), FAILURES: (\d+), ERRORS: (\d+), SKIPPED: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_21(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = None
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_22(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(None, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_23(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, None)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_24(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_25(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, )
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_26(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = None
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_27(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(None)
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_28(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(None))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_29(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(2))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_30(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = None
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_31(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(None)
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_32(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(None))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_33(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(3))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_34(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = None
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_35(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(None)
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_36(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(None))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_37(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(4))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_38(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = None
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_39(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(None)
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_40(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(None))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_41(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(5))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_42(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = None
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_43(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["XXtests_runXX"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_44(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["TESTS_RUN"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_45(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = None
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_46(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["XXtests_failedXX"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_47(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["TESTS_FAILED"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_48(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures - errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_49(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = None
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_50(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["XXtests_passedXX"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_51(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["TESTS_PASSED"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_52(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors + skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_53(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures + errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_54(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run + failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_55(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = None
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_56(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["XXtests_skippedXX"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_57(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["TESTS_SKIPPED"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_58(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = None
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_59(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"XXTime elapsed: ([\d.]+) sXX"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_60(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_61(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"TIME ELAPSED: ([\d.]+) S"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_62(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = None
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_63(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(None, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_64(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, None)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_65(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_66(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, )
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_67(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = None
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_68(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["XXexecution_time_msXX"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_69(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["EXECUTION_TIME_MS"] = float(time_match.group(1)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_70(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) / 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_71(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(None) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_72(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(None)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_73(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(2)) * 1000
        
        return metrics
    
    def xǁMavenRunnerǁ_parse_maven_output__mutmut_74(self, output: str) -> Dict[str, Any]:
        """
        Parse Maven test output for metrics.
        
        Args:
            output: Maven output text
            
        Returns:
            Dictionary of metrics
        """
        metrics = {
            "tests_run": 0,
            "tests_passed": 0,
            "tests_failed": 0,
            "tests_skipped": 0,
            "execution_time_ms": 0,
        }
        
        # Pattern: Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
        pattern = r"Tests run: (\d+), Failures: (\d+), Errors: (\d+), Skipped: (\d+)"
        match = re.search(pattern, output)
        
        if match:
            tests_run = int(match.group(1))
            failures = int(match.group(2))
            errors = int(match.group(3))
            skipped = int(match.group(4))
            
            metrics["tests_run"] = tests_run
            metrics["tests_failed"] = failures + errors
            metrics["tests_passed"] = tests_run - failures - errors - skipped
            metrics["tests_skipped"] = skipped
        
        # Pattern: Time elapsed: 1.234 s
        time_pattern = r"Time elapsed: ([\d.]+) s"
        time_match = re.search(time_pattern, output)
        
        if time_match:
            metrics["execution_time_ms"] = float(time_match.group(1)) * 1001
        
        return metrics
    
    xǁMavenRunnerǁ_parse_maven_output__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMavenRunnerǁ_parse_maven_output__mutmut_1': xǁMavenRunnerǁ_parse_maven_output__mutmut_1, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_2': xǁMavenRunnerǁ_parse_maven_output__mutmut_2, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_3': xǁMavenRunnerǁ_parse_maven_output__mutmut_3, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_4': xǁMavenRunnerǁ_parse_maven_output__mutmut_4, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_5': xǁMavenRunnerǁ_parse_maven_output__mutmut_5, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_6': xǁMavenRunnerǁ_parse_maven_output__mutmut_6, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_7': xǁMavenRunnerǁ_parse_maven_output__mutmut_7, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_8': xǁMavenRunnerǁ_parse_maven_output__mutmut_8, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_9': xǁMavenRunnerǁ_parse_maven_output__mutmut_9, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_10': xǁMavenRunnerǁ_parse_maven_output__mutmut_10, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_11': xǁMavenRunnerǁ_parse_maven_output__mutmut_11, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_12': xǁMavenRunnerǁ_parse_maven_output__mutmut_12, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_13': xǁMavenRunnerǁ_parse_maven_output__mutmut_13, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_14': xǁMavenRunnerǁ_parse_maven_output__mutmut_14, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_15': xǁMavenRunnerǁ_parse_maven_output__mutmut_15, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_16': xǁMavenRunnerǁ_parse_maven_output__mutmut_16, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_17': xǁMavenRunnerǁ_parse_maven_output__mutmut_17, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_18': xǁMavenRunnerǁ_parse_maven_output__mutmut_18, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_19': xǁMavenRunnerǁ_parse_maven_output__mutmut_19, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_20': xǁMavenRunnerǁ_parse_maven_output__mutmut_20, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_21': xǁMavenRunnerǁ_parse_maven_output__mutmut_21, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_22': xǁMavenRunnerǁ_parse_maven_output__mutmut_22, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_23': xǁMavenRunnerǁ_parse_maven_output__mutmut_23, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_24': xǁMavenRunnerǁ_parse_maven_output__mutmut_24, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_25': xǁMavenRunnerǁ_parse_maven_output__mutmut_25, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_26': xǁMavenRunnerǁ_parse_maven_output__mutmut_26, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_27': xǁMavenRunnerǁ_parse_maven_output__mutmut_27, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_28': xǁMavenRunnerǁ_parse_maven_output__mutmut_28, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_29': xǁMavenRunnerǁ_parse_maven_output__mutmut_29, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_30': xǁMavenRunnerǁ_parse_maven_output__mutmut_30, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_31': xǁMavenRunnerǁ_parse_maven_output__mutmut_31, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_32': xǁMavenRunnerǁ_parse_maven_output__mutmut_32, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_33': xǁMavenRunnerǁ_parse_maven_output__mutmut_33, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_34': xǁMavenRunnerǁ_parse_maven_output__mutmut_34, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_35': xǁMavenRunnerǁ_parse_maven_output__mutmut_35, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_36': xǁMavenRunnerǁ_parse_maven_output__mutmut_36, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_37': xǁMavenRunnerǁ_parse_maven_output__mutmut_37, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_38': xǁMavenRunnerǁ_parse_maven_output__mutmut_38, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_39': xǁMavenRunnerǁ_parse_maven_output__mutmut_39, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_40': xǁMavenRunnerǁ_parse_maven_output__mutmut_40, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_41': xǁMavenRunnerǁ_parse_maven_output__mutmut_41, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_42': xǁMavenRunnerǁ_parse_maven_output__mutmut_42, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_43': xǁMavenRunnerǁ_parse_maven_output__mutmut_43, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_44': xǁMavenRunnerǁ_parse_maven_output__mutmut_44, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_45': xǁMavenRunnerǁ_parse_maven_output__mutmut_45, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_46': xǁMavenRunnerǁ_parse_maven_output__mutmut_46, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_47': xǁMavenRunnerǁ_parse_maven_output__mutmut_47, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_48': xǁMavenRunnerǁ_parse_maven_output__mutmut_48, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_49': xǁMavenRunnerǁ_parse_maven_output__mutmut_49, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_50': xǁMavenRunnerǁ_parse_maven_output__mutmut_50, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_51': xǁMavenRunnerǁ_parse_maven_output__mutmut_51, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_52': xǁMavenRunnerǁ_parse_maven_output__mutmut_52, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_53': xǁMavenRunnerǁ_parse_maven_output__mutmut_53, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_54': xǁMavenRunnerǁ_parse_maven_output__mutmut_54, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_55': xǁMavenRunnerǁ_parse_maven_output__mutmut_55, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_56': xǁMavenRunnerǁ_parse_maven_output__mutmut_56, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_57': xǁMavenRunnerǁ_parse_maven_output__mutmut_57, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_58': xǁMavenRunnerǁ_parse_maven_output__mutmut_58, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_59': xǁMavenRunnerǁ_parse_maven_output__mutmut_59, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_60': xǁMavenRunnerǁ_parse_maven_output__mutmut_60, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_61': xǁMavenRunnerǁ_parse_maven_output__mutmut_61, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_62': xǁMavenRunnerǁ_parse_maven_output__mutmut_62, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_63': xǁMavenRunnerǁ_parse_maven_output__mutmut_63, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_64': xǁMavenRunnerǁ_parse_maven_output__mutmut_64, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_65': xǁMavenRunnerǁ_parse_maven_output__mutmut_65, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_66': xǁMavenRunnerǁ_parse_maven_output__mutmut_66, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_67': xǁMavenRunnerǁ_parse_maven_output__mutmut_67, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_68': xǁMavenRunnerǁ_parse_maven_output__mutmut_68, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_69': xǁMavenRunnerǁ_parse_maven_output__mutmut_69, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_70': xǁMavenRunnerǁ_parse_maven_output__mutmut_70, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_71': xǁMavenRunnerǁ_parse_maven_output__mutmut_71, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_72': xǁMavenRunnerǁ_parse_maven_output__mutmut_72, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_73': xǁMavenRunnerǁ_parse_maven_output__mutmut_73, 
        'xǁMavenRunnerǁ_parse_maven_output__mutmut_74': xǁMavenRunnerǁ_parse_maven_output__mutmut_74
    }
    
    def _parse_maven_output(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMavenRunnerǁ_parse_maven_output__mutmut_orig"), object.__getattribute__(self, "xǁMavenRunnerǁ_parse_maven_output__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_maven_output.__signature__ = _mutmut_signature(xǁMavenRunnerǁ_parse_maven_output__mutmut_orig)
    xǁMavenRunnerǁ_parse_maven_output__mutmut_orig.__name__ = 'xǁMavenRunnerǁ_parse_maven_output'
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_orig(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_1(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is not None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_2(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = None
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_3(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" * "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_4(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir * "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_5(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "XXtargetXX" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_6(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "TARGET" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_7(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "XXsurefire-reportsXX"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_8(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "SUREFIRE-REPORTS"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_9(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = None
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_10(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(None)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_11(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_12(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(None)
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_13(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = None
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_14(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob(None):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_15(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("XXTEST-*.xmlXX"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_16(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("test-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_17(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.XML"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_18(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = None
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_19(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(None)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_20(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = None
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_21(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = None
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_22(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "XXnameXX": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_23(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "NAME": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_24(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get(None),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_25(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("XXnameXX"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_26(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("NAME"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_27(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "XXtestsXX": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_28(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "TESTS": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_29(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(None),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_30(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get(None, 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_31(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", None)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_32(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get(0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_33(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", )),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_34(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("XXtestsXX", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_35(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("TESTS", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_36(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 1)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_37(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "XXfailuresXX": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_38(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "FAILURES": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_39(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(None),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_40(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get(None, 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_41(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", None)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_42(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get(0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_43(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", )),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_44(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("XXfailuresXX", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_45(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("FAILURES", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_46(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 1)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_47(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "XXerrorsXX": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_48(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "ERRORS": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_49(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(None),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_50(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get(None, 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_51(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", None)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_52(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get(0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_53(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", )),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_54(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("XXerrorsXX", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_55(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("ERRORS", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_56(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 1)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_57(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "XXskippedXX": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_58(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "SKIPPED": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_59(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(None),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_60(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get(None, 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_61(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", None)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_62(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get(0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_63(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", )),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_64(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("XXskippedXX", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_65(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("SKIPPED", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_66(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 1)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_67(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "XXtimeXX": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_68(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "TIME": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_69(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(None),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_70(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get(None, 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_71(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", None)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_72(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get(0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_73(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", )),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_74(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("XXtimeXX", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_75(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("TIME", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_76(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 1)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_77(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "XXtestcasesXX": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_78(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "TESTCASES": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_79(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall(None):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_80(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("XXtestcaseXX"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_81(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("TESTCASE"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_82(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = None
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_83(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "XXnameXX": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_84(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "NAME": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_85(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get(None),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_86(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("XXnameXX"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_87(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("NAME"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_88(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "XXclassnameXX": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_89(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "CLASSNAME": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_90(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get(None),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_91(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("XXclassnameXX"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_92(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("CLASSNAME"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_93(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "XXtimeXX": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_94(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "TIME": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_95(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(None),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_96(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get(None, 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_97(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", None)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_98(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get(0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_99(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", )),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_100(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("XXtimeXX", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_101(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("TIME", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_102(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 1)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_103(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "XXpassedXX": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_104(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "PASSED": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_105(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": False,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_106(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "XXfailureXX": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_107(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "FAILURE": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_108(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "XXerrorXX": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_109(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "ERROR": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_110(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "XXskippedXX": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_111(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "SKIPPED": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_112(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": True,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_113(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = None
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_114(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find(None)
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_115(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.rfind("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_116(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("XXfailureXX")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_117(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("FAILURE")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_118(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_119(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = None
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_120(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["XXpassedXX"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_121(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["PASSED"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_122(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = True
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_123(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = None
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_124(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["XXfailureXX"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_125(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["FAILURE"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_126(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "XXmessageXX": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_127(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "MESSAGE": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_128(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get(None),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_129(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("XXmessageXX"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_130(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("MESSAGE"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_131(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "XXtypeXX": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_132(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "TYPE": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_133(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get(None),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_134(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("XXtypeXX"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_135(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("TYPE"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_136(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "XXtextXX": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_137(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "TEXT": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_138(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = None
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_139(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find(None)
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_140(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.rfind("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_141(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("XXerrorXX")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_142(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("ERROR")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_143(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_144(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = None
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_145(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["XXpassedXX"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_146(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["PASSED"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_147(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = True
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_148(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = None
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_149(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["XXerrorXX"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_150(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["ERROR"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_151(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "XXmessageXX": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_152(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "MESSAGE": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_153(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get(None),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_154(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("XXmessageXX"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_155(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("MESSAGE"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_156(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "XXtypeXX": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_157(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "TYPE": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_158(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get(None),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_159(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("XXtypeXX"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_160(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("TYPE"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_161(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "XXtextXX": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_162(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "TEXT": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_163(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = None
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_164(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find(None)
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_165(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.rfind("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_166(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("XXskippedXX")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_167(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("SKIPPED")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_168(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_169(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = None
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_170(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["XXskippedXX"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_171(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["SKIPPED"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_172(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = False
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_173(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(None)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_174(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["XXtestcasesXX"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_175(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["TESTCASES"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_176(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(None)
                
            except Exception as e:
                logger.error(f"Failed to parse JUnit XML {xml_file}: {e}")
        
        return results
    
    def xǁMavenRunnerǁparse_junit_xml__mutmut_177(self, surefire_reports_dir: Optional[str] = None) -> List[Dict[str, Any]]:
        """
        Parse JUnit XML reports.
        
        Args:
            surefire_reports_dir: Path to surefire-reports directory
            
        Returns:
            List of test result dictionaries
        """
        if surefire_reports_dir is None:
            surefire_reports_dir = self.project_dir / "target" / "surefire-reports"
        else:
            surefire_reports_dir = Path(surefire_reports_dir)
        
        if not surefire_reports_dir.exists():
            logger.warning(f"Surefire reports directory not found: {surefire_reports_dir}")
            return []
        
        results = []
        
        # Find all TEST-*.xml files
        for xml_file in surefire_reports_dir.glob("TEST-*.xml"):
            try:
                tree = ET.parse(xml_file)
                root = tree.getroot()
                
                # Parse testsuite
                testsuite = {
                    "name": root.get("name"),
                    "tests": int(root.get("tests", 0)),
                    "failures": int(root.get("failures", 0)),
                    "errors": int(root.get("errors", 0)),
                    "skipped": int(root.get("skipped", 0)),
                    "time": float(root.get("time", 0)),
                    "testcases": [],
                }
                
                # Parse testcases
                for testcase in root.findall("testcase"):
                    tc = {
                        "name": testcase.get("name"),
                        "classname": testcase.get("classname"),
                        "time": float(testcase.get("time", 0)),
                        "passed": True,
                        "failure": None,
                        "error": None,
                        "skipped": False,
                    }
                    
                    # Check for failure
                    failure = testcase.find("failure")
                    if failure is not None:
                        tc["passed"] = False
                        tc["failure"] = {
                            "message": failure.get("message"),
                            "type": failure.get("type"),
                            "text": failure.text,
                        }
                    
                    # Check for error
                    error = testcase.find("error")
                    if error is not None:
                        tc["passed"] = False
                        tc["error"] = {
                            "message": error.get("message"),
                            "type": error.get("type"),
                            "text": error.text,
                        }
                    
                    # Check for skipped
                    skipped = testcase.find("skipped")
                    if skipped is not None:
                        tc["skipped"] = True
                    
                    testsuite["testcases"].append(tc)
                
                results.append(testsuite)
                
            except Exception as e:
                logger.error(None)
        
        return results
    
    xǁMavenRunnerǁparse_junit_xml__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁMavenRunnerǁparse_junit_xml__mutmut_1': xǁMavenRunnerǁparse_junit_xml__mutmut_1, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_2': xǁMavenRunnerǁparse_junit_xml__mutmut_2, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_3': xǁMavenRunnerǁparse_junit_xml__mutmut_3, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_4': xǁMavenRunnerǁparse_junit_xml__mutmut_4, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_5': xǁMavenRunnerǁparse_junit_xml__mutmut_5, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_6': xǁMavenRunnerǁparse_junit_xml__mutmut_6, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_7': xǁMavenRunnerǁparse_junit_xml__mutmut_7, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_8': xǁMavenRunnerǁparse_junit_xml__mutmut_8, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_9': xǁMavenRunnerǁparse_junit_xml__mutmut_9, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_10': xǁMavenRunnerǁparse_junit_xml__mutmut_10, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_11': xǁMavenRunnerǁparse_junit_xml__mutmut_11, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_12': xǁMavenRunnerǁparse_junit_xml__mutmut_12, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_13': xǁMavenRunnerǁparse_junit_xml__mutmut_13, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_14': xǁMavenRunnerǁparse_junit_xml__mutmut_14, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_15': xǁMavenRunnerǁparse_junit_xml__mutmut_15, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_16': xǁMavenRunnerǁparse_junit_xml__mutmut_16, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_17': xǁMavenRunnerǁparse_junit_xml__mutmut_17, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_18': xǁMavenRunnerǁparse_junit_xml__mutmut_18, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_19': xǁMavenRunnerǁparse_junit_xml__mutmut_19, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_20': xǁMavenRunnerǁparse_junit_xml__mutmut_20, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_21': xǁMavenRunnerǁparse_junit_xml__mutmut_21, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_22': xǁMavenRunnerǁparse_junit_xml__mutmut_22, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_23': xǁMavenRunnerǁparse_junit_xml__mutmut_23, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_24': xǁMavenRunnerǁparse_junit_xml__mutmut_24, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_25': xǁMavenRunnerǁparse_junit_xml__mutmut_25, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_26': xǁMavenRunnerǁparse_junit_xml__mutmut_26, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_27': xǁMavenRunnerǁparse_junit_xml__mutmut_27, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_28': xǁMavenRunnerǁparse_junit_xml__mutmut_28, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_29': xǁMavenRunnerǁparse_junit_xml__mutmut_29, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_30': xǁMavenRunnerǁparse_junit_xml__mutmut_30, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_31': xǁMavenRunnerǁparse_junit_xml__mutmut_31, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_32': xǁMavenRunnerǁparse_junit_xml__mutmut_32, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_33': xǁMavenRunnerǁparse_junit_xml__mutmut_33, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_34': xǁMavenRunnerǁparse_junit_xml__mutmut_34, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_35': xǁMavenRunnerǁparse_junit_xml__mutmut_35, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_36': xǁMavenRunnerǁparse_junit_xml__mutmut_36, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_37': xǁMavenRunnerǁparse_junit_xml__mutmut_37, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_38': xǁMavenRunnerǁparse_junit_xml__mutmut_38, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_39': xǁMavenRunnerǁparse_junit_xml__mutmut_39, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_40': xǁMavenRunnerǁparse_junit_xml__mutmut_40, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_41': xǁMavenRunnerǁparse_junit_xml__mutmut_41, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_42': xǁMavenRunnerǁparse_junit_xml__mutmut_42, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_43': xǁMavenRunnerǁparse_junit_xml__mutmut_43, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_44': xǁMavenRunnerǁparse_junit_xml__mutmut_44, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_45': xǁMavenRunnerǁparse_junit_xml__mutmut_45, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_46': xǁMavenRunnerǁparse_junit_xml__mutmut_46, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_47': xǁMavenRunnerǁparse_junit_xml__mutmut_47, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_48': xǁMavenRunnerǁparse_junit_xml__mutmut_48, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_49': xǁMavenRunnerǁparse_junit_xml__mutmut_49, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_50': xǁMavenRunnerǁparse_junit_xml__mutmut_50, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_51': xǁMavenRunnerǁparse_junit_xml__mutmut_51, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_52': xǁMavenRunnerǁparse_junit_xml__mutmut_52, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_53': xǁMavenRunnerǁparse_junit_xml__mutmut_53, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_54': xǁMavenRunnerǁparse_junit_xml__mutmut_54, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_55': xǁMavenRunnerǁparse_junit_xml__mutmut_55, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_56': xǁMavenRunnerǁparse_junit_xml__mutmut_56, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_57': xǁMavenRunnerǁparse_junit_xml__mutmut_57, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_58': xǁMavenRunnerǁparse_junit_xml__mutmut_58, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_59': xǁMavenRunnerǁparse_junit_xml__mutmut_59, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_60': xǁMavenRunnerǁparse_junit_xml__mutmut_60, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_61': xǁMavenRunnerǁparse_junit_xml__mutmut_61, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_62': xǁMavenRunnerǁparse_junit_xml__mutmut_62, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_63': xǁMavenRunnerǁparse_junit_xml__mutmut_63, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_64': xǁMavenRunnerǁparse_junit_xml__mutmut_64, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_65': xǁMavenRunnerǁparse_junit_xml__mutmut_65, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_66': xǁMavenRunnerǁparse_junit_xml__mutmut_66, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_67': xǁMavenRunnerǁparse_junit_xml__mutmut_67, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_68': xǁMavenRunnerǁparse_junit_xml__mutmut_68, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_69': xǁMavenRunnerǁparse_junit_xml__mutmut_69, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_70': xǁMavenRunnerǁparse_junit_xml__mutmut_70, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_71': xǁMavenRunnerǁparse_junit_xml__mutmut_71, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_72': xǁMavenRunnerǁparse_junit_xml__mutmut_72, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_73': xǁMavenRunnerǁparse_junit_xml__mutmut_73, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_74': xǁMavenRunnerǁparse_junit_xml__mutmut_74, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_75': xǁMavenRunnerǁparse_junit_xml__mutmut_75, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_76': xǁMavenRunnerǁparse_junit_xml__mutmut_76, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_77': xǁMavenRunnerǁparse_junit_xml__mutmut_77, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_78': xǁMavenRunnerǁparse_junit_xml__mutmut_78, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_79': xǁMavenRunnerǁparse_junit_xml__mutmut_79, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_80': xǁMavenRunnerǁparse_junit_xml__mutmut_80, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_81': xǁMavenRunnerǁparse_junit_xml__mutmut_81, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_82': xǁMavenRunnerǁparse_junit_xml__mutmut_82, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_83': xǁMavenRunnerǁparse_junit_xml__mutmut_83, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_84': xǁMavenRunnerǁparse_junit_xml__mutmut_84, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_85': xǁMavenRunnerǁparse_junit_xml__mutmut_85, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_86': xǁMavenRunnerǁparse_junit_xml__mutmut_86, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_87': xǁMavenRunnerǁparse_junit_xml__mutmut_87, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_88': xǁMavenRunnerǁparse_junit_xml__mutmut_88, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_89': xǁMavenRunnerǁparse_junit_xml__mutmut_89, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_90': xǁMavenRunnerǁparse_junit_xml__mutmut_90, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_91': xǁMavenRunnerǁparse_junit_xml__mutmut_91, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_92': xǁMavenRunnerǁparse_junit_xml__mutmut_92, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_93': xǁMavenRunnerǁparse_junit_xml__mutmut_93, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_94': xǁMavenRunnerǁparse_junit_xml__mutmut_94, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_95': xǁMavenRunnerǁparse_junit_xml__mutmut_95, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_96': xǁMavenRunnerǁparse_junit_xml__mutmut_96, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_97': xǁMavenRunnerǁparse_junit_xml__mutmut_97, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_98': xǁMavenRunnerǁparse_junit_xml__mutmut_98, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_99': xǁMavenRunnerǁparse_junit_xml__mutmut_99, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_100': xǁMavenRunnerǁparse_junit_xml__mutmut_100, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_101': xǁMavenRunnerǁparse_junit_xml__mutmut_101, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_102': xǁMavenRunnerǁparse_junit_xml__mutmut_102, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_103': xǁMavenRunnerǁparse_junit_xml__mutmut_103, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_104': xǁMavenRunnerǁparse_junit_xml__mutmut_104, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_105': xǁMavenRunnerǁparse_junit_xml__mutmut_105, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_106': xǁMavenRunnerǁparse_junit_xml__mutmut_106, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_107': xǁMavenRunnerǁparse_junit_xml__mutmut_107, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_108': xǁMavenRunnerǁparse_junit_xml__mutmut_108, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_109': xǁMavenRunnerǁparse_junit_xml__mutmut_109, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_110': xǁMavenRunnerǁparse_junit_xml__mutmut_110, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_111': xǁMavenRunnerǁparse_junit_xml__mutmut_111, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_112': xǁMavenRunnerǁparse_junit_xml__mutmut_112, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_113': xǁMavenRunnerǁparse_junit_xml__mutmut_113, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_114': xǁMavenRunnerǁparse_junit_xml__mutmut_114, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_115': xǁMavenRunnerǁparse_junit_xml__mutmut_115, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_116': xǁMavenRunnerǁparse_junit_xml__mutmut_116, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_117': xǁMavenRunnerǁparse_junit_xml__mutmut_117, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_118': xǁMavenRunnerǁparse_junit_xml__mutmut_118, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_119': xǁMavenRunnerǁparse_junit_xml__mutmut_119, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_120': xǁMavenRunnerǁparse_junit_xml__mutmut_120, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_121': xǁMavenRunnerǁparse_junit_xml__mutmut_121, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_122': xǁMavenRunnerǁparse_junit_xml__mutmut_122, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_123': xǁMavenRunnerǁparse_junit_xml__mutmut_123, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_124': xǁMavenRunnerǁparse_junit_xml__mutmut_124, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_125': xǁMavenRunnerǁparse_junit_xml__mutmut_125, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_126': xǁMavenRunnerǁparse_junit_xml__mutmut_126, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_127': xǁMavenRunnerǁparse_junit_xml__mutmut_127, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_128': xǁMavenRunnerǁparse_junit_xml__mutmut_128, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_129': xǁMavenRunnerǁparse_junit_xml__mutmut_129, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_130': xǁMavenRunnerǁparse_junit_xml__mutmut_130, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_131': xǁMavenRunnerǁparse_junit_xml__mutmut_131, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_132': xǁMavenRunnerǁparse_junit_xml__mutmut_132, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_133': xǁMavenRunnerǁparse_junit_xml__mutmut_133, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_134': xǁMavenRunnerǁparse_junit_xml__mutmut_134, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_135': xǁMavenRunnerǁparse_junit_xml__mutmut_135, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_136': xǁMavenRunnerǁparse_junit_xml__mutmut_136, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_137': xǁMavenRunnerǁparse_junit_xml__mutmut_137, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_138': xǁMavenRunnerǁparse_junit_xml__mutmut_138, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_139': xǁMavenRunnerǁparse_junit_xml__mutmut_139, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_140': xǁMavenRunnerǁparse_junit_xml__mutmut_140, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_141': xǁMavenRunnerǁparse_junit_xml__mutmut_141, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_142': xǁMavenRunnerǁparse_junit_xml__mutmut_142, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_143': xǁMavenRunnerǁparse_junit_xml__mutmut_143, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_144': xǁMavenRunnerǁparse_junit_xml__mutmut_144, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_145': xǁMavenRunnerǁparse_junit_xml__mutmut_145, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_146': xǁMavenRunnerǁparse_junit_xml__mutmut_146, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_147': xǁMavenRunnerǁparse_junit_xml__mutmut_147, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_148': xǁMavenRunnerǁparse_junit_xml__mutmut_148, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_149': xǁMavenRunnerǁparse_junit_xml__mutmut_149, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_150': xǁMavenRunnerǁparse_junit_xml__mutmut_150, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_151': xǁMavenRunnerǁparse_junit_xml__mutmut_151, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_152': xǁMavenRunnerǁparse_junit_xml__mutmut_152, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_153': xǁMavenRunnerǁparse_junit_xml__mutmut_153, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_154': xǁMavenRunnerǁparse_junit_xml__mutmut_154, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_155': xǁMavenRunnerǁparse_junit_xml__mutmut_155, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_156': xǁMavenRunnerǁparse_junit_xml__mutmut_156, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_157': xǁMavenRunnerǁparse_junit_xml__mutmut_157, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_158': xǁMavenRunnerǁparse_junit_xml__mutmut_158, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_159': xǁMavenRunnerǁparse_junit_xml__mutmut_159, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_160': xǁMavenRunnerǁparse_junit_xml__mutmut_160, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_161': xǁMavenRunnerǁparse_junit_xml__mutmut_161, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_162': xǁMavenRunnerǁparse_junit_xml__mutmut_162, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_163': xǁMavenRunnerǁparse_junit_xml__mutmut_163, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_164': xǁMavenRunnerǁparse_junit_xml__mutmut_164, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_165': xǁMavenRunnerǁparse_junit_xml__mutmut_165, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_166': xǁMavenRunnerǁparse_junit_xml__mutmut_166, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_167': xǁMavenRunnerǁparse_junit_xml__mutmut_167, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_168': xǁMavenRunnerǁparse_junit_xml__mutmut_168, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_169': xǁMavenRunnerǁparse_junit_xml__mutmut_169, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_170': xǁMavenRunnerǁparse_junit_xml__mutmut_170, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_171': xǁMavenRunnerǁparse_junit_xml__mutmut_171, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_172': xǁMavenRunnerǁparse_junit_xml__mutmut_172, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_173': xǁMavenRunnerǁparse_junit_xml__mutmut_173, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_174': xǁMavenRunnerǁparse_junit_xml__mutmut_174, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_175': xǁMavenRunnerǁparse_junit_xml__mutmut_175, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_176': xǁMavenRunnerǁparse_junit_xml__mutmut_176, 
        'xǁMavenRunnerǁparse_junit_xml__mutmut_177': xǁMavenRunnerǁparse_junit_xml__mutmut_177
    }
    
    def parse_junit_xml(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁMavenRunnerǁparse_junit_xml__mutmut_orig"), object.__getattribute__(self, "xǁMavenRunnerǁparse_junit_xml__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_junit_xml.__signature__ = _mutmut_signature(xǁMavenRunnerǁparse_junit_xml__mutmut_orig)
    xǁMavenRunnerǁparse_junit_xml__mutmut_orig.__name__ = 'xǁMavenRunnerǁparse_junit_xml'


class RunnerAgent(BaseAgent):
    """
    Runner Agent executes Maven tests and analyzes results.
    
    Workflow:
    1. Retrieve generated tests from ContextManager
    2. Write test files to disk
    3. Execute Maven tests
    4. Parse JUnit XML results
    5. Analyze failures
    6. Store execution results in ContextManager
    7. Trigger regeneration for failed tests (feedback loop)
    
    Features:
    - Maven test execution
    - JUnit XML parsing
    - Failure analysis
    - Feedback loop for test regeneration
    - Retry logic with exponential backoff
    - Parallel test execution support
    - Timeout handling
    """
    
    def xǁRunnerAgentǁ__init____mutmut_orig(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_1(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 3,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_2(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 301,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_3(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=None,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_4(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=None,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_5(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=None,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_6(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=None,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_7(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=None,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_8(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_9(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_10(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_11(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_12(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_13(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = None
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_14(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(None)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_15(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = None
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_16(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(None, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_17(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, None)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_18(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_19(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, )
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_20(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = None
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_21(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = None
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_22(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = None
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_23(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["XXtests_runXX"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_24(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["TESTS_RUN"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_25(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 1
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_26(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = None
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_27(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["XXtests_passedXX"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_28(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["TESTS_PASSED"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_29(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 1
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_30(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = None
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_31(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["XXtests_failedXX"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_32(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["TESTS_FAILED"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_33(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 1
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_34(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = None
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_35(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["XXexecution_time_msXX"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_36(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["EXECUTION_TIME_MS"] = 0
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_37(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 1
        self._metrics["retries"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_38(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = None
    
    def xǁRunnerAgentǁ__init____mutmut_39(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["XXretriesXX"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_40(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["RETRIES"] = 0
    
    def xǁRunnerAgentǁ__init____mutmut_41(
        self,
        config: AgentConfig,
        context_manager: ContextManager,
        message_router,
        event_bus,
        task_queue,
        project_dir: str,
        maven_home: Optional[str] = None,
        max_retries: int = 2,
        timeout: int = 300,
    ):
        """
        Initialize Runner Agent.
        
        Args:
            config: Agent configuration
            context_manager: Shared context manager
            message_router: Message router for inter-agent communication
            event_bus: Event bus for publishing events
            task_queue: Task queue for processing
            project_dir: Maven project directory
            maven_home: Optional Maven installation path
            max_retries: Maximum retries for failed tests
            timeout: Test execution timeout in seconds
        """
        super().__init__(
            config=config,
            context_manager=context_manager,
            router=message_router,
            event_bus=event_bus,
            task_queue=task_queue,
        )
        
        self.project_dir = Path(project_dir)
        self.maven_runner = MavenRunner(project_dir, maven_home)
        self.max_retries = max_retries
        self.timeout = timeout
        
        # Metrics
        self._metrics["tests_run"] = 0
        self._metrics["tests_passed"] = 0
        self._metrics["tests_failed"] = 0
        self._metrics["execution_time_ms"] = 0
        self._metrics["retries"] = 1
    
    xǁRunnerAgentǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ__init____mutmut_1': xǁRunnerAgentǁ__init____mutmut_1, 
        'xǁRunnerAgentǁ__init____mutmut_2': xǁRunnerAgentǁ__init____mutmut_2, 
        'xǁRunnerAgentǁ__init____mutmut_3': xǁRunnerAgentǁ__init____mutmut_3, 
        'xǁRunnerAgentǁ__init____mutmut_4': xǁRunnerAgentǁ__init____mutmut_4, 
        'xǁRunnerAgentǁ__init____mutmut_5': xǁRunnerAgentǁ__init____mutmut_5, 
        'xǁRunnerAgentǁ__init____mutmut_6': xǁRunnerAgentǁ__init____mutmut_6, 
        'xǁRunnerAgentǁ__init____mutmut_7': xǁRunnerAgentǁ__init____mutmut_7, 
        'xǁRunnerAgentǁ__init____mutmut_8': xǁRunnerAgentǁ__init____mutmut_8, 
        'xǁRunnerAgentǁ__init____mutmut_9': xǁRunnerAgentǁ__init____mutmut_9, 
        'xǁRunnerAgentǁ__init____mutmut_10': xǁRunnerAgentǁ__init____mutmut_10, 
        'xǁRunnerAgentǁ__init____mutmut_11': xǁRunnerAgentǁ__init____mutmut_11, 
        'xǁRunnerAgentǁ__init____mutmut_12': xǁRunnerAgentǁ__init____mutmut_12, 
        'xǁRunnerAgentǁ__init____mutmut_13': xǁRunnerAgentǁ__init____mutmut_13, 
        'xǁRunnerAgentǁ__init____mutmut_14': xǁRunnerAgentǁ__init____mutmut_14, 
        'xǁRunnerAgentǁ__init____mutmut_15': xǁRunnerAgentǁ__init____mutmut_15, 
        'xǁRunnerAgentǁ__init____mutmut_16': xǁRunnerAgentǁ__init____mutmut_16, 
        'xǁRunnerAgentǁ__init____mutmut_17': xǁRunnerAgentǁ__init____mutmut_17, 
        'xǁRunnerAgentǁ__init____mutmut_18': xǁRunnerAgentǁ__init____mutmut_18, 
        'xǁRunnerAgentǁ__init____mutmut_19': xǁRunnerAgentǁ__init____mutmut_19, 
        'xǁRunnerAgentǁ__init____mutmut_20': xǁRunnerAgentǁ__init____mutmut_20, 
        'xǁRunnerAgentǁ__init____mutmut_21': xǁRunnerAgentǁ__init____mutmut_21, 
        'xǁRunnerAgentǁ__init____mutmut_22': xǁRunnerAgentǁ__init____mutmut_22, 
        'xǁRunnerAgentǁ__init____mutmut_23': xǁRunnerAgentǁ__init____mutmut_23, 
        'xǁRunnerAgentǁ__init____mutmut_24': xǁRunnerAgentǁ__init____mutmut_24, 
        'xǁRunnerAgentǁ__init____mutmut_25': xǁRunnerAgentǁ__init____mutmut_25, 
        'xǁRunnerAgentǁ__init____mutmut_26': xǁRunnerAgentǁ__init____mutmut_26, 
        'xǁRunnerAgentǁ__init____mutmut_27': xǁRunnerAgentǁ__init____mutmut_27, 
        'xǁRunnerAgentǁ__init____mutmut_28': xǁRunnerAgentǁ__init____mutmut_28, 
        'xǁRunnerAgentǁ__init____mutmut_29': xǁRunnerAgentǁ__init____mutmut_29, 
        'xǁRunnerAgentǁ__init____mutmut_30': xǁRunnerAgentǁ__init____mutmut_30, 
        'xǁRunnerAgentǁ__init____mutmut_31': xǁRunnerAgentǁ__init____mutmut_31, 
        'xǁRunnerAgentǁ__init____mutmut_32': xǁRunnerAgentǁ__init____mutmut_32, 
        'xǁRunnerAgentǁ__init____mutmut_33': xǁRunnerAgentǁ__init____mutmut_33, 
        'xǁRunnerAgentǁ__init____mutmut_34': xǁRunnerAgentǁ__init____mutmut_34, 
        'xǁRunnerAgentǁ__init____mutmut_35': xǁRunnerAgentǁ__init____mutmut_35, 
        'xǁRunnerAgentǁ__init____mutmut_36': xǁRunnerAgentǁ__init____mutmut_36, 
        'xǁRunnerAgentǁ__init____mutmut_37': xǁRunnerAgentǁ__init____mutmut_37, 
        'xǁRunnerAgentǁ__init____mutmut_38': xǁRunnerAgentǁ__init____mutmut_38, 
        'xǁRunnerAgentǁ__init____mutmut_39': xǁRunnerAgentǁ__init____mutmut_39, 
        'xǁRunnerAgentǁ__init____mutmut_40': xǁRunnerAgentǁ__init____mutmut_40, 
        'xǁRunnerAgentǁ__init____mutmut_41': xǁRunnerAgentǁ__init____mutmut_41
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁRunnerAgentǁ__init____mutmut_orig)
    xǁRunnerAgentǁ__init____mutmut_orig.__name__ = 'xǁRunnerAgentǁ__init__'
    
    def xǁRunnerAgentǁregister_handlers__mutmut_orig(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_1(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=None,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_2(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type=None,
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_3(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=None,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_4(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_5(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_6(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_7(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="XXexecute_testsXX",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_8(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="EXECUTE_TESTS",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_9(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=None,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_10(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type=None,
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_11(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=None,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_12(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_13(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_14(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_15(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="XXexecute_single_testXX",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_16(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="EXECUTE_SINGLE_TEST",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_17(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=None,
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_18(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type=None,
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_19(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            handler=None,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_20(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            message_type="analyze_failures",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_21(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_22(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="analyze_failures",
            )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_23(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="XXanalyze_failuresXX",
            handler=self._handle_analyze_failures_message,
        )
    
    def xǁRunnerAgentǁregister_handlers__mutmut_24(self) -> None:
        """Register message handlers for test execution."""
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_tests",
            handler=self._handle_execute_tests_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="execute_single_test",
            handler=self._handle_execute_single_test_message,
        )
        
        self.message_router.register(
            agent_type=AgentType.RUNNER,
            message_type="ANALYZE_FAILURES",
            handler=self._handle_analyze_failures_message,
        )
    
    xǁRunnerAgentǁregister_handlers__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁregister_handlers__mutmut_1': xǁRunnerAgentǁregister_handlers__mutmut_1, 
        'xǁRunnerAgentǁregister_handlers__mutmut_2': xǁRunnerAgentǁregister_handlers__mutmut_2, 
        'xǁRunnerAgentǁregister_handlers__mutmut_3': xǁRunnerAgentǁregister_handlers__mutmut_3, 
        'xǁRunnerAgentǁregister_handlers__mutmut_4': xǁRunnerAgentǁregister_handlers__mutmut_4, 
        'xǁRunnerAgentǁregister_handlers__mutmut_5': xǁRunnerAgentǁregister_handlers__mutmut_5, 
        'xǁRunnerAgentǁregister_handlers__mutmut_6': xǁRunnerAgentǁregister_handlers__mutmut_6, 
        'xǁRunnerAgentǁregister_handlers__mutmut_7': xǁRunnerAgentǁregister_handlers__mutmut_7, 
        'xǁRunnerAgentǁregister_handlers__mutmut_8': xǁRunnerAgentǁregister_handlers__mutmut_8, 
        'xǁRunnerAgentǁregister_handlers__mutmut_9': xǁRunnerAgentǁregister_handlers__mutmut_9, 
        'xǁRunnerAgentǁregister_handlers__mutmut_10': xǁRunnerAgentǁregister_handlers__mutmut_10, 
        'xǁRunnerAgentǁregister_handlers__mutmut_11': xǁRunnerAgentǁregister_handlers__mutmut_11, 
        'xǁRunnerAgentǁregister_handlers__mutmut_12': xǁRunnerAgentǁregister_handlers__mutmut_12, 
        'xǁRunnerAgentǁregister_handlers__mutmut_13': xǁRunnerAgentǁregister_handlers__mutmut_13, 
        'xǁRunnerAgentǁregister_handlers__mutmut_14': xǁRunnerAgentǁregister_handlers__mutmut_14, 
        'xǁRunnerAgentǁregister_handlers__mutmut_15': xǁRunnerAgentǁregister_handlers__mutmut_15, 
        'xǁRunnerAgentǁregister_handlers__mutmut_16': xǁRunnerAgentǁregister_handlers__mutmut_16, 
        'xǁRunnerAgentǁregister_handlers__mutmut_17': xǁRunnerAgentǁregister_handlers__mutmut_17, 
        'xǁRunnerAgentǁregister_handlers__mutmut_18': xǁRunnerAgentǁregister_handlers__mutmut_18, 
        'xǁRunnerAgentǁregister_handlers__mutmut_19': xǁRunnerAgentǁregister_handlers__mutmut_19, 
        'xǁRunnerAgentǁregister_handlers__mutmut_20': xǁRunnerAgentǁregister_handlers__mutmut_20, 
        'xǁRunnerAgentǁregister_handlers__mutmut_21': xǁRunnerAgentǁregister_handlers__mutmut_21, 
        'xǁRunnerAgentǁregister_handlers__mutmut_22': xǁRunnerAgentǁregister_handlers__mutmut_22, 
        'xǁRunnerAgentǁregister_handlers__mutmut_23': xǁRunnerAgentǁregister_handlers__mutmut_23, 
        'xǁRunnerAgentǁregister_handlers__mutmut_24': xǁRunnerAgentǁregister_handlers__mutmut_24
    }
    
    def register_handlers(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁregister_handlers__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁregister_handlers__mutmut_mutants"), args, kwargs, self)
        return result 
    
    register_handlers.__signature__ = _mutmut_signature(xǁRunnerAgentǁregister_handlers__mutmut_orig)
    xǁRunnerAgentǁregister_handlers__mutmut_orig.__name__ = 'xǁRunnerAgentǁregister_handlers'
    
    async def xǁRunnerAgentǁprocess_task__mutmut_orig(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_1(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = None
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_2(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type != "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_3(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "XXexecute_testsXX":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_4(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "EXECUTE_TESTS":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_5(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(None)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_6(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type != "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_7(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "XXexecute_single_testXX":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_8(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "EXECUTE_SINGLE_TEST":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_9(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(None)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_10(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type != "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_11(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "XXanalyze_failuresXX":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_12(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "ANALYZE_FAILURES":
            return await self._analyze_failures(task)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_13(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(None)
        else:
            raise ValueError(f"Unsupported task type: {task_type}")
    
    async def xǁRunnerAgentǁprocess_task__mutmut_14(self, task: Task) -> Dict[str, Any]:
        """
        Process test execution tasks.
        
        Args:
            task: Task to process
            
        Returns:
            Task result dictionary
        """
        task_type = task.task_type
        
        if task_type == "execute_tests":
            return await self._execute_tests(task)
        elif task_type == "execute_single_test":
            return await self._execute_single_test(task)
        elif task_type == "analyze_failures":
            return await self._analyze_failures(task)
        else:
            raise ValueError(None)
    
    xǁRunnerAgentǁprocess_task__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁprocess_task__mutmut_1': xǁRunnerAgentǁprocess_task__mutmut_1, 
        'xǁRunnerAgentǁprocess_task__mutmut_2': xǁRunnerAgentǁprocess_task__mutmut_2, 
        'xǁRunnerAgentǁprocess_task__mutmut_3': xǁRunnerAgentǁprocess_task__mutmut_3, 
        'xǁRunnerAgentǁprocess_task__mutmut_4': xǁRunnerAgentǁprocess_task__mutmut_4, 
        'xǁRunnerAgentǁprocess_task__mutmut_5': xǁRunnerAgentǁprocess_task__mutmut_5, 
        'xǁRunnerAgentǁprocess_task__mutmut_6': xǁRunnerAgentǁprocess_task__mutmut_6, 
        'xǁRunnerAgentǁprocess_task__mutmut_7': xǁRunnerAgentǁprocess_task__mutmut_7, 
        'xǁRunnerAgentǁprocess_task__mutmut_8': xǁRunnerAgentǁprocess_task__mutmut_8, 
        'xǁRunnerAgentǁprocess_task__mutmut_9': xǁRunnerAgentǁprocess_task__mutmut_9, 
        'xǁRunnerAgentǁprocess_task__mutmut_10': xǁRunnerAgentǁprocess_task__mutmut_10, 
        'xǁRunnerAgentǁprocess_task__mutmut_11': xǁRunnerAgentǁprocess_task__mutmut_11, 
        'xǁRunnerAgentǁprocess_task__mutmut_12': xǁRunnerAgentǁprocess_task__mutmut_12, 
        'xǁRunnerAgentǁprocess_task__mutmut_13': xǁRunnerAgentǁprocess_task__mutmut_13, 
        'xǁRunnerAgentǁprocess_task__mutmut_14': xǁRunnerAgentǁprocess_task__mutmut_14
    }
    
    def process_task(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁprocess_task__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁprocess_task__mutmut_mutants"), args, kwargs, self)
        return result 
    
    process_task.__signature__ = _mutmut_signature(xǁRunnerAgentǁprocess_task__mutmut_orig)
    xǁRunnerAgentǁprocess_task__mutmut_orig.__name__ = 'xǁRunnerAgentǁprocess_task'
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_orig(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_1(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = None
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_2(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get(None, [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_3(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", None)
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_4(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get([])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_5(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", )
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_6(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("XXtest_idsXX", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_7(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("TEST_IDS", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_8(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = None
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_9(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get(None)
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_10(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("XXsession_idXX")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_11(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("SESSION_ID")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_12(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_13(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning(None)
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_14(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("XXNo test IDs provided for executionXX")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_15(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("no test ids provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_16(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("NO TEST IDS PROVIDED FOR EXECUTION")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_17(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"XXstatusXX": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_18(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"STATUS": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_19(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "XXerrorXX", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_20(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "ERROR", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_21(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "XXerrorXX": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_22(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "ERROR": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_23(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "XXNo test IDs providedXX"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_24(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "no test ids provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_25(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "NO TEST IDS PROVIDED"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_26(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(None)
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_27(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = None
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_28(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = None
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_29(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=None
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_30(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(None) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_31(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(None)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_32(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_33(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning(None)
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_34(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("XXNo tests found to executeXX")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_35(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("no tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_36(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("NO TESTS FOUND TO EXECUTE")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_37(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"XXstatusXX": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_38(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"STATUS": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_39(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "XXerrorXX", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_40(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "ERROR", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_41(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "XXerrorXX": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_42(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "ERROR": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_43(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "XXNo tests foundXX"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_44(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "no tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_45(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "NO TESTS FOUND"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_46(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(None)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_47(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = None
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_48(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = None
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_49(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=None,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_50(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=None,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_51(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_52(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_53(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = None
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_54(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = None
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_55(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = None
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_56(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(None, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_57(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, None, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_58(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, None)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_59(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_60(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_61(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, )
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_62(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(None)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_63(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(None)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_64(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] = metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_65(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] -= metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_66(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["XXtests_runXX"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_67(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["TESTS_RUN"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_68(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get(None, 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_69(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", None)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_70(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get(0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_71(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", )
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_72(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("XXtests_runXX", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_73(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("TESTS_RUN", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_74(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 1)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_75(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] = metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_76(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] -= metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_77(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["XXtests_passedXX"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_78(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["TESTS_PASSED"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_79(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get(None, 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_80(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", None)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_81(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get(0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_82(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", )
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_83(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("XXtests_passedXX", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_84(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("TESTS_PASSED", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_85(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 1)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_86(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] = metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_87(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] -= metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_88(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["XXtests_failedXX"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_89(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["TESTS_FAILED"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_90(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get(None, 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_91(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", None)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_92(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get(0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_93(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", )
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_94(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("XXtests_failedXX", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_95(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("TESTS_FAILED", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_96(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 1)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_97(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] = metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_98(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] -= metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_99(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["XXexecution_time_msXX"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_100(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["EXECUTION_TIME_MS"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_101(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get(None, 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_102(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", None)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_103(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get(0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_104(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", )
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_105(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("XXexecution_time_msXX", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_106(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("EXECUTION_TIME_MS", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_107(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 1)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_108(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = None
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_109(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_110(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests or session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_111(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(None, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_112(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, None)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_113(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_114(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, )
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_115(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type=None,
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_116(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data=None,
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_117(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_118(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_119(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="XXtests_executedXX",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_120(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="TESTS_EXECUTED",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_121(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "XXsession_idXX": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_122(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "SESSION_ID": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_123(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(None),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_124(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "XXtests_runXX": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_125(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "TESTS_RUN": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_126(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "XXtests_passedXX": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_127(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "TESTS_PASSED": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_128(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "XXtests_failedXX": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_129(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "TESTS_FAILED": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_130(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "XXexecution_time_msXX": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_131(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "EXECUTION_TIME_MS": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_132(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get(None, 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_133(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", None),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_134(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get(0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_135(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", ),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_136(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("XXexecution_time_msXX", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_137(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("EXECUTION_TIME_MS", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_138(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 1),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_139(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "XXstatusXX": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_140(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "STATUS": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_141(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "XXsuccessXX",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_142(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "SUCCESS",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_143(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "XXtests_runXX": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_144(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "TESTS_RUN": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_145(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "XXtests_passedXX": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_146(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "TESTS_PASSED": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_147(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "XXtests_failedXX": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_148(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "TESTS_FAILED": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_149(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "XXexecution_time_msXX": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_150(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "EXECUTION_TIME_MS": metrics.get("execution_time_ms", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_151(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get(None, 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_152(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", None),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_153(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get(0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_154(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", ),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_155(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("XXexecution_time_msXX", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_156(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("EXECUTION_TIME_MS", 0),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_157(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 1),
            "maven_success": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_158(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "XXmaven_successXX": success,
        }
    
    async def xǁRunnerAgentǁ_execute_tests__mutmut_159(self, task: Task) -> Dict[str, Any]:
        """
        Execute multiple tests.
        
        Args:
            task: Task with test_ids in payload
            
        Returns:
            Result with execution summary
        """
        test_ids = task.payload.get("test_ids", [])
        session_id = task.payload.get("session_id")
        
        if not test_ids:
            logger.warning("No test IDs provided for execution")
            return {"status": "error", "error": "No test IDs provided"}
        
        logger.info(f"Executing {len(test_ids)} tests")
        
        # Retrieve tests
        tests = []
        for test_id in test_ids:
            test = await self.context_manager.get_generated_test(
                test_id=UUID(test_id) if isinstance(test_id, str) else test_id
            )
            if test:
                tests.append(test)
        
        if not tests:
            logger.warning("No tests found to execute")
            return {"status": "error", "error": "No tests found"}
        
        # Write tests to disk
        await self._write_tests_to_disk(tests)
        
        # Execute Maven tests
        test_classes = [test.test_class_name for test in tests]
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=test_classes,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution results
        execution_results = []
        for test in tests:
            result = await self._create_execution_result(test, junit_results, output)
            if result:
                await self.context_manager.store_execution_result(result)
                execution_results.append(result)
        
        # Update metrics
        self._metrics["tests_run"] += metrics.get("tests_run", 0)
        self._metrics["tests_passed"] += metrics.get("tests_passed", 0)
        self._metrics["tests_failed"] += metrics.get("tests_failed", 0)
        self._metrics["execution_time_ms"] += metrics.get("execution_time_ms", 0)
        
        # Analyze failures
        failed_tests = [r for r in execution_results if not r.passed]
        
        # Trigger regeneration for failed tests (feedback loop)
        if failed_tests and session_id:
            await self._trigger_regeneration(failed_tests, session_id)
        
        # Publish event
        if session_id:
            await self.event_bus.publish(
                event_type="tests_executed",
                data={
                    "session_id": str(session_id),
                    "tests_run": len(execution_results),
                    "tests_passed": len([r for r in execution_results if r.passed]),
                    "tests_failed": len(failed_tests),
                    "execution_time_ms": metrics.get("execution_time_ms", 0),
                },
            )
        
        return {
            "status": "success",
            "tests_run": len(execution_results),
            "tests_passed": len([r for r in execution_results if r.passed]),
            "tests_failed": len(failed_tests),
            "execution_time_ms": metrics.get("execution_time_ms", 0),
            "MAVEN_SUCCESS": success,
        }
    
    xǁRunnerAgentǁ_execute_tests__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_execute_tests__mutmut_1': xǁRunnerAgentǁ_execute_tests__mutmut_1, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_2': xǁRunnerAgentǁ_execute_tests__mutmut_2, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_3': xǁRunnerAgentǁ_execute_tests__mutmut_3, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_4': xǁRunnerAgentǁ_execute_tests__mutmut_4, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_5': xǁRunnerAgentǁ_execute_tests__mutmut_5, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_6': xǁRunnerAgentǁ_execute_tests__mutmut_6, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_7': xǁRunnerAgentǁ_execute_tests__mutmut_7, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_8': xǁRunnerAgentǁ_execute_tests__mutmut_8, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_9': xǁRunnerAgentǁ_execute_tests__mutmut_9, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_10': xǁRunnerAgentǁ_execute_tests__mutmut_10, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_11': xǁRunnerAgentǁ_execute_tests__mutmut_11, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_12': xǁRunnerAgentǁ_execute_tests__mutmut_12, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_13': xǁRunnerAgentǁ_execute_tests__mutmut_13, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_14': xǁRunnerAgentǁ_execute_tests__mutmut_14, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_15': xǁRunnerAgentǁ_execute_tests__mutmut_15, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_16': xǁRunnerAgentǁ_execute_tests__mutmut_16, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_17': xǁRunnerAgentǁ_execute_tests__mutmut_17, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_18': xǁRunnerAgentǁ_execute_tests__mutmut_18, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_19': xǁRunnerAgentǁ_execute_tests__mutmut_19, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_20': xǁRunnerAgentǁ_execute_tests__mutmut_20, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_21': xǁRunnerAgentǁ_execute_tests__mutmut_21, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_22': xǁRunnerAgentǁ_execute_tests__mutmut_22, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_23': xǁRunnerAgentǁ_execute_tests__mutmut_23, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_24': xǁRunnerAgentǁ_execute_tests__mutmut_24, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_25': xǁRunnerAgentǁ_execute_tests__mutmut_25, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_26': xǁRunnerAgentǁ_execute_tests__mutmut_26, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_27': xǁRunnerAgentǁ_execute_tests__mutmut_27, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_28': xǁRunnerAgentǁ_execute_tests__mutmut_28, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_29': xǁRunnerAgentǁ_execute_tests__mutmut_29, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_30': xǁRunnerAgentǁ_execute_tests__mutmut_30, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_31': xǁRunnerAgentǁ_execute_tests__mutmut_31, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_32': xǁRunnerAgentǁ_execute_tests__mutmut_32, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_33': xǁRunnerAgentǁ_execute_tests__mutmut_33, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_34': xǁRunnerAgentǁ_execute_tests__mutmut_34, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_35': xǁRunnerAgentǁ_execute_tests__mutmut_35, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_36': xǁRunnerAgentǁ_execute_tests__mutmut_36, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_37': xǁRunnerAgentǁ_execute_tests__mutmut_37, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_38': xǁRunnerAgentǁ_execute_tests__mutmut_38, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_39': xǁRunnerAgentǁ_execute_tests__mutmut_39, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_40': xǁRunnerAgentǁ_execute_tests__mutmut_40, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_41': xǁRunnerAgentǁ_execute_tests__mutmut_41, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_42': xǁRunnerAgentǁ_execute_tests__mutmut_42, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_43': xǁRunnerAgentǁ_execute_tests__mutmut_43, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_44': xǁRunnerAgentǁ_execute_tests__mutmut_44, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_45': xǁRunnerAgentǁ_execute_tests__mutmut_45, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_46': xǁRunnerAgentǁ_execute_tests__mutmut_46, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_47': xǁRunnerAgentǁ_execute_tests__mutmut_47, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_48': xǁRunnerAgentǁ_execute_tests__mutmut_48, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_49': xǁRunnerAgentǁ_execute_tests__mutmut_49, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_50': xǁRunnerAgentǁ_execute_tests__mutmut_50, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_51': xǁRunnerAgentǁ_execute_tests__mutmut_51, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_52': xǁRunnerAgentǁ_execute_tests__mutmut_52, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_53': xǁRunnerAgentǁ_execute_tests__mutmut_53, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_54': xǁRunnerAgentǁ_execute_tests__mutmut_54, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_55': xǁRunnerAgentǁ_execute_tests__mutmut_55, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_56': xǁRunnerAgentǁ_execute_tests__mutmut_56, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_57': xǁRunnerAgentǁ_execute_tests__mutmut_57, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_58': xǁRunnerAgentǁ_execute_tests__mutmut_58, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_59': xǁRunnerAgentǁ_execute_tests__mutmut_59, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_60': xǁRunnerAgentǁ_execute_tests__mutmut_60, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_61': xǁRunnerAgentǁ_execute_tests__mutmut_61, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_62': xǁRunnerAgentǁ_execute_tests__mutmut_62, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_63': xǁRunnerAgentǁ_execute_tests__mutmut_63, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_64': xǁRunnerAgentǁ_execute_tests__mutmut_64, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_65': xǁRunnerAgentǁ_execute_tests__mutmut_65, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_66': xǁRunnerAgentǁ_execute_tests__mutmut_66, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_67': xǁRunnerAgentǁ_execute_tests__mutmut_67, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_68': xǁRunnerAgentǁ_execute_tests__mutmut_68, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_69': xǁRunnerAgentǁ_execute_tests__mutmut_69, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_70': xǁRunnerAgentǁ_execute_tests__mutmut_70, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_71': xǁRunnerAgentǁ_execute_tests__mutmut_71, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_72': xǁRunnerAgentǁ_execute_tests__mutmut_72, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_73': xǁRunnerAgentǁ_execute_tests__mutmut_73, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_74': xǁRunnerAgentǁ_execute_tests__mutmut_74, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_75': xǁRunnerAgentǁ_execute_tests__mutmut_75, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_76': xǁRunnerAgentǁ_execute_tests__mutmut_76, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_77': xǁRunnerAgentǁ_execute_tests__mutmut_77, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_78': xǁRunnerAgentǁ_execute_tests__mutmut_78, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_79': xǁRunnerAgentǁ_execute_tests__mutmut_79, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_80': xǁRunnerAgentǁ_execute_tests__mutmut_80, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_81': xǁRunnerAgentǁ_execute_tests__mutmut_81, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_82': xǁRunnerAgentǁ_execute_tests__mutmut_82, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_83': xǁRunnerAgentǁ_execute_tests__mutmut_83, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_84': xǁRunnerAgentǁ_execute_tests__mutmut_84, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_85': xǁRunnerAgentǁ_execute_tests__mutmut_85, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_86': xǁRunnerAgentǁ_execute_tests__mutmut_86, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_87': xǁRunnerAgentǁ_execute_tests__mutmut_87, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_88': xǁRunnerAgentǁ_execute_tests__mutmut_88, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_89': xǁRunnerAgentǁ_execute_tests__mutmut_89, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_90': xǁRunnerAgentǁ_execute_tests__mutmut_90, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_91': xǁRunnerAgentǁ_execute_tests__mutmut_91, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_92': xǁRunnerAgentǁ_execute_tests__mutmut_92, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_93': xǁRunnerAgentǁ_execute_tests__mutmut_93, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_94': xǁRunnerAgentǁ_execute_tests__mutmut_94, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_95': xǁRunnerAgentǁ_execute_tests__mutmut_95, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_96': xǁRunnerAgentǁ_execute_tests__mutmut_96, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_97': xǁRunnerAgentǁ_execute_tests__mutmut_97, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_98': xǁRunnerAgentǁ_execute_tests__mutmut_98, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_99': xǁRunnerAgentǁ_execute_tests__mutmut_99, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_100': xǁRunnerAgentǁ_execute_tests__mutmut_100, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_101': xǁRunnerAgentǁ_execute_tests__mutmut_101, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_102': xǁRunnerAgentǁ_execute_tests__mutmut_102, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_103': xǁRunnerAgentǁ_execute_tests__mutmut_103, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_104': xǁRunnerAgentǁ_execute_tests__mutmut_104, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_105': xǁRunnerAgentǁ_execute_tests__mutmut_105, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_106': xǁRunnerAgentǁ_execute_tests__mutmut_106, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_107': xǁRunnerAgentǁ_execute_tests__mutmut_107, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_108': xǁRunnerAgentǁ_execute_tests__mutmut_108, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_109': xǁRunnerAgentǁ_execute_tests__mutmut_109, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_110': xǁRunnerAgentǁ_execute_tests__mutmut_110, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_111': xǁRunnerAgentǁ_execute_tests__mutmut_111, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_112': xǁRunnerAgentǁ_execute_tests__mutmut_112, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_113': xǁRunnerAgentǁ_execute_tests__mutmut_113, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_114': xǁRunnerAgentǁ_execute_tests__mutmut_114, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_115': xǁRunnerAgentǁ_execute_tests__mutmut_115, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_116': xǁRunnerAgentǁ_execute_tests__mutmut_116, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_117': xǁRunnerAgentǁ_execute_tests__mutmut_117, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_118': xǁRunnerAgentǁ_execute_tests__mutmut_118, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_119': xǁRunnerAgentǁ_execute_tests__mutmut_119, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_120': xǁRunnerAgentǁ_execute_tests__mutmut_120, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_121': xǁRunnerAgentǁ_execute_tests__mutmut_121, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_122': xǁRunnerAgentǁ_execute_tests__mutmut_122, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_123': xǁRunnerAgentǁ_execute_tests__mutmut_123, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_124': xǁRunnerAgentǁ_execute_tests__mutmut_124, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_125': xǁRunnerAgentǁ_execute_tests__mutmut_125, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_126': xǁRunnerAgentǁ_execute_tests__mutmut_126, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_127': xǁRunnerAgentǁ_execute_tests__mutmut_127, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_128': xǁRunnerAgentǁ_execute_tests__mutmut_128, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_129': xǁRunnerAgentǁ_execute_tests__mutmut_129, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_130': xǁRunnerAgentǁ_execute_tests__mutmut_130, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_131': xǁRunnerAgentǁ_execute_tests__mutmut_131, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_132': xǁRunnerAgentǁ_execute_tests__mutmut_132, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_133': xǁRunnerAgentǁ_execute_tests__mutmut_133, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_134': xǁRunnerAgentǁ_execute_tests__mutmut_134, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_135': xǁRunnerAgentǁ_execute_tests__mutmut_135, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_136': xǁRunnerAgentǁ_execute_tests__mutmut_136, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_137': xǁRunnerAgentǁ_execute_tests__mutmut_137, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_138': xǁRunnerAgentǁ_execute_tests__mutmut_138, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_139': xǁRunnerAgentǁ_execute_tests__mutmut_139, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_140': xǁRunnerAgentǁ_execute_tests__mutmut_140, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_141': xǁRunnerAgentǁ_execute_tests__mutmut_141, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_142': xǁRunnerAgentǁ_execute_tests__mutmut_142, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_143': xǁRunnerAgentǁ_execute_tests__mutmut_143, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_144': xǁRunnerAgentǁ_execute_tests__mutmut_144, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_145': xǁRunnerAgentǁ_execute_tests__mutmut_145, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_146': xǁRunnerAgentǁ_execute_tests__mutmut_146, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_147': xǁRunnerAgentǁ_execute_tests__mutmut_147, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_148': xǁRunnerAgentǁ_execute_tests__mutmut_148, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_149': xǁRunnerAgentǁ_execute_tests__mutmut_149, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_150': xǁRunnerAgentǁ_execute_tests__mutmut_150, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_151': xǁRunnerAgentǁ_execute_tests__mutmut_151, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_152': xǁRunnerAgentǁ_execute_tests__mutmut_152, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_153': xǁRunnerAgentǁ_execute_tests__mutmut_153, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_154': xǁRunnerAgentǁ_execute_tests__mutmut_154, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_155': xǁRunnerAgentǁ_execute_tests__mutmut_155, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_156': xǁRunnerAgentǁ_execute_tests__mutmut_156, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_157': xǁRunnerAgentǁ_execute_tests__mutmut_157, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_158': xǁRunnerAgentǁ_execute_tests__mutmut_158, 
        'xǁRunnerAgentǁ_execute_tests__mutmut_159': xǁRunnerAgentǁ_execute_tests__mutmut_159
    }
    
    def _execute_tests(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_execute_tests__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_execute_tests__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _execute_tests.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_execute_tests__mutmut_orig)
    xǁRunnerAgentǁ_execute_tests__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_execute_tests'
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_orig(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_1(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = None
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_2(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get(None)
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_3(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("XXtest_idXX")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_4(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("TEST_ID")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_5(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_6(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"XXstatusXX": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_7(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"STATUS": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_8(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "XXerrorXX", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_9(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "ERROR", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_10(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "XXerrorXX": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_11(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "ERROR": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_12(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "XXNo test_id providedXX"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_13(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "no test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_14(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "NO TEST_ID PROVIDED"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_15(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = None
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_16(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=None
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_17(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(None) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_18(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_19(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"XXstatusXX": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_20(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"STATUS": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_21(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "XXerrorXX", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_22(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "ERROR", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_23(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "XXerrorXX": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_24(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "ERROR": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_25(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk(None)
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_26(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = None
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_27(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=None,
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_28(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=None,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_29(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_30(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_31(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = None
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_32(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = None
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_33(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(None, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_34(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, None, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_35(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, None)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_36(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_37(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_38(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, )
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_39(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(None)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_40(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] = 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_41(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] -= 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_42(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["XXtests_runXX"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_43(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["TESTS_RUN"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_44(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 2
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_45(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] = 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_46(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] -= 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_47(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["XXtests_passedXX"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_48(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["TESTS_PASSED"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_49(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 2
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_50(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] = 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_51(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] -= 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_52(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["XXtests_failedXX"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_53(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["TESTS_FAILED"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_54(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 2
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_55(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] = result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_56(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] -= result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_57(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["XXexecution_time_msXX"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_58(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["EXECUTION_TIME_MS"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_59(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "XXstatusXX": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_60(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "STATUS": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_61(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "XXsuccessXX",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_62(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "SUCCESS",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_63(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "XXpassedXX": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_64(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "PASSED": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_65(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "XXexecution_time_msXX": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_66(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "EXECUTION_TIME_MS": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_67(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "XXerror_messageXX": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_68(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "ERROR_MESSAGE": result.error_message,
            }
        else:
            return {"status": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_69(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"XXstatusXX": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_70(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"STATUS": "error", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_71(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "XXerrorXX", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_72(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "ERROR", "error": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_73(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "XXerrorXX": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_74(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "ERROR": "Failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_75(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "XXFailed to create execution resultXX"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_76(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "failed to create execution result"}
    
    async def xǁRunnerAgentǁ_execute_single_test__mutmut_77(self, task: Task) -> Dict[str, Any]:
        """
        Execute a single test.
        
        Args:
            task: Task with test_id in payload
            
        Returns:
            Result with execution details
        """
        test_id = task.payload.get("test_id")
        
        if not test_id:
            return {"status": "error", "error": "No test_id provided"}
        
        # Retrieve test
        test = await self.context_manager.get_generated_test(
            test_id=UUID(test_id) if isinstance(test_id, str) else test_id
        )
        
        if not test:
            return {"status": "error", "error": f"Test not found: {test_id}"}
        
        # Write test to disk
        await self._write_tests_to_disk([test])
        
        # Execute Maven
        success, output, metrics = await self.maven_runner.run_tests(
            test_classes=[test.test_class_name],
            timeout=self.timeout,
        )
        
        # Parse JUnit XML
        junit_results = self.maven_runner.parse_junit_xml()
        
        # Create execution result
        result = await self._create_execution_result(test, junit_results, output)
        
        if result:
            await self.context_manager.store_execution_result(result)
            
            self._metrics["tests_run"] += 1
            if result.passed:
                self._metrics["tests_passed"] += 1
            else:
                self._metrics["tests_failed"] += 1
            self._metrics["execution_time_ms"] += result.execution_time_ms
            
            return {
                "status": "success",
                "passed": result.passed,
                "execution_time_ms": result.execution_time_ms,
                "error_message": result.error_message,
            }
        else:
            return {"status": "error", "error": "FAILED TO CREATE EXECUTION RESULT"}
    
    xǁRunnerAgentǁ_execute_single_test__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_execute_single_test__mutmut_1': xǁRunnerAgentǁ_execute_single_test__mutmut_1, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_2': xǁRunnerAgentǁ_execute_single_test__mutmut_2, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_3': xǁRunnerAgentǁ_execute_single_test__mutmut_3, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_4': xǁRunnerAgentǁ_execute_single_test__mutmut_4, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_5': xǁRunnerAgentǁ_execute_single_test__mutmut_5, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_6': xǁRunnerAgentǁ_execute_single_test__mutmut_6, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_7': xǁRunnerAgentǁ_execute_single_test__mutmut_7, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_8': xǁRunnerAgentǁ_execute_single_test__mutmut_8, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_9': xǁRunnerAgentǁ_execute_single_test__mutmut_9, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_10': xǁRunnerAgentǁ_execute_single_test__mutmut_10, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_11': xǁRunnerAgentǁ_execute_single_test__mutmut_11, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_12': xǁRunnerAgentǁ_execute_single_test__mutmut_12, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_13': xǁRunnerAgentǁ_execute_single_test__mutmut_13, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_14': xǁRunnerAgentǁ_execute_single_test__mutmut_14, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_15': xǁRunnerAgentǁ_execute_single_test__mutmut_15, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_16': xǁRunnerAgentǁ_execute_single_test__mutmut_16, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_17': xǁRunnerAgentǁ_execute_single_test__mutmut_17, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_18': xǁRunnerAgentǁ_execute_single_test__mutmut_18, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_19': xǁRunnerAgentǁ_execute_single_test__mutmut_19, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_20': xǁRunnerAgentǁ_execute_single_test__mutmut_20, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_21': xǁRunnerAgentǁ_execute_single_test__mutmut_21, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_22': xǁRunnerAgentǁ_execute_single_test__mutmut_22, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_23': xǁRunnerAgentǁ_execute_single_test__mutmut_23, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_24': xǁRunnerAgentǁ_execute_single_test__mutmut_24, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_25': xǁRunnerAgentǁ_execute_single_test__mutmut_25, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_26': xǁRunnerAgentǁ_execute_single_test__mutmut_26, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_27': xǁRunnerAgentǁ_execute_single_test__mutmut_27, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_28': xǁRunnerAgentǁ_execute_single_test__mutmut_28, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_29': xǁRunnerAgentǁ_execute_single_test__mutmut_29, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_30': xǁRunnerAgentǁ_execute_single_test__mutmut_30, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_31': xǁRunnerAgentǁ_execute_single_test__mutmut_31, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_32': xǁRunnerAgentǁ_execute_single_test__mutmut_32, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_33': xǁRunnerAgentǁ_execute_single_test__mutmut_33, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_34': xǁRunnerAgentǁ_execute_single_test__mutmut_34, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_35': xǁRunnerAgentǁ_execute_single_test__mutmut_35, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_36': xǁRunnerAgentǁ_execute_single_test__mutmut_36, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_37': xǁRunnerAgentǁ_execute_single_test__mutmut_37, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_38': xǁRunnerAgentǁ_execute_single_test__mutmut_38, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_39': xǁRunnerAgentǁ_execute_single_test__mutmut_39, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_40': xǁRunnerAgentǁ_execute_single_test__mutmut_40, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_41': xǁRunnerAgentǁ_execute_single_test__mutmut_41, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_42': xǁRunnerAgentǁ_execute_single_test__mutmut_42, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_43': xǁRunnerAgentǁ_execute_single_test__mutmut_43, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_44': xǁRunnerAgentǁ_execute_single_test__mutmut_44, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_45': xǁRunnerAgentǁ_execute_single_test__mutmut_45, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_46': xǁRunnerAgentǁ_execute_single_test__mutmut_46, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_47': xǁRunnerAgentǁ_execute_single_test__mutmut_47, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_48': xǁRunnerAgentǁ_execute_single_test__mutmut_48, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_49': xǁRunnerAgentǁ_execute_single_test__mutmut_49, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_50': xǁRunnerAgentǁ_execute_single_test__mutmut_50, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_51': xǁRunnerAgentǁ_execute_single_test__mutmut_51, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_52': xǁRunnerAgentǁ_execute_single_test__mutmut_52, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_53': xǁRunnerAgentǁ_execute_single_test__mutmut_53, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_54': xǁRunnerAgentǁ_execute_single_test__mutmut_54, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_55': xǁRunnerAgentǁ_execute_single_test__mutmut_55, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_56': xǁRunnerAgentǁ_execute_single_test__mutmut_56, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_57': xǁRunnerAgentǁ_execute_single_test__mutmut_57, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_58': xǁRunnerAgentǁ_execute_single_test__mutmut_58, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_59': xǁRunnerAgentǁ_execute_single_test__mutmut_59, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_60': xǁRunnerAgentǁ_execute_single_test__mutmut_60, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_61': xǁRunnerAgentǁ_execute_single_test__mutmut_61, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_62': xǁRunnerAgentǁ_execute_single_test__mutmut_62, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_63': xǁRunnerAgentǁ_execute_single_test__mutmut_63, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_64': xǁRunnerAgentǁ_execute_single_test__mutmut_64, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_65': xǁRunnerAgentǁ_execute_single_test__mutmut_65, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_66': xǁRunnerAgentǁ_execute_single_test__mutmut_66, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_67': xǁRunnerAgentǁ_execute_single_test__mutmut_67, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_68': xǁRunnerAgentǁ_execute_single_test__mutmut_68, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_69': xǁRunnerAgentǁ_execute_single_test__mutmut_69, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_70': xǁRunnerAgentǁ_execute_single_test__mutmut_70, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_71': xǁRunnerAgentǁ_execute_single_test__mutmut_71, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_72': xǁRunnerAgentǁ_execute_single_test__mutmut_72, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_73': xǁRunnerAgentǁ_execute_single_test__mutmut_73, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_74': xǁRunnerAgentǁ_execute_single_test__mutmut_74, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_75': xǁRunnerAgentǁ_execute_single_test__mutmut_75, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_76': xǁRunnerAgentǁ_execute_single_test__mutmut_76, 
        'xǁRunnerAgentǁ_execute_single_test__mutmut_77': xǁRunnerAgentǁ_execute_single_test__mutmut_77
    }
    
    def _execute_single_test(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_execute_single_test__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_execute_single_test__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _execute_single_test.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_execute_single_test__mutmut_orig)
    xǁRunnerAgentǁ_execute_single_test__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_execute_single_test'
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_orig(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_1(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = None
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_2(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get(None, [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_3(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", None)
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_4(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get([])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_5(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", )
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_6(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("XXresult_idsXX", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_7(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("RESULT_IDS", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_8(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_9(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"XXstatusXX": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_10(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"STATUS": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_11(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "XXerrorXX", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_12(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "ERROR", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_13(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "XXerrorXX": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_14(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "ERROR": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_15(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "XXNo result IDs providedXX"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_16(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "no result ids provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_17(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "NO RESULT IDS PROVIDED"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_18(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = None
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_19(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = None
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_20(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=None
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_21(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(None) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_22(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(None)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_23(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = None
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_24(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "XXtotal_failuresXX": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_25(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "TOTAL_FAILURES": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_26(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "XXfailure_typesXX": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_27(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "FAILURE_TYPES": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_28(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "XXcommon_errorsXX": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_29(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "COMMON_ERRORS": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_30(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "XXsuggestionsXX": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_31(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "SUGGESTIONS": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_32(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_33(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = None
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_34(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(None)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_35(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    None
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_36(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["XXfailure_typesXX"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_37(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["FAILURE_TYPES"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_38(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) - 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_39(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(None, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_40(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, None) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_41(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_42(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, ) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_43(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["XXfailure_typesXX"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_44(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["FAILURE_TYPES"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_45(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 1) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_46(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 2
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_47(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(None)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_48(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["XXcommon_errorsXX"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_49(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["COMMON_ERRORS"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_50(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = None
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_51(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["XXsuggestionsXX"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_52(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["SUGGESTIONS"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_53(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(None)
        
        return {
            "status": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_54(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "XXstatusXX": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_55(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "STATUS": "success",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_56(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "XXsuccessXX",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_57(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "SUCCESS",
            "analysis": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_58(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "XXanalysisXX": analysis,
        }
    
    async def xǁRunnerAgentǁ_analyze_failures__mutmut_59(self, task: Task) -> Dict[str, Any]:
        """
        Analyze test failures.
        
        Args:
            task: Task with result_ids in payload
            
        Returns:
            Failure analysis
        """
        result_ids = task.payload.get("result_ids", [])
        
        if not result_ids:
            return {"status": "error", "error": "No result IDs provided"}
        
        # Retrieve execution results
        results = []
        for result_id in result_ids:
            result = await self.context_manager.get_execution_result(
                result_id=UUID(result_id) if isinstance(result_id, str) else result_id
            )
            if result:
                results.append(result)
        
        # Analyze failures
        analysis = {
            "total_failures": len(results),
            "failure_types": {},
            "common_errors": [],
            "suggestions": [],
        }
        
        for result in results:
            if not result.passed:
                # Categorize failure
                failure_type = self._categorize_failure(result)
                analysis["failure_types"][failure_type] = \
                    analysis["failure_types"].get(failure_type, 0) + 1
                
                # Extract common errors
                if result.error_message:
                    analysis["common_errors"].append(result.error_message)
        
        # Generate suggestions
        analysis["suggestions"] = self._generate_suggestions(analysis)
        
        return {
            "status": "success",
            "ANALYSIS": analysis,
        }
    
    xǁRunnerAgentǁ_analyze_failures__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_analyze_failures__mutmut_1': xǁRunnerAgentǁ_analyze_failures__mutmut_1, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_2': xǁRunnerAgentǁ_analyze_failures__mutmut_2, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_3': xǁRunnerAgentǁ_analyze_failures__mutmut_3, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_4': xǁRunnerAgentǁ_analyze_failures__mutmut_4, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_5': xǁRunnerAgentǁ_analyze_failures__mutmut_5, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_6': xǁRunnerAgentǁ_analyze_failures__mutmut_6, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_7': xǁRunnerAgentǁ_analyze_failures__mutmut_7, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_8': xǁRunnerAgentǁ_analyze_failures__mutmut_8, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_9': xǁRunnerAgentǁ_analyze_failures__mutmut_9, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_10': xǁRunnerAgentǁ_analyze_failures__mutmut_10, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_11': xǁRunnerAgentǁ_analyze_failures__mutmut_11, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_12': xǁRunnerAgentǁ_analyze_failures__mutmut_12, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_13': xǁRunnerAgentǁ_analyze_failures__mutmut_13, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_14': xǁRunnerAgentǁ_analyze_failures__mutmut_14, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_15': xǁRunnerAgentǁ_analyze_failures__mutmut_15, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_16': xǁRunnerAgentǁ_analyze_failures__mutmut_16, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_17': xǁRunnerAgentǁ_analyze_failures__mutmut_17, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_18': xǁRunnerAgentǁ_analyze_failures__mutmut_18, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_19': xǁRunnerAgentǁ_analyze_failures__mutmut_19, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_20': xǁRunnerAgentǁ_analyze_failures__mutmut_20, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_21': xǁRunnerAgentǁ_analyze_failures__mutmut_21, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_22': xǁRunnerAgentǁ_analyze_failures__mutmut_22, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_23': xǁRunnerAgentǁ_analyze_failures__mutmut_23, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_24': xǁRunnerAgentǁ_analyze_failures__mutmut_24, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_25': xǁRunnerAgentǁ_analyze_failures__mutmut_25, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_26': xǁRunnerAgentǁ_analyze_failures__mutmut_26, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_27': xǁRunnerAgentǁ_analyze_failures__mutmut_27, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_28': xǁRunnerAgentǁ_analyze_failures__mutmut_28, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_29': xǁRunnerAgentǁ_analyze_failures__mutmut_29, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_30': xǁRunnerAgentǁ_analyze_failures__mutmut_30, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_31': xǁRunnerAgentǁ_analyze_failures__mutmut_31, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_32': xǁRunnerAgentǁ_analyze_failures__mutmut_32, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_33': xǁRunnerAgentǁ_analyze_failures__mutmut_33, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_34': xǁRunnerAgentǁ_analyze_failures__mutmut_34, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_35': xǁRunnerAgentǁ_analyze_failures__mutmut_35, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_36': xǁRunnerAgentǁ_analyze_failures__mutmut_36, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_37': xǁRunnerAgentǁ_analyze_failures__mutmut_37, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_38': xǁRunnerAgentǁ_analyze_failures__mutmut_38, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_39': xǁRunnerAgentǁ_analyze_failures__mutmut_39, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_40': xǁRunnerAgentǁ_analyze_failures__mutmut_40, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_41': xǁRunnerAgentǁ_analyze_failures__mutmut_41, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_42': xǁRunnerAgentǁ_analyze_failures__mutmut_42, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_43': xǁRunnerAgentǁ_analyze_failures__mutmut_43, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_44': xǁRunnerAgentǁ_analyze_failures__mutmut_44, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_45': xǁRunnerAgentǁ_analyze_failures__mutmut_45, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_46': xǁRunnerAgentǁ_analyze_failures__mutmut_46, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_47': xǁRunnerAgentǁ_analyze_failures__mutmut_47, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_48': xǁRunnerAgentǁ_analyze_failures__mutmut_48, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_49': xǁRunnerAgentǁ_analyze_failures__mutmut_49, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_50': xǁRunnerAgentǁ_analyze_failures__mutmut_50, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_51': xǁRunnerAgentǁ_analyze_failures__mutmut_51, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_52': xǁRunnerAgentǁ_analyze_failures__mutmut_52, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_53': xǁRunnerAgentǁ_analyze_failures__mutmut_53, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_54': xǁRunnerAgentǁ_analyze_failures__mutmut_54, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_55': xǁRunnerAgentǁ_analyze_failures__mutmut_55, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_56': xǁRunnerAgentǁ_analyze_failures__mutmut_56, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_57': xǁRunnerAgentǁ_analyze_failures__mutmut_57, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_58': xǁRunnerAgentǁ_analyze_failures__mutmut_58, 
        'xǁRunnerAgentǁ_analyze_failures__mutmut_59': xǁRunnerAgentǁ_analyze_failures__mutmut_59
    }
    
    def _analyze_failures(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_analyze_failures__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_analyze_failures__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _analyze_failures.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_analyze_failures__mutmut_orig)
    xǁRunnerAgentǁ_analyze_failures__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_analyze_failures'
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_orig(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_1(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = None
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_2(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" * "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_3(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" * "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_4(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" * "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_5(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir * "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_6(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "XXsrcXX" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_7(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "SRC" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_8(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "XXtestXX" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_9(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "TEST" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_10(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "XXjavaXX" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_11(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "JAVA" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_12(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "XXgeneratedXX"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_13(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "GENERATED"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_14(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=None, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_15(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=None)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_16(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_17(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, )
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_18(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=False, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_19(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=False)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_20(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = None
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_21(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = None
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_22(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir * filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_23(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(None, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_24(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, None, encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_25(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding=None) as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_26(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open('w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_27(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_28(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', ) as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_29(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'XXwXX', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_30(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'W', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_31(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='XXutf-8XX') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_32(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='UTF-8') as f:
                f.write(test.test_code)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_33(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(None)
            
            logger.info(f"Written test file: {filepath}")
    
    async def xǁRunnerAgentǁ_write_tests_to_disk__mutmut_34(self, tests: List[GeneratedTest]) -> None:
        """
        Write test files to disk.
        
        Args:
            tests: List of generated tests
        """
        # Determine test source directory
        test_dir = self.project_dir / "src" / "test" / "java" / "generated"
        test_dir.mkdir(parents=True, exist_ok=True)
        
        for test in tests:
            # Generate filename from class name
            filename = f"{test.test_class_name}.java"
            filepath = test_dir / filename
            
            # Write test code
            with open(filepath, 'w', encoding='utf-8') as f:
                f.write(test.test_code)
            
            logger.info(None)
    
    xǁRunnerAgentǁ_write_tests_to_disk__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_1': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_1, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_2': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_2, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_3': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_3, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_4': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_4, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_5': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_5, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_6': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_6, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_7': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_7, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_8': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_8, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_9': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_9, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_10': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_10, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_11': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_11, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_12': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_12, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_13': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_13, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_14': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_14, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_15': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_15, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_16': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_16, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_17': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_17, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_18': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_18, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_19': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_19, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_20': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_20, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_21': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_21, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_22': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_22, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_23': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_23, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_24': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_24, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_25': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_25, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_26': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_26, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_27': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_27, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_28': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_28, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_29': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_29, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_30': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_30, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_31': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_31, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_32': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_32, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_33': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_33, 
        'xǁRunnerAgentǁ_write_tests_to_disk__mutmut_34': xǁRunnerAgentǁ_write_tests_to_disk__mutmut_34
    }
    
    def _write_tests_to_disk(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_write_tests_to_disk__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_write_tests_to_disk__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _write_tests_to_disk.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_write_tests_to_disk__mutmut_orig)
    xǁRunnerAgentǁ_write_tests_to_disk__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_write_tests_to_disk'
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_orig(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_1(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["XXtestcasesXX"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_2(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["TESTCASES"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_3(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["XXclassnameXX"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_4(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["CLASSNAME"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_5(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] != f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_6(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = None
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_7(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=None,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_8(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=None,
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_9(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=None,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_10(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=None,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_11(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_12(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_13(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_14(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_15(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["XXpassedXX"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_16(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["PASSED"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_17(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] / 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_18(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["XXtimeXX"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_19(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["TIME"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_20(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1001,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_21(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get(None):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_22(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("XXfailureXX"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_23(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("FAILURE"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_24(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = None
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_25(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["XXfailureXX"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_26(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["FAILURE"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_27(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = None
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_28(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get(None)
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_29(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("XXmessageXX")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_30(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("MESSAGE")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_31(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = None
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_32(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get(None)
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_33(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("XXtextXX")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_34(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("TEXT")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_35(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get(None):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_36(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("XXtextXX"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_37(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("TEXT"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_38(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = None
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_39(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                None
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_40(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["XXtextXX"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_41(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["TEXT"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_42(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get(None):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_43(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("XXerrorXX"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_44(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("ERROR"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_45(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = None
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_46(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["XXerrorXX"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_47(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["ERROR"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_48(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = None
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_49(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get(None)
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_50(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("XXmessageXX")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_51(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("MESSAGE")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_52(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = None
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_53(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get(None)
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_54(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("XXtextXX")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_55(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("TEXT")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_56(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(None)
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_57(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=None,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_58(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=None,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_59(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=None,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_60(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message=None,
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_61(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=None,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_62(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_63(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_64(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_65(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_66(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_67(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=True,
            execution_time_ms=0,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_68(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=1,
            error_message="Test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_69(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="XXTest not executed or result not foundXX",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_70(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="test not executed or result not found",
            maven_output=maven_output,
        )
    
    async def xǁRunnerAgentǁ_create_execution_result__mutmut_71(
        self,
        test: GeneratedTest,
        junit_results: List[Dict[str, Any]],
        maven_output: str,
    ) -> Optional[TestExecutionResult]:
        """
        Create execution result from JUnit XML and Maven output.
        
        Args:
            test: Generated test
            junit_results: Parsed JUnit XML results
            maven_output: Maven output text
            
        Returns:
            TestExecutionResult or None
        """
        # Find matching test case in JUnit results
        for testsuite in junit_results:
            for testcase in testsuite["testcases"]:
                if testcase["classname"] == f"generated.{test.test_class_name}":
                    # Create result
                    result = TestExecutionResult(
                        test_id=test.id,
                        passed=testcase["passed"],
                        execution_time_ms=testcase["time"] * 1000,
                        maven_output=maven_output,
                    )
                    
                    # Add failure details
                    if testcase.get("failure"):
                        failure = testcase["failure"]
                        result.error_message = failure.get("message")
                        result.stack_trace = failure.get("text")
                        
                        # Parse assertion failures
                        if failure.get("text"):
                            result.assertion_failures = self._parse_assertion_failures(
                                failure["text"]
                            )
                    
                    # Add error details
                    if testcase.get("error"):
                        error = testcase["error"]
                        result.error_message = error.get("message")
                        result.stack_trace = error.get("text")
                    
                    return result
        
        # No matching test case found, create basic result
        logger.warning(f"No JUnit result found for test: {test.test_class_name}")
        return TestExecutionResult(
            test_id=test.id,
            passed=False,
            execution_time_ms=0,
            error_message="TEST NOT EXECUTED OR RESULT NOT FOUND",
            maven_output=maven_output,
        )
    
    xǁRunnerAgentǁ_create_execution_result__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_create_execution_result__mutmut_1': xǁRunnerAgentǁ_create_execution_result__mutmut_1, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_2': xǁRunnerAgentǁ_create_execution_result__mutmut_2, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_3': xǁRunnerAgentǁ_create_execution_result__mutmut_3, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_4': xǁRunnerAgentǁ_create_execution_result__mutmut_4, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_5': xǁRunnerAgentǁ_create_execution_result__mutmut_5, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_6': xǁRunnerAgentǁ_create_execution_result__mutmut_6, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_7': xǁRunnerAgentǁ_create_execution_result__mutmut_7, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_8': xǁRunnerAgentǁ_create_execution_result__mutmut_8, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_9': xǁRunnerAgentǁ_create_execution_result__mutmut_9, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_10': xǁRunnerAgentǁ_create_execution_result__mutmut_10, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_11': xǁRunnerAgentǁ_create_execution_result__mutmut_11, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_12': xǁRunnerAgentǁ_create_execution_result__mutmut_12, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_13': xǁRunnerAgentǁ_create_execution_result__mutmut_13, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_14': xǁRunnerAgentǁ_create_execution_result__mutmut_14, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_15': xǁRunnerAgentǁ_create_execution_result__mutmut_15, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_16': xǁRunnerAgentǁ_create_execution_result__mutmut_16, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_17': xǁRunnerAgentǁ_create_execution_result__mutmut_17, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_18': xǁRunnerAgentǁ_create_execution_result__mutmut_18, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_19': xǁRunnerAgentǁ_create_execution_result__mutmut_19, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_20': xǁRunnerAgentǁ_create_execution_result__mutmut_20, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_21': xǁRunnerAgentǁ_create_execution_result__mutmut_21, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_22': xǁRunnerAgentǁ_create_execution_result__mutmut_22, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_23': xǁRunnerAgentǁ_create_execution_result__mutmut_23, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_24': xǁRunnerAgentǁ_create_execution_result__mutmut_24, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_25': xǁRunnerAgentǁ_create_execution_result__mutmut_25, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_26': xǁRunnerAgentǁ_create_execution_result__mutmut_26, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_27': xǁRunnerAgentǁ_create_execution_result__mutmut_27, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_28': xǁRunnerAgentǁ_create_execution_result__mutmut_28, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_29': xǁRunnerAgentǁ_create_execution_result__mutmut_29, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_30': xǁRunnerAgentǁ_create_execution_result__mutmut_30, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_31': xǁRunnerAgentǁ_create_execution_result__mutmut_31, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_32': xǁRunnerAgentǁ_create_execution_result__mutmut_32, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_33': xǁRunnerAgentǁ_create_execution_result__mutmut_33, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_34': xǁRunnerAgentǁ_create_execution_result__mutmut_34, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_35': xǁRunnerAgentǁ_create_execution_result__mutmut_35, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_36': xǁRunnerAgentǁ_create_execution_result__mutmut_36, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_37': xǁRunnerAgentǁ_create_execution_result__mutmut_37, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_38': xǁRunnerAgentǁ_create_execution_result__mutmut_38, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_39': xǁRunnerAgentǁ_create_execution_result__mutmut_39, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_40': xǁRunnerAgentǁ_create_execution_result__mutmut_40, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_41': xǁRunnerAgentǁ_create_execution_result__mutmut_41, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_42': xǁRunnerAgentǁ_create_execution_result__mutmut_42, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_43': xǁRunnerAgentǁ_create_execution_result__mutmut_43, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_44': xǁRunnerAgentǁ_create_execution_result__mutmut_44, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_45': xǁRunnerAgentǁ_create_execution_result__mutmut_45, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_46': xǁRunnerAgentǁ_create_execution_result__mutmut_46, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_47': xǁRunnerAgentǁ_create_execution_result__mutmut_47, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_48': xǁRunnerAgentǁ_create_execution_result__mutmut_48, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_49': xǁRunnerAgentǁ_create_execution_result__mutmut_49, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_50': xǁRunnerAgentǁ_create_execution_result__mutmut_50, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_51': xǁRunnerAgentǁ_create_execution_result__mutmut_51, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_52': xǁRunnerAgentǁ_create_execution_result__mutmut_52, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_53': xǁRunnerAgentǁ_create_execution_result__mutmut_53, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_54': xǁRunnerAgentǁ_create_execution_result__mutmut_54, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_55': xǁRunnerAgentǁ_create_execution_result__mutmut_55, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_56': xǁRunnerAgentǁ_create_execution_result__mutmut_56, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_57': xǁRunnerAgentǁ_create_execution_result__mutmut_57, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_58': xǁRunnerAgentǁ_create_execution_result__mutmut_58, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_59': xǁRunnerAgentǁ_create_execution_result__mutmut_59, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_60': xǁRunnerAgentǁ_create_execution_result__mutmut_60, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_61': xǁRunnerAgentǁ_create_execution_result__mutmut_61, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_62': xǁRunnerAgentǁ_create_execution_result__mutmut_62, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_63': xǁRunnerAgentǁ_create_execution_result__mutmut_63, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_64': xǁRunnerAgentǁ_create_execution_result__mutmut_64, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_65': xǁRunnerAgentǁ_create_execution_result__mutmut_65, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_66': xǁRunnerAgentǁ_create_execution_result__mutmut_66, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_67': xǁRunnerAgentǁ_create_execution_result__mutmut_67, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_68': xǁRunnerAgentǁ_create_execution_result__mutmut_68, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_69': xǁRunnerAgentǁ_create_execution_result__mutmut_69, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_70': xǁRunnerAgentǁ_create_execution_result__mutmut_70, 
        'xǁRunnerAgentǁ_create_execution_result__mutmut_71': xǁRunnerAgentǁ_create_execution_result__mutmut_71
    }
    
    def _create_execution_result(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_create_execution_result__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_create_execution_result__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _create_execution_result.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_create_execution_result__mutmut_orig)
    xǁRunnerAgentǁ_create_execution_result__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_create_execution_result'
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_orig(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_1(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = None
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_2(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = None
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_3(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"XXExpected: .+ but was: .+XX"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_4(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_5(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"EXPECTED: .+ BUT WAS: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_6(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = None
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_7(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(None, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_8(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, None)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_9(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_10(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, )
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_11(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(None)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_12(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "XXExpecting actual not to be nullXX" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_13(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "expecting actual not to be null" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_14(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "EXPECTING ACTUAL NOT TO BE NULL" in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_15(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" not in stack_trace:
            failures.append("Null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_16(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append(None)
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_17(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("XXNull value encounteredXX")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_18(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("null value encountered")
        
        return failures
    
    def xǁRunnerAgentǁ_parse_assertion_failures__mutmut_19(self, stack_trace: str) -> List[str]:
        """
        Parse assertion failures from stack trace.
        
        Args:
            stack_trace: Stack trace text
            
        Returns:
            List of assertion failure messages
        """
        failures = []
        
        # Pattern: Expected: <value> but was: <value>
        pattern = r"Expected: .+ but was: .+"
        matches = re.findall(pattern, stack_trace)
        failures.extend(matches)
        
        # Pattern: Expecting actual not to be null
        if "Expecting actual not to be null" in stack_trace:
            failures.append("NULL VALUE ENCOUNTERED")
        
        return failures
    
    xǁRunnerAgentǁ_parse_assertion_failures__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_1': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_1, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_2': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_2, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_3': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_3, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_4': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_4, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_5': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_5, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_6': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_6, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_7': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_7, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_8': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_8, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_9': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_9, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_10': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_10, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_11': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_11, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_12': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_12, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_13': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_13, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_14': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_14, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_15': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_15, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_16': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_16, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_17': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_17, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_18': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_18, 
        'xǁRunnerAgentǁ_parse_assertion_failures__mutmut_19': xǁRunnerAgentǁ_parse_assertion_failures__mutmut_19
    }
    
    def _parse_assertion_failures(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_parse_assertion_failures__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_parse_assertion_failures__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_assertion_failures.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_parse_assertion_failures__mutmut_orig)
    xǁRunnerAgentǁ_parse_assertion_failures__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_parse_assertion_failures'
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_orig(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_1(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_2(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "XXunknownXX"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_3(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "UNKNOWN"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_4(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = None
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_5(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.upper()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_6(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error and "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_7(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "XXassertionXX" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_8(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "ASSERTION" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_9(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" not in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_10(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "XXexpectedXX" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_11(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "EXPECTED" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_12(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" not in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_13(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "XXassertion_failureXX"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_14(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "ASSERTION_FAILURE"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_15(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "XXtimeoutXX" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_16(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "TIMEOUT" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_17(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" not in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_18(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "XXtimeoutXX"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_19(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "TIMEOUT"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_20(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error and "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_21(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "XXconnectionXX" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_22(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "CONNECTION" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_23(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" not in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_24(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "XXnetworkXX" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_25(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "NETWORK" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_26(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" not in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_27(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "XXnetwork_errorXX"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_28(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "NETWORK_ERROR"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_29(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "XXnullXX" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_30(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "NULL" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_31(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" not in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_32(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "XXnull_pointerXX"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_33(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "NULL_POINTER"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_34(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error and "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_35(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "XXcompileXX" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_36(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "COMPILE" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_37(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" not in error or "syntax" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_38(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "XXsyntaxXX" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_39(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "SYNTAX" in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_40(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" not in error:
            return "compilation_error"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_41(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "XXcompilation_errorXX"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_42(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "COMPILATION_ERROR"
        else:
            return "runtime_error"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_43(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "XXruntime_errorXX"
    
    def xǁRunnerAgentǁ_categorize_failure__mutmut_44(self, result: TestExecutionResult) -> str:
        """
        Categorize test failure.
        
        Args:
            result: Test execution result
            
        Returns:
            Failure category
        """
        if not result.error_message:
            return "unknown"
        
        error = result.error_message.lower()
        
        if "assertion" in error or "expected" in error:
            return "assertion_failure"
        elif "timeout" in error:
            return "timeout"
        elif "connection" in error or "network" in error:
            return "network_error"
        elif "null" in error:
            return "null_pointer"
        elif "compile" in error or "syntax" in error:
            return "compilation_error"
        else:
            return "RUNTIME_ERROR"
    
    xǁRunnerAgentǁ_categorize_failure__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_categorize_failure__mutmut_1': xǁRunnerAgentǁ_categorize_failure__mutmut_1, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_2': xǁRunnerAgentǁ_categorize_failure__mutmut_2, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_3': xǁRunnerAgentǁ_categorize_failure__mutmut_3, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_4': xǁRunnerAgentǁ_categorize_failure__mutmut_4, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_5': xǁRunnerAgentǁ_categorize_failure__mutmut_5, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_6': xǁRunnerAgentǁ_categorize_failure__mutmut_6, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_7': xǁRunnerAgentǁ_categorize_failure__mutmut_7, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_8': xǁRunnerAgentǁ_categorize_failure__mutmut_8, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_9': xǁRunnerAgentǁ_categorize_failure__mutmut_9, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_10': xǁRunnerAgentǁ_categorize_failure__mutmut_10, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_11': xǁRunnerAgentǁ_categorize_failure__mutmut_11, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_12': xǁRunnerAgentǁ_categorize_failure__mutmut_12, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_13': xǁRunnerAgentǁ_categorize_failure__mutmut_13, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_14': xǁRunnerAgentǁ_categorize_failure__mutmut_14, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_15': xǁRunnerAgentǁ_categorize_failure__mutmut_15, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_16': xǁRunnerAgentǁ_categorize_failure__mutmut_16, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_17': xǁRunnerAgentǁ_categorize_failure__mutmut_17, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_18': xǁRunnerAgentǁ_categorize_failure__mutmut_18, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_19': xǁRunnerAgentǁ_categorize_failure__mutmut_19, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_20': xǁRunnerAgentǁ_categorize_failure__mutmut_20, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_21': xǁRunnerAgentǁ_categorize_failure__mutmut_21, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_22': xǁRunnerAgentǁ_categorize_failure__mutmut_22, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_23': xǁRunnerAgentǁ_categorize_failure__mutmut_23, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_24': xǁRunnerAgentǁ_categorize_failure__mutmut_24, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_25': xǁRunnerAgentǁ_categorize_failure__mutmut_25, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_26': xǁRunnerAgentǁ_categorize_failure__mutmut_26, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_27': xǁRunnerAgentǁ_categorize_failure__mutmut_27, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_28': xǁRunnerAgentǁ_categorize_failure__mutmut_28, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_29': xǁRunnerAgentǁ_categorize_failure__mutmut_29, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_30': xǁRunnerAgentǁ_categorize_failure__mutmut_30, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_31': xǁRunnerAgentǁ_categorize_failure__mutmut_31, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_32': xǁRunnerAgentǁ_categorize_failure__mutmut_32, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_33': xǁRunnerAgentǁ_categorize_failure__mutmut_33, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_34': xǁRunnerAgentǁ_categorize_failure__mutmut_34, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_35': xǁRunnerAgentǁ_categorize_failure__mutmut_35, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_36': xǁRunnerAgentǁ_categorize_failure__mutmut_36, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_37': xǁRunnerAgentǁ_categorize_failure__mutmut_37, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_38': xǁRunnerAgentǁ_categorize_failure__mutmut_38, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_39': xǁRunnerAgentǁ_categorize_failure__mutmut_39, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_40': xǁRunnerAgentǁ_categorize_failure__mutmut_40, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_41': xǁRunnerAgentǁ_categorize_failure__mutmut_41, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_42': xǁRunnerAgentǁ_categorize_failure__mutmut_42, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_43': xǁRunnerAgentǁ_categorize_failure__mutmut_43, 
        'xǁRunnerAgentǁ_categorize_failure__mutmut_44': xǁRunnerAgentǁ_categorize_failure__mutmut_44
    }
    
    def _categorize_failure(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_categorize_failure__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_categorize_failure__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _categorize_failure.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_categorize_failure__mutmut_orig)
    xǁRunnerAgentǁ_categorize_failure__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_categorize_failure'
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_orig(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_1(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = None
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_2(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = None
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_3(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get(None, {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_4(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", None)
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_5(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get({})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_6(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", )
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_7(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("XXfailure_typesXX", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_8(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("FAILURE_TYPES", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_9(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "XXassertion_failureXX" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_10(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "ASSERTION_FAILURE" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_11(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" not in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_12(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                None
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_13(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "XXReview oracle assertions - they may not match actual API behaviorXX"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_14(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "review oracle assertions - they may not match actual api behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_15(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "REVIEW ORACLE ASSERTIONS - THEY MAY NOT MATCH ACTUAL API BEHAVIOR"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_16(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "XXnetwork_errorXX" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_17(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "NETWORK_ERROR" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_18(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" not in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_19(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                None
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_20(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "XXCheck API availability and network connectivityXX"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_21(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "check api availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_22(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "CHECK API AVAILABILITY AND NETWORK CONNECTIVITY"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_23(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "XXnull_pointerXX" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_24(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "NULL_POINTER" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_25(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" not in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_26(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                None
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_27(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "XXReview response schema validation - fields may be missing or nullXX"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_28(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_29(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "REVIEW RESPONSE SCHEMA VALIDATION - FIELDS MAY BE MISSING OR NULL"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_30(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "XXtimeoutXX" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_31(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "TIMEOUT" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_32(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" not in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_33(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                None
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_34(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "XXIncrease test timeout or optimize API performanceXX"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_35(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "increase test timeout or optimize api performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_36(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "INCREASE TEST TIMEOUT OR OPTIMIZE API PERFORMANCE"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_37(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "XXcompilation_errorXX" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_38(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "COMPILATION_ERROR" in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_39(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" not in failure_types:
            suggestions.append(
                "Review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_40(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                None
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_41(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "XXReview generated code syntax and importsXX"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_42(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "review generated code syntax and imports"
            )
        
        return suggestions
    
    def xǁRunnerAgentǁ_generate_suggestions__mutmut_43(self, analysis: Dict[str, Any]) -> List[str]:
        """
        Generate suggestions based on failure analysis.
        
        Args:
            analysis: Failure analysis
            
        Returns:
            List of suggestions
        """
        suggestions = []
        
        failure_types = analysis.get("failure_types", {})
        
        if "assertion_failure" in failure_types:
            suggestions.append(
                "Review oracle assertions - they may not match actual API behavior"
            )
        
        if "network_error" in failure_types:
            suggestions.append(
                "Check API availability and network connectivity"
            )
        
        if "null_pointer" in failure_types:
            suggestions.append(
                "Review response schema validation - fields may be missing or null"
            )
        
        if "timeout" in failure_types:
            suggestions.append(
                "Increase test timeout or optimize API performance"
            )
        
        if "compilation_error" in failure_types:
            suggestions.append(
                "REVIEW GENERATED CODE SYNTAX AND IMPORTS"
            )
        
        return suggestions
    
    xǁRunnerAgentǁ_generate_suggestions__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_generate_suggestions__mutmut_1': xǁRunnerAgentǁ_generate_suggestions__mutmut_1, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_2': xǁRunnerAgentǁ_generate_suggestions__mutmut_2, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_3': xǁRunnerAgentǁ_generate_suggestions__mutmut_3, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_4': xǁRunnerAgentǁ_generate_suggestions__mutmut_4, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_5': xǁRunnerAgentǁ_generate_suggestions__mutmut_5, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_6': xǁRunnerAgentǁ_generate_suggestions__mutmut_6, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_7': xǁRunnerAgentǁ_generate_suggestions__mutmut_7, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_8': xǁRunnerAgentǁ_generate_suggestions__mutmut_8, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_9': xǁRunnerAgentǁ_generate_suggestions__mutmut_9, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_10': xǁRunnerAgentǁ_generate_suggestions__mutmut_10, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_11': xǁRunnerAgentǁ_generate_suggestions__mutmut_11, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_12': xǁRunnerAgentǁ_generate_suggestions__mutmut_12, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_13': xǁRunnerAgentǁ_generate_suggestions__mutmut_13, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_14': xǁRunnerAgentǁ_generate_suggestions__mutmut_14, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_15': xǁRunnerAgentǁ_generate_suggestions__mutmut_15, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_16': xǁRunnerAgentǁ_generate_suggestions__mutmut_16, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_17': xǁRunnerAgentǁ_generate_suggestions__mutmut_17, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_18': xǁRunnerAgentǁ_generate_suggestions__mutmut_18, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_19': xǁRunnerAgentǁ_generate_suggestions__mutmut_19, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_20': xǁRunnerAgentǁ_generate_suggestions__mutmut_20, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_21': xǁRunnerAgentǁ_generate_suggestions__mutmut_21, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_22': xǁRunnerAgentǁ_generate_suggestions__mutmut_22, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_23': xǁRunnerAgentǁ_generate_suggestions__mutmut_23, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_24': xǁRunnerAgentǁ_generate_suggestions__mutmut_24, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_25': xǁRunnerAgentǁ_generate_suggestions__mutmut_25, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_26': xǁRunnerAgentǁ_generate_suggestions__mutmut_26, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_27': xǁRunnerAgentǁ_generate_suggestions__mutmut_27, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_28': xǁRunnerAgentǁ_generate_suggestions__mutmut_28, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_29': xǁRunnerAgentǁ_generate_suggestions__mutmut_29, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_30': xǁRunnerAgentǁ_generate_suggestions__mutmut_30, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_31': xǁRunnerAgentǁ_generate_suggestions__mutmut_31, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_32': xǁRunnerAgentǁ_generate_suggestions__mutmut_32, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_33': xǁRunnerAgentǁ_generate_suggestions__mutmut_33, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_34': xǁRunnerAgentǁ_generate_suggestions__mutmut_34, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_35': xǁRunnerAgentǁ_generate_suggestions__mutmut_35, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_36': xǁRunnerAgentǁ_generate_suggestions__mutmut_36, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_37': xǁRunnerAgentǁ_generate_suggestions__mutmut_37, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_38': xǁRunnerAgentǁ_generate_suggestions__mutmut_38, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_39': xǁRunnerAgentǁ_generate_suggestions__mutmut_39, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_40': xǁRunnerAgentǁ_generate_suggestions__mutmut_40, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_41': xǁRunnerAgentǁ_generate_suggestions__mutmut_41, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_42': xǁRunnerAgentǁ_generate_suggestions__mutmut_42, 
        'xǁRunnerAgentǁ_generate_suggestions__mutmut_43': xǁRunnerAgentǁ_generate_suggestions__mutmut_43
    }
    
    def _generate_suggestions(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_generate_suggestions__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_generate_suggestions__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _generate_suggestions.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_generate_suggestions__mutmut_orig)
    xǁRunnerAgentǁ_generate_suggestions__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_generate_suggestions'
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_orig(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_1(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(None)
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_2(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count > self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_3(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    None
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_4(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                break
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_5(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = None
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_6(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=None,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_7(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=None,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_8(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type=None,
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_9(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload=None,
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_10(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=None,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_11(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_12(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_13(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_14(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_15(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_16(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="XXregenerate_testXX",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_17(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="REGENERATE_TEST",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_18(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "XXtest_idXX": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_19(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "TEST_ID": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_20(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(None),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_21(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "XXfailure_reasonXX": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_22(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "FAILURE_REASON": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_23(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "XXassertion_failuresXX": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_24(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "ASSERTION_FAILURES": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_25(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "XXretry_countXX": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_26(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "RETRY_COUNT": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_27(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count - 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_28(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 2,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_29(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(None)
            
            self._metrics["retries"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_30(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] = 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_31(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] -= 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_32(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["XXretriesXX"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_33(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["RETRIES"] += 1
    
    async def xǁRunnerAgentǁ_trigger_regeneration__mutmut_34(
        self,
        failed_results: List[TestExecutionResult],
        session_id: UUID,
    ) -> None:
        """
        Trigger test regeneration for failed tests (feedback loop).
        
        Args:
            failed_results: List of failed test results
            session_id: Workflow session ID
        """
        logger.info(f"Triggering regeneration for {len(failed_results)} failed tests")
        
        # Build regeneration requests
        for result in failed_results:
            # Check retry count
            if result.retry_count >= self.max_retries:
                logger.warning(
                    f"Max retries reached for test {result.test_id}, skipping regeneration"
                )
                continue
            
            # Send message to Contractor for regeneration
            message = MessageBuilder.create_request(
                from_agent=AgentType.RUNNER,
                to_agent=AgentType.CONTRACTOR,
                message_type="regenerate_test",
                payload={
                    "test_id": str(result.test_id),
                    "failure_reason": result.error_message,
                    "assertion_failures": result.assertion_failures,
                    "retry_count": result.retry_count + 1,
                },
                session_id=session_id,
            )
            
            await self.message_router.send(message)
            
            self._metrics["retries"] += 2
    
    xǁRunnerAgentǁ_trigger_regeneration__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_trigger_regeneration__mutmut_1': xǁRunnerAgentǁ_trigger_regeneration__mutmut_1, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_2': xǁRunnerAgentǁ_trigger_regeneration__mutmut_2, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_3': xǁRunnerAgentǁ_trigger_regeneration__mutmut_3, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_4': xǁRunnerAgentǁ_trigger_regeneration__mutmut_4, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_5': xǁRunnerAgentǁ_trigger_regeneration__mutmut_5, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_6': xǁRunnerAgentǁ_trigger_regeneration__mutmut_6, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_7': xǁRunnerAgentǁ_trigger_regeneration__mutmut_7, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_8': xǁRunnerAgentǁ_trigger_regeneration__mutmut_8, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_9': xǁRunnerAgentǁ_trigger_regeneration__mutmut_9, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_10': xǁRunnerAgentǁ_trigger_regeneration__mutmut_10, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_11': xǁRunnerAgentǁ_trigger_regeneration__mutmut_11, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_12': xǁRunnerAgentǁ_trigger_regeneration__mutmut_12, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_13': xǁRunnerAgentǁ_trigger_regeneration__mutmut_13, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_14': xǁRunnerAgentǁ_trigger_regeneration__mutmut_14, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_15': xǁRunnerAgentǁ_trigger_regeneration__mutmut_15, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_16': xǁRunnerAgentǁ_trigger_regeneration__mutmut_16, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_17': xǁRunnerAgentǁ_trigger_regeneration__mutmut_17, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_18': xǁRunnerAgentǁ_trigger_regeneration__mutmut_18, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_19': xǁRunnerAgentǁ_trigger_regeneration__mutmut_19, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_20': xǁRunnerAgentǁ_trigger_regeneration__mutmut_20, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_21': xǁRunnerAgentǁ_trigger_regeneration__mutmut_21, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_22': xǁRunnerAgentǁ_trigger_regeneration__mutmut_22, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_23': xǁRunnerAgentǁ_trigger_regeneration__mutmut_23, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_24': xǁRunnerAgentǁ_trigger_regeneration__mutmut_24, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_25': xǁRunnerAgentǁ_trigger_regeneration__mutmut_25, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_26': xǁRunnerAgentǁ_trigger_regeneration__mutmut_26, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_27': xǁRunnerAgentǁ_trigger_regeneration__mutmut_27, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_28': xǁRunnerAgentǁ_trigger_regeneration__mutmut_28, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_29': xǁRunnerAgentǁ_trigger_regeneration__mutmut_29, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_30': xǁRunnerAgentǁ_trigger_regeneration__mutmut_30, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_31': xǁRunnerAgentǁ_trigger_regeneration__mutmut_31, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_32': xǁRunnerAgentǁ_trigger_regeneration__mutmut_32, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_33': xǁRunnerAgentǁ_trigger_regeneration__mutmut_33, 
        'xǁRunnerAgentǁ_trigger_regeneration__mutmut_34': xǁRunnerAgentǁ_trigger_regeneration__mutmut_34
    }
    
    def _trigger_regeneration(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_trigger_regeneration__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_trigger_regeneration__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _trigger_regeneration.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_trigger_regeneration__mutmut_orig)
    xǁRunnerAgentǁ_trigger_regeneration__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_trigger_regeneration'
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_orig(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_1(self, message) -> None:
        """Handle execute_tests message."""
        task = None
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_2(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=None,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_3(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type=None,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_4(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=None,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_5(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=None,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_6(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_7(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_8(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_9(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_10(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="XXexecute_testsXX",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_11(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="EXECUTE_TESTS",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_12(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(None)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_13(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = None
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_14(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=None,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_15(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data=None,
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_16(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_17(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_18(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"XXstatusXX": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_19(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"STATUS": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_20(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "XXacceptedXX", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_21(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "ACCEPTED", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_22(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "XXtask_idXX": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_23(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "TASK_ID": str(task.id)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_24(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(None)},
        )
        await self.message_router.send(response)
    
    # Message handlers
    
    async def xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_25(self, message) -> None:
        """Handle execute_tests message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_tests",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(None)
    
    xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_1': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_1, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_2': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_2, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_3': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_3, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_4': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_4, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_5': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_5, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_6': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_6, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_7': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_7, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_8': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_8, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_9': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_9, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_10': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_10, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_11': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_11, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_12': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_12, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_13': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_13, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_14': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_14, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_15': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_15, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_16': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_16, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_17': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_17, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_18': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_18, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_19': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_19, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_20': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_20, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_21': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_21, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_22': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_22, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_23': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_23, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_24': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_24, 
        'xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_25': xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_25
    }
    
    def _handle_execute_tests_message(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _handle_execute_tests_message.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_orig)
    xǁRunnerAgentǁ_handle_execute_tests_message__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_handle_execute_tests_message'
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_orig(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_1(self, message) -> None:
        """Handle execute_single_test message."""
        task = None
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_2(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=None,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_3(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type=None,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_4(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=None,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_5(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=None,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_6(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_7(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_8(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_9(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_10(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="XXexecute_single_testXX",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_11(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="EXECUTE_SINGLE_TEST",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_12(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(None)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_13(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = None
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_14(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=None,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_15(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data=None,
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_16(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_17(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_18(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"XXstatusXX": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_19(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"STATUS": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_20(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "XXacceptedXX", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_21(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "ACCEPTED", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_22(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "XXtask_idXX": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_23(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "TASK_ID": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_24(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(None)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_25(self, message) -> None:
        """Handle execute_single_test message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="execute_single_test",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(None)
    
    xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_1': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_1, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_2': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_2, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_3': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_3, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_4': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_4, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_5': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_5, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_6': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_6, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_7': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_7, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_8': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_8, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_9': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_9, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_10': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_10, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_11': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_11, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_12': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_12, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_13': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_13, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_14': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_14, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_15': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_15, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_16': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_16, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_17': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_17, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_18': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_18, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_19': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_19, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_20': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_20, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_21': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_21, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_22': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_22, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_23': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_23, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_24': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_24, 
        'xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_25': xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_25
    }
    
    def _handle_execute_single_test_message(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _handle_execute_single_test_message.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_orig)
    xǁRunnerAgentǁ_handle_execute_single_test_message__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_handle_execute_single_test_message'
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_orig(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_1(self, message) -> None:
        """Handle analyze_failures message."""
        task = None
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_2(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=None,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_3(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type=None,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_4(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=None,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_5(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=None,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_6(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_7(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_8(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_9(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_10(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="XXanalyze_failuresXX",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_11(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="ANALYZE_FAILURES",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_12(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(None)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_13(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = None
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_14(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=None,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_15(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data=None,
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_16(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_17(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_18(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"XXstatusXX": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_19(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"STATUS": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_20(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "XXacceptedXX", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_21(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "ACCEPTED", "task_id": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_22(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "XXtask_idXX": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_23(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "TASK_ID": str(task.id)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_24(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(None)},
        )
        await self.message_router.send(response)
    
    async def xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_25(self, message) -> None:
        """Handle analyze_failures message."""
        task = Task(
            agent_type=AgentType.RUNNER,
            task_type="analyze_failures",
            session_id=message.session_id,
            payload=message.payload,
        )
        
        await self.task_queue.submit(task)
        
        response = MessageBuilder.create_response(
            original_message=message,
            response_data={"status": "accepted", "task_id": str(task.id)},
        )
        await self.message_router.send(None)
    
    xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_1': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_1, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_2': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_2, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_3': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_3, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_4': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_4, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_5': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_5, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_6': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_6, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_7': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_7, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_8': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_8, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_9': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_9, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_10': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_10, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_11': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_11, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_12': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_12, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_13': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_13, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_14': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_14, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_15': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_15, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_16': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_16, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_17': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_17, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_18': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_18, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_19': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_19, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_20': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_20, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_21': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_21, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_22': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_22, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_23': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_23, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_24': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_24, 
        'xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_25': xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_25
    }
    
    def _handle_analyze_failures_message(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _handle_analyze_failures_message.__signature__ = _mutmut_signature(xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_orig)
    xǁRunnerAgentǁ_handle_analyze_failures_message__mutmut_orig.__name__ = 'xǁRunnerAgentǁ_handle_analyze_failures_message'
    
    def xǁRunnerAgentǁ__repr____mutmut_orig(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['tests_run']}, "
            f"tests_passed={self._metrics['tests_passed']}, "
            f"tests_failed={self._metrics['tests_failed']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_1(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['XXtests_runXX']}, "
            f"tests_passed={self._metrics['tests_passed']}, "
            f"tests_failed={self._metrics['tests_failed']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_2(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['TESTS_RUN']}, "
            f"tests_passed={self._metrics['tests_passed']}, "
            f"tests_failed={self._metrics['tests_failed']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_3(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['tests_run']}, "
            f"tests_passed={self._metrics['XXtests_passedXX']}, "
            f"tests_failed={self._metrics['tests_failed']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_4(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['tests_run']}, "
            f"tests_passed={self._metrics['TESTS_PASSED']}, "
            f"tests_failed={self._metrics['tests_failed']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_5(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['tests_run']}, "
            f"tests_passed={self._metrics['tests_passed']}, "
            f"tests_failed={self._metrics['XXtests_failedXX']})"
        )
    
    def xǁRunnerAgentǁ__repr____mutmut_6(self) -> str:
        """String representation."""
        return (
            f"RunnerAgent(state={self.state.value}, "
            f"active_tasks={len(self.active_tasks)}, "
            f"tests_run={self._metrics['tests_run']}, "
            f"tests_passed={self._metrics['tests_passed']}, "
            f"tests_failed={self._metrics['TESTS_FAILED']})"
        )
    
    xǁRunnerAgentǁ__repr____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁRunnerAgentǁ__repr____mutmut_1': xǁRunnerAgentǁ__repr____mutmut_1, 
        'xǁRunnerAgentǁ__repr____mutmut_2': xǁRunnerAgentǁ__repr____mutmut_2, 
        'xǁRunnerAgentǁ__repr____mutmut_3': xǁRunnerAgentǁ__repr____mutmut_3, 
        'xǁRunnerAgentǁ__repr____mutmut_4': xǁRunnerAgentǁ__repr____mutmut_4, 
        'xǁRunnerAgentǁ__repr____mutmut_5': xǁRunnerAgentǁ__repr____mutmut_5, 
        'xǁRunnerAgentǁ__repr____mutmut_6': xǁRunnerAgentǁ__repr____mutmut_6
    }
    
    def __repr__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁRunnerAgentǁ__repr____mutmut_orig"), object.__getattribute__(self, "xǁRunnerAgentǁ__repr____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __repr__.__signature__ = _mutmut_signature(xǁRunnerAgentǁ__repr____mutmut_orig)
    xǁRunnerAgentǁ__repr____mutmut_orig.__name__ = 'xǁRunnerAgentǁ__repr__'
