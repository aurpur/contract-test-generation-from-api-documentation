"""
Bruno collection parser for extracting API documentation.

Author: Aurel IKAMA HONEY

This module provides functionality to parse Bruno API collections from both JSON
and .bru file formats, extracting endpoints, requests, and documentation.
"""

import json
import re
from pathlib import Path
from typing import Dict, List, Optional, Union, Any
from loguru import logger

from .bruno_models import (
    BrunoCollection,
    BrunoItem,
    BrunoRequest,
    BrunoHeader,
    BrunoParam,
    BrunoBody,
    BrunoAuth,
    BrunoScript,
    BrunoConfig,
    BrunoRoot,
    BrunoParseResult,
)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class BrunoParser:
    """
    Parser for Bruno API collections.
    
    Supports:
    - JSON format collections
    - Individual .bru files
    - Folder structures with multiple .bru files
    """
    
    def xǁBrunoParserǁ__init____mutmut_orig(self):
        """Initialize the Bruno parser."""
        self.logger = logger.bind(component="BrunoParser")
    
    def xǁBrunoParserǁ__init____mutmut_1(self):
        """Initialize the Bruno parser."""
        self.logger = None
    
    def xǁBrunoParserǁ__init____mutmut_2(self):
        """Initialize the Bruno parser."""
        self.logger = logger.bind(component=None)
    
    def xǁBrunoParserǁ__init____mutmut_3(self):
        """Initialize the Bruno parser."""
        self.logger = logger.bind(component="XXBrunoParserXX")
    
    def xǁBrunoParserǁ__init____mutmut_4(self):
        """Initialize the Bruno parser."""
        self.logger = logger.bind(component="brunoparser")
    
    def xǁBrunoParserǁ__init____mutmut_5(self):
        """Initialize the Bruno parser."""
        self.logger = logger.bind(component="BRUNOPARSER")
    
    xǁBrunoParserǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ__init____mutmut_1': xǁBrunoParserǁ__init____mutmut_1, 
        'xǁBrunoParserǁ__init____mutmut_2': xǁBrunoParserǁ__init____mutmut_2, 
        'xǁBrunoParserǁ__init____mutmut_3': xǁBrunoParserǁ__init____mutmut_3, 
        'xǁBrunoParserǁ__init____mutmut_4': xǁBrunoParserǁ__init____mutmut_4, 
        'xǁBrunoParserǁ__init____mutmut_5': xǁBrunoParserǁ__init____mutmut_5
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁBrunoParserǁ__init____mutmut_orig)
    xǁBrunoParserǁ__init____mutmut_orig.__name__ = 'xǁBrunoParserǁ__init__'
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_orig(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_1(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = None
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_2(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(None)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_3(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_4(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(None)
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_5(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(None)
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_6(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(None, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_7(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, None, encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_8(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding=None) as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_9(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open('r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_10(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_11(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', ) as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_12(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'XXrXX', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_13(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'R', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_14(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='XXutf-8XX') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_15(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='UTF-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_16(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = None
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_17(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(None)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_18(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = None
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_19(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = None
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_20(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(None)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_21(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = None
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_22(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=None,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_23(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=None,
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_24(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=None,
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_25(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=None,
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_26(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=None,
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_27(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=None,
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_28(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=None,
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_29(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_30(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_31(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_32(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_33(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_34(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_35(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_36(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['XXtotal_requestsXX'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_37(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['TOTAL_REQUESTS'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_38(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['XXtotal_foldersXX'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_39(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['TOTAL_FOLDERS'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_40(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['XXendpointsXX'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_41(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['ENDPOINTS'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_42(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['XXmethodsXX'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_43(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['METHODS'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_44(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['XXhas_authenticationXX'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_45(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['HAS_AUTHENTICATION'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_46(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['XXhas_documentationXX'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_47(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['HAS_DOCUMENTATION'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_48(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                None
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_49(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['XXtotal_requestsXX']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_50(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['TOTAL_REQUESTS']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_51(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['XXtotal_foldersXX']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_52(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['TOTAL_FOLDERS']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_53(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(None)
        except Exception as e:
            raise ValueError(f"Failed to parse Bruno collection: {e}")
    
    def xǁBrunoParserǁparse_collection_from_json__mutmut_54(self, json_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse a Bruno collection from a JSON file.
        
        Args:
            json_path: Path to the JSON collection file
            
        Returns:
            BrunoParseResult with parsed collection and metadata
            
        Raises:
            FileNotFoundError: If the JSON file doesn't exist
            ValueError: If the JSON is invalid or doesn't match Bruno schema
        """
        json_path = Path(json_path)
        
        if not json_path.exists():
            raise FileNotFoundError(f"JSON file not found: {json_path}")
        
        self.logger.info(f"Parsing Bruno collection from JSON: {json_path}")
        
        try:
            with open(json_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            # Parse the collection using Pydantic models (no validation of JSON bodies)
            collection = BrunoCollection(**data)
            
            # Extract metadata with optimized single-pass traversal
            metadata = self._extract_metadata_optimized(collection)
            
            result = BrunoParseResult(
                collection=collection,
                total_requests=metadata['total_requests'],
                total_folders=metadata['total_folders'],
                endpoints=metadata['endpoints'],
                methods=metadata['methods'],
                has_authentication=metadata['has_authentication'],
                has_documentation=metadata['has_documentation'],
            )
            
            self.logger.success(
                f"✓ Parsed collection '{collection.name}': "
                f"{metadata['total_requests']} requests, {metadata['total_folders']} folders"
            )
            
            return result
            
        except json.JSONDecodeError as e:
            raise ValueError(f"Invalid JSON format: {e}")
        except Exception as e:
            raise ValueError(None)
    
    xǁBrunoParserǁparse_collection_from_json__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁparse_collection_from_json__mutmut_1': xǁBrunoParserǁparse_collection_from_json__mutmut_1, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_2': xǁBrunoParserǁparse_collection_from_json__mutmut_2, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_3': xǁBrunoParserǁparse_collection_from_json__mutmut_3, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_4': xǁBrunoParserǁparse_collection_from_json__mutmut_4, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_5': xǁBrunoParserǁparse_collection_from_json__mutmut_5, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_6': xǁBrunoParserǁparse_collection_from_json__mutmut_6, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_7': xǁBrunoParserǁparse_collection_from_json__mutmut_7, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_8': xǁBrunoParserǁparse_collection_from_json__mutmut_8, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_9': xǁBrunoParserǁparse_collection_from_json__mutmut_9, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_10': xǁBrunoParserǁparse_collection_from_json__mutmut_10, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_11': xǁBrunoParserǁparse_collection_from_json__mutmut_11, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_12': xǁBrunoParserǁparse_collection_from_json__mutmut_12, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_13': xǁBrunoParserǁparse_collection_from_json__mutmut_13, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_14': xǁBrunoParserǁparse_collection_from_json__mutmut_14, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_15': xǁBrunoParserǁparse_collection_from_json__mutmut_15, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_16': xǁBrunoParserǁparse_collection_from_json__mutmut_16, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_17': xǁBrunoParserǁparse_collection_from_json__mutmut_17, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_18': xǁBrunoParserǁparse_collection_from_json__mutmut_18, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_19': xǁBrunoParserǁparse_collection_from_json__mutmut_19, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_20': xǁBrunoParserǁparse_collection_from_json__mutmut_20, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_21': xǁBrunoParserǁparse_collection_from_json__mutmut_21, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_22': xǁBrunoParserǁparse_collection_from_json__mutmut_22, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_23': xǁBrunoParserǁparse_collection_from_json__mutmut_23, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_24': xǁBrunoParserǁparse_collection_from_json__mutmut_24, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_25': xǁBrunoParserǁparse_collection_from_json__mutmut_25, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_26': xǁBrunoParserǁparse_collection_from_json__mutmut_26, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_27': xǁBrunoParserǁparse_collection_from_json__mutmut_27, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_28': xǁBrunoParserǁparse_collection_from_json__mutmut_28, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_29': xǁBrunoParserǁparse_collection_from_json__mutmut_29, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_30': xǁBrunoParserǁparse_collection_from_json__mutmut_30, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_31': xǁBrunoParserǁparse_collection_from_json__mutmut_31, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_32': xǁBrunoParserǁparse_collection_from_json__mutmut_32, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_33': xǁBrunoParserǁparse_collection_from_json__mutmut_33, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_34': xǁBrunoParserǁparse_collection_from_json__mutmut_34, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_35': xǁBrunoParserǁparse_collection_from_json__mutmut_35, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_36': xǁBrunoParserǁparse_collection_from_json__mutmut_36, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_37': xǁBrunoParserǁparse_collection_from_json__mutmut_37, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_38': xǁBrunoParserǁparse_collection_from_json__mutmut_38, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_39': xǁBrunoParserǁparse_collection_from_json__mutmut_39, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_40': xǁBrunoParserǁparse_collection_from_json__mutmut_40, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_41': xǁBrunoParserǁparse_collection_from_json__mutmut_41, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_42': xǁBrunoParserǁparse_collection_from_json__mutmut_42, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_43': xǁBrunoParserǁparse_collection_from_json__mutmut_43, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_44': xǁBrunoParserǁparse_collection_from_json__mutmut_44, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_45': xǁBrunoParserǁparse_collection_from_json__mutmut_45, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_46': xǁBrunoParserǁparse_collection_from_json__mutmut_46, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_47': xǁBrunoParserǁparse_collection_from_json__mutmut_47, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_48': xǁBrunoParserǁparse_collection_from_json__mutmut_48, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_49': xǁBrunoParserǁparse_collection_from_json__mutmut_49, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_50': xǁBrunoParserǁparse_collection_from_json__mutmut_50, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_51': xǁBrunoParserǁparse_collection_from_json__mutmut_51, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_52': xǁBrunoParserǁparse_collection_from_json__mutmut_52, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_53': xǁBrunoParserǁparse_collection_from_json__mutmut_53, 
        'xǁBrunoParserǁparse_collection_from_json__mutmut_54': xǁBrunoParserǁparse_collection_from_json__mutmut_54
    }
    
    def parse_collection_from_json(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁparse_collection_from_json__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁparse_collection_from_json__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_collection_from_json.__signature__ = _mutmut_signature(xǁBrunoParserǁparse_collection_from_json__mutmut_orig)
    xǁBrunoParserǁparse_collection_from_json__mutmut_orig.__name__ = 'xǁBrunoParserǁparse_collection_from_json'
    
    def xǁBrunoParserǁparse_bru_file__mutmut_orig(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_1(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = None
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_2(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(None)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_3(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_4(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(None)
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_5(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(None)
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_6(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(None, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_7(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, None, encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_8(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding=None) as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_9(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open('r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_10(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_11(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', ) as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_12(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'XXrXX', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_13(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'R', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_14(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='XXutf-8XX') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_15(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='UTF-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_16(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = None
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_17(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = None
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_18(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(None, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_19(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, None)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_20(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_21(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, )
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_22(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(None)
            
            return item
            
        except Exception as e:
            raise ValueError(f"Failed to parse .bru file: {e}")
    
    def xǁBrunoParserǁparse_bru_file__mutmut_23(self, bru_path: Union[str, Path]) -> BrunoItem:
        """
        Parse a single .bru file.
        
        Args:
            bru_path: Path to the .bru file
            
        Returns:
            BrunoItem representing the parsed request
            
        Raises:
            FileNotFoundError: If the .bru file doesn't exist
            ValueError: If the .bru format is invalid
        """
        bru_path = Path(bru_path)
        
        if not bru_path.exists():
            raise FileNotFoundError(f".bru file not found: {bru_path}")
        
        self.logger.info(f"Parsing .bru file: {bru_path}")
        
        try:
            with open(bru_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Parse .bru format
            item = self._parse_bru_content(content, bru_path.stem)
            
            self.logger.success(f"✓ Parsed .bru file '{item.name}'")
            
            return item
            
        except Exception as e:
            raise ValueError(None)
    
    xǁBrunoParserǁparse_bru_file__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁparse_bru_file__mutmut_1': xǁBrunoParserǁparse_bru_file__mutmut_1, 
        'xǁBrunoParserǁparse_bru_file__mutmut_2': xǁBrunoParserǁparse_bru_file__mutmut_2, 
        'xǁBrunoParserǁparse_bru_file__mutmut_3': xǁBrunoParserǁparse_bru_file__mutmut_3, 
        'xǁBrunoParserǁparse_bru_file__mutmut_4': xǁBrunoParserǁparse_bru_file__mutmut_4, 
        'xǁBrunoParserǁparse_bru_file__mutmut_5': xǁBrunoParserǁparse_bru_file__mutmut_5, 
        'xǁBrunoParserǁparse_bru_file__mutmut_6': xǁBrunoParserǁparse_bru_file__mutmut_6, 
        'xǁBrunoParserǁparse_bru_file__mutmut_7': xǁBrunoParserǁparse_bru_file__mutmut_7, 
        'xǁBrunoParserǁparse_bru_file__mutmut_8': xǁBrunoParserǁparse_bru_file__mutmut_8, 
        'xǁBrunoParserǁparse_bru_file__mutmut_9': xǁBrunoParserǁparse_bru_file__mutmut_9, 
        'xǁBrunoParserǁparse_bru_file__mutmut_10': xǁBrunoParserǁparse_bru_file__mutmut_10, 
        'xǁBrunoParserǁparse_bru_file__mutmut_11': xǁBrunoParserǁparse_bru_file__mutmut_11, 
        'xǁBrunoParserǁparse_bru_file__mutmut_12': xǁBrunoParserǁparse_bru_file__mutmut_12, 
        'xǁBrunoParserǁparse_bru_file__mutmut_13': xǁBrunoParserǁparse_bru_file__mutmut_13, 
        'xǁBrunoParserǁparse_bru_file__mutmut_14': xǁBrunoParserǁparse_bru_file__mutmut_14, 
        'xǁBrunoParserǁparse_bru_file__mutmut_15': xǁBrunoParserǁparse_bru_file__mutmut_15, 
        'xǁBrunoParserǁparse_bru_file__mutmut_16': xǁBrunoParserǁparse_bru_file__mutmut_16, 
        'xǁBrunoParserǁparse_bru_file__mutmut_17': xǁBrunoParserǁparse_bru_file__mutmut_17, 
        'xǁBrunoParserǁparse_bru_file__mutmut_18': xǁBrunoParserǁparse_bru_file__mutmut_18, 
        'xǁBrunoParserǁparse_bru_file__mutmut_19': xǁBrunoParserǁparse_bru_file__mutmut_19, 
        'xǁBrunoParserǁparse_bru_file__mutmut_20': xǁBrunoParserǁparse_bru_file__mutmut_20, 
        'xǁBrunoParserǁparse_bru_file__mutmut_21': xǁBrunoParserǁparse_bru_file__mutmut_21, 
        'xǁBrunoParserǁparse_bru_file__mutmut_22': xǁBrunoParserǁparse_bru_file__mutmut_22, 
        'xǁBrunoParserǁparse_bru_file__mutmut_23': xǁBrunoParserǁparse_bru_file__mutmut_23
    }
    
    def parse_bru_file(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁparse_bru_file__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁparse_bru_file__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_bru_file.__signature__ = _mutmut_signature(xǁBrunoParserǁparse_bru_file__mutmut_orig)
    xǁBrunoParserǁparse_bru_file__mutmut_orig.__name__ = 'xǁBrunoParserǁparse_bru_file'
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_orig(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_1(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = None
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_2(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(None)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_3(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_4(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(None)
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_5(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(None)
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_6(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = None
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_7(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(None)
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_8(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob(None))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_9(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("XX*.bruXX"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_10(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.BRU"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_11(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_12(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(None)
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_13(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = None
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_14(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = None
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_15(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(None)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_16(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(None)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_17(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(None)
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_18(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = None
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_19(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=None,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_20(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version=None,
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_21(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=None,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_22(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=None
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_23(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_24(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_25(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_26(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_27(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="XX1XX",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_28(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version=None,
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_29(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=None,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_30(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=None,
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_31(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_32(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_33(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_34(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="XX1XX",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_35(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = None
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_36(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(None)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_37(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = None
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_38(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=None,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_39(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=None,
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_40(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=None,
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_41(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=None,
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_42(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=None,
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_43(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=None,
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_44(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=None,
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_45(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_46(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_47(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_48(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_49(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_50(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_51(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_52(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['XXtotal_requestsXX'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_53(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['TOTAL_REQUESTS'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_54(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['XXtotal_foldersXX'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_55(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['TOTAL_FOLDERS'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_56(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['XXendpointsXX'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_57(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['ENDPOINTS'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_58(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['XXmethodsXX'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_59(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['METHODS'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_60(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['XXhas_authenticationXX'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_61(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['HAS_AUTHENTICATION'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_62(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['XXhas_documentationXX'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_63(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['HAS_DOCUMENTATION'],
        )
        
        self.logger.success(
            f"✓ Parsed {len(bru_files)} .bru files from '{folder_path.name}'"
        )
        
        return result
    
    def xǁBrunoParserǁparse_bru_folder__mutmut_64(self, folder_path: Union[str, Path]) -> BrunoParseResult:
        """
        Parse all .bru files in a folder structure.
        
        Args:
            folder_path: Path to the folder containing .bru files
            
        Returns:
            BrunoParseResult with all parsed requests
            
        Raises:
            FileNotFoundError: If the folder doesn't exist
        """
        folder_path = Path(folder_path)
        
        if not folder_path.exists():
            raise FileNotFoundError(f"Folder not found: {folder_path}")
        
        self.logger.info(f"Parsing .bru files from folder: {folder_path}")
        
        # Find all .bru files
        bru_files = list(folder_path.rglob("*.bru"))
        
        if not bru_files:
            self.logger.warning(f"No .bru files found in {folder_path}")
        
        items = []
        for bru_file in bru_files:
            try:
                item = self.parse_bru_file(bru_file)
                items.append(item)
            except Exception as e:
                self.logger.error(f"Failed to parse {bru_file}: {e}")
        
        # Create a collection from the parsed items
        collection = BrunoCollection(
            name=folder_path.name,
            version="1",
            items=items,
            brunoConfig=BrunoConfig(
                version="1",
                name=folder_path.name,
                filesCount=len(items),
            )
        )
        
        # Extract metadata with optimized single-pass traversal
        metadata = self._extract_metadata_optimized(collection)
        
        result = BrunoParseResult(
            collection=collection,
            total_requests=metadata['total_requests'],
            total_folders=metadata['total_folders'],
            endpoints=metadata['endpoints'],
            methods=metadata['methods'],
            has_authentication=metadata['has_authentication'],
            has_documentation=metadata['has_documentation'],
        )
        
        self.logger.success(
            None
        )
        
        return result
    
    xǁBrunoParserǁparse_bru_folder__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁparse_bru_folder__mutmut_1': xǁBrunoParserǁparse_bru_folder__mutmut_1, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_2': xǁBrunoParserǁparse_bru_folder__mutmut_2, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_3': xǁBrunoParserǁparse_bru_folder__mutmut_3, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_4': xǁBrunoParserǁparse_bru_folder__mutmut_4, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_5': xǁBrunoParserǁparse_bru_folder__mutmut_5, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_6': xǁBrunoParserǁparse_bru_folder__mutmut_6, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_7': xǁBrunoParserǁparse_bru_folder__mutmut_7, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_8': xǁBrunoParserǁparse_bru_folder__mutmut_8, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_9': xǁBrunoParserǁparse_bru_folder__mutmut_9, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_10': xǁBrunoParserǁparse_bru_folder__mutmut_10, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_11': xǁBrunoParserǁparse_bru_folder__mutmut_11, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_12': xǁBrunoParserǁparse_bru_folder__mutmut_12, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_13': xǁBrunoParserǁparse_bru_folder__mutmut_13, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_14': xǁBrunoParserǁparse_bru_folder__mutmut_14, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_15': xǁBrunoParserǁparse_bru_folder__mutmut_15, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_16': xǁBrunoParserǁparse_bru_folder__mutmut_16, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_17': xǁBrunoParserǁparse_bru_folder__mutmut_17, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_18': xǁBrunoParserǁparse_bru_folder__mutmut_18, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_19': xǁBrunoParserǁparse_bru_folder__mutmut_19, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_20': xǁBrunoParserǁparse_bru_folder__mutmut_20, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_21': xǁBrunoParserǁparse_bru_folder__mutmut_21, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_22': xǁBrunoParserǁparse_bru_folder__mutmut_22, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_23': xǁBrunoParserǁparse_bru_folder__mutmut_23, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_24': xǁBrunoParserǁparse_bru_folder__mutmut_24, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_25': xǁBrunoParserǁparse_bru_folder__mutmut_25, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_26': xǁBrunoParserǁparse_bru_folder__mutmut_26, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_27': xǁBrunoParserǁparse_bru_folder__mutmut_27, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_28': xǁBrunoParserǁparse_bru_folder__mutmut_28, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_29': xǁBrunoParserǁparse_bru_folder__mutmut_29, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_30': xǁBrunoParserǁparse_bru_folder__mutmut_30, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_31': xǁBrunoParserǁparse_bru_folder__mutmut_31, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_32': xǁBrunoParserǁparse_bru_folder__mutmut_32, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_33': xǁBrunoParserǁparse_bru_folder__mutmut_33, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_34': xǁBrunoParserǁparse_bru_folder__mutmut_34, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_35': xǁBrunoParserǁparse_bru_folder__mutmut_35, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_36': xǁBrunoParserǁparse_bru_folder__mutmut_36, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_37': xǁBrunoParserǁparse_bru_folder__mutmut_37, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_38': xǁBrunoParserǁparse_bru_folder__mutmut_38, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_39': xǁBrunoParserǁparse_bru_folder__mutmut_39, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_40': xǁBrunoParserǁparse_bru_folder__mutmut_40, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_41': xǁBrunoParserǁparse_bru_folder__mutmut_41, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_42': xǁBrunoParserǁparse_bru_folder__mutmut_42, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_43': xǁBrunoParserǁparse_bru_folder__mutmut_43, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_44': xǁBrunoParserǁparse_bru_folder__mutmut_44, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_45': xǁBrunoParserǁparse_bru_folder__mutmut_45, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_46': xǁBrunoParserǁparse_bru_folder__mutmut_46, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_47': xǁBrunoParserǁparse_bru_folder__mutmut_47, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_48': xǁBrunoParserǁparse_bru_folder__mutmut_48, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_49': xǁBrunoParserǁparse_bru_folder__mutmut_49, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_50': xǁBrunoParserǁparse_bru_folder__mutmut_50, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_51': xǁBrunoParserǁparse_bru_folder__mutmut_51, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_52': xǁBrunoParserǁparse_bru_folder__mutmut_52, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_53': xǁBrunoParserǁparse_bru_folder__mutmut_53, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_54': xǁBrunoParserǁparse_bru_folder__mutmut_54, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_55': xǁBrunoParserǁparse_bru_folder__mutmut_55, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_56': xǁBrunoParserǁparse_bru_folder__mutmut_56, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_57': xǁBrunoParserǁparse_bru_folder__mutmut_57, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_58': xǁBrunoParserǁparse_bru_folder__mutmut_58, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_59': xǁBrunoParserǁparse_bru_folder__mutmut_59, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_60': xǁBrunoParserǁparse_bru_folder__mutmut_60, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_61': xǁBrunoParserǁparse_bru_folder__mutmut_61, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_62': xǁBrunoParserǁparse_bru_folder__mutmut_62, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_63': xǁBrunoParserǁparse_bru_folder__mutmut_63, 
        'xǁBrunoParserǁparse_bru_folder__mutmut_64': xǁBrunoParserǁparse_bru_folder__mutmut_64
    }
    
    def parse_bru_folder(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁparse_bru_folder__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁparse_bru_folder__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_bru_folder.__signature__ = _mutmut_signature(xǁBrunoParserǁparse_bru_folder__mutmut_orig)
    xǁBrunoParserǁparse_bru_folder__mutmut_orig.__name__ = 'xǁBrunoParserǁparse_bru_folder'
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_orig(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_1(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = None
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_2(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(None)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_3(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = None
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_4(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get(None, {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_5(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', None)
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_6(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get({})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_7(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', )
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_8(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('XXmetaXX', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_9(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('META', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_10(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = None
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_11(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get(None, name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_12(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', None)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_13(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get(name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_14(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', )
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_15(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('XXnameXX', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_16(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('NAME', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_17(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = None
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_18(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get(None, 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_19(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', None)
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_20(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_21(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', )
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_22(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('XXtypeXX', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_23(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('TYPE', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_24(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'XXhttpXX')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_25(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'HTTP')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_26(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = ""
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_27(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = ""
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_28(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['XXgetXX', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_29(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['GET', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_30(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'XXpostXX', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_31(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'POST', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_32(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'XXputXX', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_33(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'PUT', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_34(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'XXdeleteXX', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_35(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'DELETE', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_36(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'XXpatchXX', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_37(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'PATCH', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_38(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'XXheadXX', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_39(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'HEAD', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_40(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'XXoptionsXX']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_41(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'OPTIONS']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_42(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb not in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_43(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = None
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_44(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.lower()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_45(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = None
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_46(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = None
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_47(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get(None, '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_48(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', None)
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_49(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_50(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', )
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_51(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('XXurlXX', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_52(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('URL', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_53(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', 'XXXX')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_54(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                return
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_55(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method and not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_56(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_57(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_58(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(None)
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_59(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = None
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_60(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'XXheadersXX' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_61(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'HEADERS' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_62(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' not in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_63(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['XXheadersXX'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_64(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['HEADERS'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_65(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(None)
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_66(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=None, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_67(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=None))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_68(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_69(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, ))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_70(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = None
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_71(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections and 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_72(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'XXparamsXX' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_73(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'PARAMS' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_74(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' not in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_75(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'XXqueryXX' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_76(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'QUERY' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_77(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' not in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_78(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = None
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_79(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get(None, sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_80(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', None)
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_81(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get(sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_82(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', )
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_83(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('XXparamsXX', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_84(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('PARAMS', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_85(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get(None, {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_86(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', None))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_87(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get({}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_88(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', ))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_89(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('XXqueryXX', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_90(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('QUERY', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_91(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(None)
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_92(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=None, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_93(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=None))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_94(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_95(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, ))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_96(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = None
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_97(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'XXbody:jsonXX' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_98(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'BODY:JSON' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_99(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' not in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_100(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = None
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_101(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'XXjsonXX'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_102(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'JSON'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_103(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = None  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_104(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get(None, '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_105(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', None)  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_106(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_107(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', )  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_108(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['XXbody:jsonXX'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_109(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['BODY:JSON'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_110(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('XX_raw_contentXX', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_111(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_RAW_CONTENT', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_112(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', 'XXXX')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_113(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'XXbody:xmlXX' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_114(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'BODY:XML' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_115(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' not in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_116(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = None
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_117(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'XXxmlXX'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_118(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'XML'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_119(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = None
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_120(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get(None, '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_121(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', None)
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_122(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_123(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', )
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_124(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['XXbody:xmlXX'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_125(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['BODY:XML'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_126(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('XX_raw_contentXX', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_127(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_RAW_CONTENT', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_128(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', 'XXXX')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_129(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'XXbody:textXX' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_130(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'BODY:TEXT' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_131(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' not in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_132(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = None
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_133(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'XXtextXX'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_134(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'TEXT'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_135(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = None
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_136(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get(None, '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_137(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', None)
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_138(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_139(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', )
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_140(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['XXbody:textXX'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_141(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['BODY:TEXT'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_142(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('XX_raw_contentXX', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_143(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_RAW_CONTENT', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_144(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', 'XXXX')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_145(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = None
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_146(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'XXauthXX' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_147(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'AUTH' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_148(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' not in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_149(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = None
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_150(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['XXauthXX']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_151(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['AUTH']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_152(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = None
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_153(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get(None, 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_154(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', None)
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_155(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_156(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', )
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_157(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('XXmodeXX', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_158(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('MODE', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_159(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'XXnoneXX')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_160(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'NONE')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_161(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'XXbasicXX' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_162(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'BASIC' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_163(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' not in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_164(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = None
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_165(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get(None)
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_166(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['XXbasicXX'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_167(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['BASIC'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_168(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('XXusernameXX')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_169(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('USERNAME')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_170(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = None
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_171(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get(None)
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_172(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['XXbasicXX'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_173(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['BASIC'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_174(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('XXpasswordXX')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_175(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('PASSWORD')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_176(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'XXbearerXX' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_177(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'BEARER' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_178(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' not in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_179(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = None
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_180(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get(None)
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_181(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['XXbearerXX'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_182(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['BEARER'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_183(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('XXtokenXX')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_184(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('TOKEN')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_185(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = None
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_186(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get(None, '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_187(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', None)
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_188(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_189(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', )
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_190(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get(None, {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_191(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', None).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_192(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get({}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_193(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', ).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_194(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('XXdocsXX', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_195(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('DOCS', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_196(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('XX_raw_contentXX', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_197(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_RAW_CONTENT', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_198(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', 'XXXX')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_199(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = None
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_200(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get(None, '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_201(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', None)
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_202(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_203(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', )
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_204(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get(None, {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_205(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', None).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_206(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get({}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_207(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', ).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_208(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('XXtestsXX', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_209(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('TESTS', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_210(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('XX_raw_contentXX', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_211(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_RAW_CONTENT', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_212(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', 'XXXX')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_213(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = None
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_214(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get(None, '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_215(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', None)
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_216(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_217(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', )
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_218(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get(None, {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_219(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', None).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_220(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get({}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_221(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', ).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_222(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('XXassertXX', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_223(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('ASSERT', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_224(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('XX_raw_contentXX', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_225(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_RAW_CONTENT', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_226(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', 'XXXX')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_227(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = None
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_228(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=None,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_229(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=None,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_230(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=None,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_231(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=None,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_232(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=None,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_233(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=None,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_234(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=None,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_235(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=None,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_236(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_237(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_238(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_239(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_240(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_241(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_242(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_243(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_244(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests and assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_245(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = None
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_246(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=None,
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_247(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=None,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_248(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=None,
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_249(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            request=None,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_250(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            name=request_name,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_251(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            filename=f"{name}.bru",
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_252(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            request=request,
        )
        
        return item
    
    def xǁBrunoParserǁ_parse_bru_content__mutmut_253(self, content: str, name: str) -> BrunoItem:
        """
        Parse the content of a .bru file.
        
        The .bru format is a custom text format with sections like:
        meta {
          name: Request Name
          type: http
        }
        
        get {
          url: https://api.example.com/endpoint
        }
        
        headers {
          Content-Type: application/json
        }
        
        body:json {
          {"key": "value"}
        }
        
        docs {
          Description of the request
        }
        """
        sections = self._extract_sections(content)
        
        # Parse meta section
        meta = sections.get('meta', {})
        request_name = meta.get('name', name)
        request_type = meta.get('type', 'http')
        
        # Parse request method and URL
        method = None
        url = None
        
        for verb in ['get', 'post', 'put', 'delete', 'patch', 'head', 'options']:
            if verb in sections:
                method = verb.upper()
                verb_data = sections[verb]
                url = verb_data.get('url', '')
                break
        
        if not method or not url:
            raise ValueError(f"Could not extract method and URL from .bru file")
        
        # Parse headers
        headers = []
        if 'headers' in sections:
            for key, value in sections['headers'].items():
                headers.append(BrunoHeader(name=key, value=value))
        
        # Parse query params
        params = []
        if 'params' in sections or 'query' in sections:
            query_section = sections.get('params', sections.get('query', {}))
            for key, value in query_section.items():
                params.append(BrunoParam(name=key, value=value))
        
        # Parse body (lazy - no immediate JSON validation)
        body = BrunoBody()
        if 'body:json' in sections:
            body.mode = 'json'
            body.json = sections['body:json'].get('_raw_content', '')  # No validation yet
        elif 'body:xml' in sections:
            body.mode = 'xml'
            body.xml = sections['body:xml'].get('_raw_content', '')
        elif 'body:text' in sections:
            body.mode = 'text'
            body.text = sections['body:text'].get('_raw_content', '')
        
        # Parse auth
        auth = BrunoAuth()
        if 'auth' in sections:
            auth_data = sections['auth']
            auth.mode = auth_data.get('mode', 'none')
            if 'basic' in auth_data:
                auth.username = auth_data['basic'].get('username')
                auth.password = auth_data['basic'].get('password')
            elif 'bearer' in auth_data:
                auth.token = auth_data['bearer'].get('token')
        
        # Parse docs
        docs = sections.get('docs', {}).get('_raw_content', '')
        
        # Parse tests/assertions
        tests = sections.get('tests', {}).get('_raw_content', '')
        assertions = sections.get('assert', {}).get('_raw_content', '')
        
        # Create the request
        request = BrunoRequest(
            url=url,
            method=method,
            headers=headers,
            params=params,
            body=body,
            auth=auth,
            docs=docs,
            tests=tests or assertions,
        )
        
        # Create the item
        item = BrunoItem(
            type=request_type,
            name=request_name,
            filename=f"{name}.bru",
            )
        
        return item
    
    xǁBrunoParserǁ_parse_bru_content__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_parse_bru_content__mutmut_1': xǁBrunoParserǁ_parse_bru_content__mutmut_1, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_2': xǁBrunoParserǁ_parse_bru_content__mutmut_2, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_3': xǁBrunoParserǁ_parse_bru_content__mutmut_3, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_4': xǁBrunoParserǁ_parse_bru_content__mutmut_4, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_5': xǁBrunoParserǁ_parse_bru_content__mutmut_5, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_6': xǁBrunoParserǁ_parse_bru_content__mutmut_6, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_7': xǁBrunoParserǁ_parse_bru_content__mutmut_7, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_8': xǁBrunoParserǁ_parse_bru_content__mutmut_8, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_9': xǁBrunoParserǁ_parse_bru_content__mutmut_9, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_10': xǁBrunoParserǁ_parse_bru_content__mutmut_10, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_11': xǁBrunoParserǁ_parse_bru_content__mutmut_11, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_12': xǁBrunoParserǁ_parse_bru_content__mutmut_12, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_13': xǁBrunoParserǁ_parse_bru_content__mutmut_13, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_14': xǁBrunoParserǁ_parse_bru_content__mutmut_14, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_15': xǁBrunoParserǁ_parse_bru_content__mutmut_15, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_16': xǁBrunoParserǁ_parse_bru_content__mutmut_16, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_17': xǁBrunoParserǁ_parse_bru_content__mutmut_17, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_18': xǁBrunoParserǁ_parse_bru_content__mutmut_18, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_19': xǁBrunoParserǁ_parse_bru_content__mutmut_19, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_20': xǁBrunoParserǁ_parse_bru_content__mutmut_20, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_21': xǁBrunoParserǁ_parse_bru_content__mutmut_21, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_22': xǁBrunoParserǁ_parse_bru_content__mutmut_22, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_23': xǁBrunoParserǁ_parse_bru_content__mutmut_23, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_24': xǁBrunoParserǁ_parse_bru_content__mutmut_24, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_25': xǁBrunoParserǁ_parse_bru_content__mutmut_25, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_26': xǁBrunoParserǁ_parse_bru_content__mutmut_26, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_27': xǁBrunoParserǁ_parse_bru_content__mutmut_27, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_28': xǁBrunoParserǁ_parse_bru_content__mutmut_28, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_29': xǁBrunoParserǁ_parse_bru_content__mutmut_29, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_30': xǁBrunoParserǁ_parse_bru_content__mutmut_30, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_31': xǁBrunoParserǁ_parse_bru_content__mutmut_31, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_32': xǁBrunoParserǁ_parse_bru_content__mutmut_32, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_33': xǁBrunoParserǁ_parse_bru_content__mutmut_33, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_34': xǁBrunoParserǁ_parse_bru_content__mutmut_34, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_35': xǁBrunoParserǁ_parse_bru_content__mutmut_35, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_36': xǁBrunoParserǁ_parse_bru_content__mutmut_36, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_37': xǁBrunoParserǁ_parse_bru_content__mutmut_37, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_38': xǁBrunoParserǁ_parse_bru_content__mutmut_38, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_39': xǁBrunoParserǁ_parse_bru_content__mutmut_39, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_40': xǁBrunoParserǁ_parse_bru_content__mutmut_40, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_41': xǁBrunoParserǁ_parse_bru_content__mutmut_41, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_42': xǁBrunoParserǁ_parse_bru_content__mutmut_42, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_43': xǁBrunoParserǁ_parse_bru_content__mutmut_43, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_44': xǁBrunoParserǁ_parse_bru_content__mutmut_44, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_45': xǁBrunoParserǁ_parse_bru_content__mutmut_45, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_46': xǁBrunoParserǁ_parse_bru_content__mutmut_46, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_47': xǁBrunoParserǁ_parse_bru_content__mutmut_47, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_48': xǁBrunoParserǁ_parse_bru_content__mutmut_48, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_49': xǁBrunoParserǁ_parse_bru_content__mutmut_49, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_50': xǁBrunoParserǁ_parse_bru_content__mutmut_50, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_51': xǁBrunoParserǁ_parse_bru_content__mutmut_51, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_52': xǁBrunoParserǁ_parse_bru_content__mutmut_52, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_53': xǁBrunoParserǁ_parse_bru_content__mutmut_53, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_54': xǁBrunoParserǁ_parse_bru_content__mutmut_54, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_55': xǁBrunoParserǁ_parse_bru_content__mutmut_55, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_56': xǁBrunoParserǁ_parse_bru_content__mutmut_56, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_57': xǁBrunoParserǁ_parse_bru_content__mutmut_57, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_58': xǁBrunoParserǁ_parse_bru_content__mutmut_58, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_59': xǁBrunoParserǁ_parse_bru_content__mutmut_59, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_60': xǁBrunoParserǁ_parse_bru_content__mutmut_60, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_61': xǁBrunoParserǁ_parse_bru_content__mutmut_61, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_62': xǁBrunoParserǁ_parse_bru_content__mutmut_62, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_63': xǁBrunoParserǁ_parse_bru_content__mutmut_63, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_64': xǁBrunoParserǁ_parse_bru_content__mutmut_64, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_65': xǁBrunoParserǁ_parse_bru_content__mutmut_65, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_66': xǁBrunoParserǁ_parse_bru_content__mutmut_66, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_67': xǁBrunoParserǁ_parse_bru_content__mutmut_67, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_68': xǁBrunoParserǁ_parse_bru_content__mutmut_68, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_69': xǁBrunoParserǁ_parse_bru_content__mutmut_69, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_70': xǁBrunoParserǁ_parse_bru_content__mutmut_70, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_71': xǁBrunoParserǁ_parse_bru_content__mutmut_71, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_72': xǁBrunoParserǁ_parse_bru_content__mutmut_72, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_73': xǁBrunoParserǁ_parse_bru_content__mutmut_73, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_74': xǁBrunoParserǁ_parse_bru_content__mutmut_74, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_75': xǁBrunoParserǁ_parse_bru_content__mutmut_75, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_76': xǁBrunoParserǁ_parse_bru_content__mutmut_76, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_77': xǁBrunoParserǁ_parse_bru_content__mutmut_77, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_78': xǁBrunoParserǁ_parse_bru_content__mutmut_78, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_79': xǁBrunoParserǁ_parse_bru_content__mutmut_79, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_80': xǁBrunoParserǁ_parse_bru_content__mutmut_80, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_81': xǁBrunoParserǁ_parse_bru_content__mutmut_81, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_82': xǁBrunoParserǁ_parse_bru_content__mutmut_82, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_83': xǁBrunoParserǁ_parse_bru_content__mutmut_83, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_84': xǁBrunoParserǁ_parse_bru_content__mutmut_84, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_85': xǁBrunoParserǁ_parse_bru_content__mutmut_85, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_86': xǁBrunoParserǁ_parse_bru_content__mutmut_86, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_87': xǁBrunoParserǁ_parse_bru_content__mutmut_87, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_88': xǁBrunoParserǁ_parse_bru_content__mutmut_88, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_89': xǁBrunoParserǁ_parse_bru_content__mutmut_89, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_90': xǁBrunoParserǁ_parse_bru_content__mutmut_90, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_91': xǁBrunoParserǁ_parse_bru_content__mutmut_91, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_92': xǁBrunoParserǁ_parse_bru_content__mutmut_92, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_93': xǁBrunoParserǁ_parse_bru_content__mutmut_93, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_94': xǁBrunoParserǁ_parse_bru_content__mutmut_94, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_95': xǁBrunoParserǁ_parse_bru_content__mutmut_95, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_96': xǁBrunoParserǁ_parse_bru_content__mutmut_96, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_97': xǁBrunoParserǁ_parse_bru_content__mutmut_97, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_98': xǁBrunoParserǁ_parse_bru_content__mutmut_98, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_99': xǁBrunoParserǁ_parse_bru_content__mutmut_99, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_100': xǁBrunoParserǁ_parse_bru_content__mutmut_100, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_101': xǁBrunoParserǁ_parse_bru_content__mutmut_101, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_102': xǁBrunoParserǁ_parse_bru_content__mutmut_102, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_103': xǁBrunoParserǁ_parse_bru_content__mutmut_103, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_104': xǁBrunoParserǁ_parse_bru_content__mutmut_104, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_105': xǁBrunoParserǁ_parse_bru_content__mutmut_105, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_106': xǁBrunoParserǁ_parse_bru_content__mutmut_106, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_107': xǁBrunoParserǁ_parse_bru_content__mutmut_107, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_108': xǁBrunoParserǁ_parse_bru_content__mutmut_108, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_109': xǁBrunoParserǁ_parse_bru_content__mutmut_109, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_110': xǁBrunoParserǁ_parse_bru_content__mutmut_110, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_111': xǁBrunoParserǁ_parse_bru_content__mutmut_111, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_112': xǁBrunoParserǁ_parse_bru_content__mutmut_112, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_113': xǁBrunoParserǁ_parse_bru_content__mutmut_113, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_114': xǁBrunoParserǁ_parse_bru_content__mutmut_114, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_115': xǁBrunoParserǁ_parse_bru_content__mutmut_115, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_116': xǁBrunoParserǁ_parse_bru_content__mutmut_116, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_117': xǁBrunoParserǁ_parse_bru_content__mutmut_117, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_118': xǁBrunoParserǁ_parse_bru_content__mutmut_118, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_119': xǁBrunoParserǁ_parse_bru_content__mutmut_119, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_120': xǁBrunoParserǁ_parse_bru_content__mutmut_120, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_121': xǁBrunoParserǁ_parse_bru_content__mutmut_121, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_122': xǁBrunoParserǁ_parse_bru_content__mutmut_122, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_123': xǁBrunoParserǁ_parse_bru_content__mutmut_123, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_124': xǁBrunoParserǁ_parse_bru_content__mutmut_124, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_125': xǁBrunoParserǁ_parse_bru_content__mutmut_125, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_126': xǁBrunoParserǁ_parse_bru_content__mutmut_126, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_127': xǁBrunoParserǁ_parse_bru_content__mutmut_127, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_128': xǁBrunoParserǁ_parse_bru_content__mutmut_128, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_129': xǁBrunoParserǁ_parse_bru_content__mutmut_129, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_130': xǁBrunoParserǁ_parse_bru_content__mutmut_130, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_131': xǁBrunoParserǁ_parse_bru_content__mutmut_131, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_132': xǁBrunoParserǁ_parse_bru_content__mutmut_132, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_133': xǁBrunoParserǁ_parse_bru_content__mutmut_133, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_134': xǁBrunoParserǁ_parse_bru_content__mutmut_134, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_135': xǁBrunoParserǁ_parse_bru_content__mutmut_135, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_136': xǁBrunoParserǁ_parse_bru_content__mutmut_136, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_137': xǁBrunoParserǁ_parse_bru_content__mutmut_137, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_138': xǁBrunoParserǁ_parse_bru_content__mutmut_138, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_139': xǁBrunoParserǁ_parse_bru_content__mutmut_139, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_140': xǁBrunoParserǁ_parse_bru_content__mutmut_140, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_141': xǁBrunoParserǁ_parse_bru_content__mutmut_141, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_142': xǁBrunoParserǁ_parse_bru_content__mutmut_142, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_143': xǁBrunoParserǁ_parse_bru_content__mutmut_143, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_144': xǁBrunoParserǁ_parse_bru_content__mutmut_144, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_145': xǁBrunoParserǁ_parse_bru_content__mutmut_145, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_146': xǁBrunoParserǁ_parse_bru_content__mutmut_146, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_147': xǁBrunoParserǁ_parse_bru_content__mutmut_147, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_148': xǁBrunoParserǁ_parse_bru_content__mutmut_148, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_149': xǁBrunoParserǁ_parse_bru_content__mutmut_149, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_150': xǁBrunoParserǁ_parse_bru_content__mutmut_150, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_151': xǁBrunoParserǁ_parse_bru_content__mutmut_151, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_152': xǁBrunoParserǁ_parse_bru_content__mutmut_152, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_153': xǁBrunoParserǁ_parse_bru_content__mutmut_153, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_154': xǁBrunoParserǁ_parse_bru_content__mutmut_154, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_155': xǁBrunoParserǁ_parse_bru_content__mutmut_155, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_156': xǁBrunoParserǁ_parse_bru_content__mutmut_156, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_157': xǁBrunoParserǁ_parse_bru_content__mutmut_157, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_158': xǁBrunoParserǁ_parse_bru_content__mutmut_158, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_159': xǁBrunoParserǁ_parse_bru_content__mutmut_159, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_160': xǁBrunoParserǁ_parse_bru_content__mutmut_160, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_161': xǁBrunoParserǁ_parse_bru_content__mutmut_161, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_162': xǁBrunoParserǁ_parse_bru_content__mutmut_162, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_163': xǁBrunoParserǁ_parse_bru_content__mutmut_163, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_164': xǁBrunoParserǁ_parse_bru_content__mutmut_164, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_165': xǁBrunoParserǁ_parse_bru_content__mutmut_165, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_166': xǁBrunoParserǁ_parse_bru_content__mutmut_166, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_167': xǁBrunoParserǁ_parse_bru_content__mutmut_167, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_168': xǁBrunoParserǁ_parse_bru_content__mutmut_168, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_169': xǁBrunoParserǁ_parse_bru_content__mutmut_169, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_170': xǁBrunoParserǁ_parse_bru_content__mutmut_170, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_171': xǁBrunoParserǁ_parse_bru_content__mutmut_171, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_172': xǁBrunoParserǁ_parse_bru_content__mutmut_172, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_173': xǁBrunoParserǁ_parse_bru_content__mutmut_173, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_174': xǁBrunoParserǁ_parse_bru_content__mutmut_174, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_175': xǁBrunoParserǁ_parse_bru_content__mutmut_175, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_176': xǁBrunoParserǁ_parse_bru_content__mutmut_176, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_177': xǁBrunoParserǁ_parse_bru_content__mutmut_177, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_178': xǁBrunoParserǁ_parse_bru_content__mutmut_178, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_179': xǁBrunoParserǁ_parse_bru_content__mutmut_179, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_180': xǁBrunoParserǁ_parse_bru_content__mutmut_180, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_181': xǁBrunoParserǁ_parse_bru_content__mutmut_181, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_182': xǁBrunoParserǁ_parse_bru_content__mutmut_182, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_183': xǁBrunoParserǁ_parse_bru_content__mutmut_183, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_184': xǁBrunoParserǁ_parse_bru_content__mutmut_184, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_185': xǁBrunoParserǁ_parse_bru_content__mutmut_185, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_186': xǁBrunoParserǁ_parse_bru_content__mutmut_186, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_187': xǁBrunoParserǁ_parse_bru_content__mutmut_187, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_188': xǁBrunoParserǁ_parse_bru_content__mutmut_188, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_189': xǁBrunoParserǁ_parse_bru_content__mutmut_189, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_190': xǁBrunoParserǁ_parse_bru_content__mutmut_190, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_191': xǁBrunoParserǁ_parse_bru_content__mutmut_191, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_192': xǁBrunoParserǁ_parse_bru_content__mutmut_192, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_193': xǁBrunoParserǁ_parse_bru_content__mutmut_193, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_194': xǁBrunoParserǁ_parse_bru_content__mutmut_194, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_195': xǁBrunoParserǁ_parse_bru_content__mutmut_195, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_196': xǁBrunoParserǁ_parse_bru_content__mutmut_196, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_197': xǁBrunoParserǁ_parse_bru_content__mutmut_197, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_198': xǁBrunoParserǁ_parse_bru_content__mutmut_198, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_199': xǁBrunoParserǁ_parse_bru_content__mutmut_199, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_200': xǁBrunoParserǁ_parse_bru_content__mutmut_200, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_201': xǁBrunoParserǁ_parse_bru_content__mutmut_201, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_202': xǁBrunoParserǁ_parse_bru_content__mutmut_202, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_203': xǁBrunoParserǁ_parse_bru_content__mutmut_203, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_204': xǁBrunoParserǁ_parse_bru_content__mutmut_204, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_205': xǁBrunoParserǁ_parse_bru_content__mutmut_205, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_206': xǁBrunoParserǁ_parse_bru_content__mutmut_206, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_207': xǁBrunoParserǁ_parse_bru_content__mutmut_207, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_208': xǁBrunoParserǁ_parse_bru_content__mutmut_208, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_209': xǁBrunoParserǁ_parse_bru_content__mutmut_209, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_210': xǁBrunoParserǁ_parse_bru_content__mutmut_210, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_211': xǁBrunoParserǁ_parse_bru_content__mutmut_211, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_212': xǁBrunoParserǁ_parse_bru_content__mutmut_212, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_213': xǁBrunoParserǁ_parse_bru_content__mutmut_213, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_214': xǁBrunoParserǁ_parse_bru_content__mutmut_214, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_215': xǁBrunoParserǁ_parse_bru_content__mutmut_215, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_216': xǁBrunoParserǁ_parse_bru_content__mutmut_216, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_217': xǁBrunoParserǁ_parse_bru_content__mutmut_217, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_218': xǁBrunoParserǁ_parse_bru_content__mutmut_218, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_219': xǁBrunoParserǁ_parse_bru_content__mutmut_219, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_220': xǁBrunoParserǁ_parse_bru_content__mutmut_220, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_221': xǁBrunoParserǁ_parse_bru_content__mutmut_221, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_222': xǁBrunoParserǁ_parse_bru_content__mutmut_222, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_223': xǁBrunoParserǁ_parse_bru_content__mutmut_223, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_224': xǁBrunoParserǁ_parse_bru_content__mutmut_224, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_225': xǁBrunoParserǁ_parse_bru_content__mutmut_225, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_226': xǁBrunoParserǁ_parse_bru_content__mutmut_226, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_227': xǁBrunoParserǁ_parse_bru_content__mutmut_227, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_228': xǁBrunoParserǁ_parse_bru_content__mutmut_228, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_229': xǁBrunoParserǁ_parse_bru_content__mutmut_229, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_230': xǁBrunoParserǁ_parse_bru_content__mutmut_230, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_231': xǁBrunoParserǁ_parse_bru_content__mutmut_231, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_232': xǁBrunoParserǁ_parse_bru_content__mutmut_232, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_233': xǁBrunoParserǁ_parse_bru_content__mutmut_233, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_234': xǁBrunoParserǁ_parse_bru_content__mutmut_234, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_235': xǁBrunoParserǁ_parse_bru_content__mutmut_235, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_236': xǁBrunoParserǁ_parse_bru_content__mutmut_236, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_237': xǁBrunoParserǁ_parse_bru_content__mutmut_237, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_238': xǁBrunoParserǁ_parse_bru_content__mutmut_238, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_239': xǁBrunoParserǁ_parse_bru_content__mutmut_239, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_240': xǁBrunoParserǁ_parse_bru_content__mutmut_240, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_241': xǁBrunoParserǁ_parse_bru_content__mutmut_241, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_242': xǁBrunoParserǁ_parse_bru_content__mutmut_242, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_243': xǁBrunoParserǁ_parse_bru_content__mutmut_243, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_244': xǁBrunoParserǁ_parse_bru_content__mutmut_244, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_245': xǁBrunoParserǁ_parse_bru_content__mutmut_245, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_246': xǁBrunoParserǁ_parse_bru_content__mutmut_246, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_247': xǁBrunoParserǁ_parse_bru_content__mutmut_247, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_248': xǁBrunoParserǁ_parse_bru_content__mutmut_248, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_249': xǁBrunoParserǁ_parse_bru_content__mutmut_249, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_250': xǁBrunoParserǁ_parse_bru_content__mutmut_250, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_251': xǁBrunoParserǁ_parse_bru_content__mutmut_251, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_252': xǁBrunoParserǁ_parse_bru_content__mutmut_252, 
        'xǁBrunoParserǁ_parse_bru_content__mutmut_253': xǁBrunoParserǁ_parse_bru_content__mutmut_253
    }
    
    def _parse_bru_content(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_parse_bru_content__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_parse_bru_content__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_bru_content.__signature__ = _mutmut_signature(xǁBrunoParserǁ_parse_bru_content__mutmut_orig)
    xǁBrunoParserǁ_parse_bru_content__mutmut_orig.__name__ = 'xǁBrunoParserǁ_parse_bru_content'
    
    def xǁBrunoParserǁ_extract_sections__mutmut_orig(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_1(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = None
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_2(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = None
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_3(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'XX(\w+(?::\w+)?)\s*\{([^}]*)\}XX'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_4(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_5(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_6(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = None
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_7(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(None, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_8(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, None, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_9(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, None)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_10(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_11(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_12(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, )
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_13(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE & re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_14(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = None
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_15(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(None).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_16(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(2).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_17(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = None
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_18(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(None).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_19(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(3).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_20(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content or not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_21(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if 'XX:XX' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_22(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' not in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_23(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_24(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith(None):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_25(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('XXbody:XX'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_26(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('BODY:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_27(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = None
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_28(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split(None):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_29(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('XX\nXX'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_30(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = None
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_31(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line or not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_32(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if 'XX:XX' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_33(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' not in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_34(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_35(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith(None):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_36(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('XX//XX'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_37(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = None
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_38(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(None, 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_39(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', None)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_40(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_41(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', )
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_42(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.rsplit(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_43(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split('XX:XX', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_44(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 2)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_45(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = None
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_46(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = None
            else:
                # Store raw content for body sections
                sections[section_name] = {'_raw_content': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_47(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = None
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_48(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'XX_raw_contentXX': section_content}
        
        return sections
    
    def xǁBrunoParserǁ_extract_sections__mutmut_49(self, content: str) -> Dict[str, Any]:
        """Extract sections from .bru file content."""
        sections = {}
        
        # Pattern to match sections: name { content }
        section_pattern = r'(\w+(?::\w+)?)\s*\{([^}]*)\}'
        
        matches = re.finditer(section_pattern, content, re.MULTILINE | re.DOTALL)
        
        for match in matches:
            section_name = match.group(1).strip()
            section_content = match.group(2).strip()
            
            # Parse key-value pairs or raw content
            if ':' in section_content and not section_name.startswith('body:'):
                # Parse as key-value pairs
                pairs = {}
                for line in section_content.split('\n'):
                    line = line.strip()
                    if ':' in line and not line.startswith('//'):
                        key, value = line.split(':', 1)
                        pairs[key.strip()] = value.strip()
                sections[section_name] = pairs
            else:
                # Store raw content for body sections
                sections[section_name] = {'_RAW_CONTENT': section_content}
        
        return sections
    
    xǁBrunoParserǁ_extract_sections__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_extract_sections__mutmut_1': xǁBrunoParserǁ_extract_sections__mutmut_1, 
        'xǁBrunoParserǁ_extract_sections__mutmut_2': xǁBrunoParserǁ_extract_sections__mutmut_2, 
        'xǁBrunoParserǁ_extract_sections__mutmut_3': xǁBrunoParserǁ_extract_sections__mutmut_3, 
        'xǁBrunoParserǁ_extract_sections__mutmut_4': xǁBrunoParserǁ_extract_sections__mutmut_4, 
        'xǁBrunoParserǁ_extract_sections__mutmut_5': xǁBrunoParserǁ_extract_sections__mutmut_5, 
        'xǁBrunoParserǁ_extract_sections__mutmut_6': xǁBrunoParserǁ_extract_sections__mutmut_6, 
        'xǁBrunoParserǁ_extract_sections__mutmut_7': xǁBrunoParserǁ_extract_sections__mutmut_7, 
        'xǁBrunoParserǁ_extract_sections__mutmut_8': xǁBrunoParserǁ_extract_sections__mutmut_8, 
        'xǁBrunoParserǁ_extract_sections__mutmut_9': xǁBrunoParserǁ_extract_sections__mutmut_9, 
        'xǁBrunoParserǁ_extract_sections__mutmut_10': xǁBrunoParserǁ_extract_sections__mutmut_10, 
        'xǁBrunoParserǁ_extract_sections__mutmut_11': xǁBrunoParserǁ_extract_sections__mutmut_11, 
        'xǁBrunoParserǁ_extract_sections__mutmut_12': xǁBrunoParserǁ_extract_sections__mutmut_12, 
        'xǁBrunoParserǁ_extract_sections__mutmut_13': xǁBrunoParserǁ_extract_sections__mutmut_13, 
        'xǁBrunoParserǁ_extract_sections__mutmut_14': xǁBrunoParserǁ_extract_sections__mutmut_14, 
        'xǁBrunoParserǁ_extract_sections__mutmut_15': xǁBrunoParserǁ_extract_sections__mutmut_15, 
        'xǁBrunoParserǁ_extract_sections__mutmut_16': xǁBrunoParserǁ_extract_sections__mutmut_16, 
        'xǁBrunoParserǁ_extract_sections__mutmut_17': xǁBrunoParserǁ_extract_sections__mutmut_17, 
        'xǁBrunoParserǁ_extract_sections__mutmut_18': xǁBrunoParserǁ_extract_sections__mutmut_18, 
        'xǁBrunoParserǁ_extract_sections__mutmut_19': xǁBrunoParserǁ_extract_sections__mutmut_19, 
        'xǁBrunoParserǁ_extract_sections__mutmut_20': xǁBrunoParserǁ_extract_sections__mutmut_20, 
        'xǁBrunoParserǁ_extract_sections__mutmut_21': xǁBrunoParserǁ_extract_sections__mutmut_21, 
        'xǁBrunoParserǁ_extract_sections__mutmut_22': xǁBrunoParserǁ_extract_sections__mutmut_22, 
        'xǁBrunoParserǁ_extract_sections__mutmut_23': xǁBrunoParserǁ_extract_sections__mutmut_23, 
        'xǁBrunoParserǁ_extract_sections__mutmut_24': xǁBrunoParserǁ_extract_sections__mutmut_24, 
        'xǁBrunoParserǁ_extract_sections__mutmut_25': xǁBrunoParserǁ_extract_sections__mutmut_25, 
        'xǁBrunoParserǁ_extract_sections__mutmut_26': xǁBrunoParserǁ_extract_sections__mutmut_26, 
        'xǁBrunoParserǁ_extract_sections__mutmut_27': xǁBrunoParserǁ_extract_sections__mutmut_27, 
        'xǁBrunoParserǁ_extract_sections__mutmut_28': xǁBrunoParserǁ_extract_sections__mutmut_28, 
        'xǁBrunoParserǁ_extract_sections__mutmut_29': xǁBrunoParserǁ_extract_sections__mutmut_29, 
        'xǁBrunoParserǁ_extract_sections__mutmut_30': xǁBrunoParserǁ_extract_sections__mutmut_30, 
        'xǁBrunoParserǁ_extract_sections__mutmut_31': xǁBrunoParserǁ_extract_sections__mutmut_31, 
        'xǁBrunoParserǁ_extract_sections__mutmut_32': xǁBrunoParserǁ_extract_sections__mutmut_32, 
        'xǁBrunoParserǁ_extract_sections__mutmut_33': xǁBrunoParserǁ_extract_sections__mutmut_33, 
        'xǁBrunoParserǁ_extract_sections__mutmut_34': xǁBrunoParserǁ_extract_sections__mutmut_34, 
        'xǁBrunoParserǁ_extract_sections__mutmut_35': xǁBrunoParserǁ_extract_sections__mutmut_35, 
        'xǁBrunoParserǁ_extract_sections__mutmut_36': xǁBrunoParserǁ_extract_sections__mutmut_36, 
        'xǁBrunoParserǁ_extract_sections__mutmut_37': xǁBrunoParserǁ_extract_sections__mutmut_37, 
        'xǁBrunoParserǁ_extract_sections__mutmut_38': xǁBrunoParserǁ_extract_sections__mutmut_38, 
        'xǁBrunoParserǁ_extract_sections__mutmut_39': xǁBrunoParserǁ_extract_sections__mutmut_39, 
        'xǁBrunoParserǁ_extract_sections__mutmut_40': xǁBrunoParserǁ_extract_sections__mutmut_40, 
        'xǁBrunoParserǁ_extract_sections__mutmut_41': xǁBrunoParserǁ_extract_sections__mutmut_41, 
        'xǁBrunoParserǁ_extract_sections__mutmut_42': xǁBrunoParserǁ_extract_sections__mutmut_42, 
        'xǁBrunoParserǁ_extract_sections__mutmut_43': xǁBrunoParserǁ_extract_sections__mutmut_43, 
        'xǁBrunoParserǁ_extract_sections__mutmut_44': xǁBrunoParserǁ_extract_sections__mutmut_44, 
        'xǁBrunoParserǁ_extract_sections__mutmut_45': xǁBrunoParserǁ_extract_sections__mutmut_45, 
        'xǁBrunoParserǁ_extract_sections__mutmut_46': xǁBrunoParserǁ_extract_sections__mutmut_46, 
        'xǁBrunoParserǁ_extract_sections__mutmut_47': xǁBrunoParserǁ_extract_sections__mutmut_47, 
        'xǁBrunoParserǁ_extract_sections__mutmut_48': xǁBrunoParserǁ_extract_sections__mutmut_48, 
        'xǁBrunoParserǁ_extract_sections__mutmut_49': xǁBrunoParserǁ_extract_sections__mutmut_49
    }
    
    def _extract_sections(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_extract_sections__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_extract_sections__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _extract_sections.__signature__ = _mutmut_signature(xǁBrunoParserǁ_extract_sections__mutmut_orig)
    xǁBrunoParserǁ_extract_sections__mutmut_orig.__name__ = 'xǁBrunoParserǁ_extract_sections'
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_orig(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_1(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = None
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_2(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'XXtotal_requestsXX': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_3(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'TOTAL_REQUESTS': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_4(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 1,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_5(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'XXtotal_foldersXX': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_6(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'TOTAL_FOLDERS': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_7(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 1,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_8(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'XXendpointsXX': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_9(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'ENDPOINTS': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_10(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'XXmethodsXX': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_11(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'METHODS': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_12(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'XXhas_authenticationXX': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_13(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'HAS_AUTHENTICATION': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_14(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': True,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_15(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'XXhas_documentationXX': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_16(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'HAS_DOCUMENTATION': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_17(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': True,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_18(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode == "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_19(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "XXnoneXX":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_20(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "NONE":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_21(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = None
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_22(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['XXhas_authenticationXX'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_23(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['HAS_AUTHENTICATION'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_24(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = False
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_25(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type != "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_26(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "XXhttpXX":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_27(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "HTTP":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_28(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] = 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_29(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] -= 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_30(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['XXtotal_requestsXX'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_31(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['TOTAL_REQUESTS'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_32(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 2
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_33(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(None)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_34(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['XXendpointsXX'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_35(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['ENDPOINTS'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_36(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(None)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_37(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['XXmethodsXX'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_38(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['METHODS'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_39(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] or item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_40(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_41(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['XXhas_authenticationXX'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_42(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['HAS_AUTHENTICATION'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_43(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode == "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_44(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "XXnoneXX":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_45(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "NONE":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_46(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = None
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_47(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['XXhas_authenticationXX'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_48(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['HAS_AUTHENTICATION'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_49(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = False
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_50(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] or item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_51(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_52(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['XXhas_documentationXX'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_53(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['HAS_DOCUMENTATION'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_54(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = None
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_55(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['XXhas_documentationXX'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_56(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['HAS_DOCUMENTATION'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_57(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = False
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_58(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type != "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_59(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "XXfolderXX":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_60(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "FOLDER":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_61(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] = 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_62(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] -= 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_63(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['XXtotal_foldersXX'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_64(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['TOTAL_FOLDERS'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_65(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 2
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_66(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(None)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_67(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(None)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_68(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = None
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_69(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['XXmethodsXX'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_70(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['METHODS'] = sorted(list(metadata['methods']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_71(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(None)
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_72(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(None))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_73(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['XXmethodsXX']))
        
        return metadata
    
    def xǁBrunoParserǁ_extract_metadata_optimized__mutmut_74(self, collection: BrunoCollection) -> Dict[str, Any]:
        """Optimized metadata extraction with single-pass traversal.
        
        Extracts all metadata in one pass to avoid multiple tree traversals:
        - counts, endpoints, methods, auth, tests, docs
        """
        metadata = {
            'total_requests': 0,
            'total_folders': 0,
            'endpoints': [],
            'methods': set(),
            'has_authentication': False,
            'has_documentation': False,
        }
        
        # Check root authentication
        if collection.root.request.auth.mode != "none":
            metadata['has_authentication'] = True
        
        # Single recursive pass
        def traverse(items: List[BrunoItem]):
            for item in items:
                if item.type == "http":
                    metadata['total_requests'] += 1
                    
                    if item.request:
                        # Extract endpoint
                        metadata['endpoints'].append(item.request.url)
                        
                        # Extract method
                        metadata['methods'].add(item.request.method)
                        
                        # Check auth
                        if not metadata['has_authentication'] and item.request.auth.mode != "none":
                            metadata['has_authentication'] = True
                        
                        # Check docs
                        if not metadata['has_documentation'] and item.request.docs:
                            metadata['has_documentation'] = True
                
                elif item.type == "folder":
                    metadata['total_folders'] += 1
                    if item.items:
                        traverse(item.items)
        
        traverse(collection.items)
        
        # Convert methods set to sorted list
        metadata['methods'] = sorted(list(metadata['METHODS']))
        
        return metadata
    
    xǁBrunoParserǁ_extract_metadata_optimized__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_1': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_1, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_2': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_2, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_3': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_3, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_4': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_4, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_5': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_5, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_6': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_6, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_7': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_7, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_8': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_8, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_9': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_9, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_10': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_10, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_11': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_11, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_12': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_12, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_13': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_13, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_14': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_14, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_15': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_15, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_16': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_16, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_17': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_17, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_18': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_18, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_19': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_19, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_20': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_20, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_21': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_21, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_22': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_22, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_23': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_23, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_24': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_24, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_25': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_25, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_26': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_26, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_27': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_27, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_28': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_28, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_29': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_29, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_30': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_30, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_31': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_31, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_32': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_32, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_33': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_33, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_34': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_34, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_35': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_35, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_36': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_36, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_37': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_37, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_38': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_38, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_39': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_39, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_40': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_40, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_41': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_41, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_42': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_42, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_43': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_43, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_44': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_44, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_45': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_45, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_46': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_46, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_47': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_47, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_48': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_48, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_49': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_49, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_50': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_50, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_51': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_51, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_52': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_52, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_53': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_53, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_54': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_54, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_55': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_55, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_56': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_56, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_57': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_57, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_58': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_58, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_59': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_59, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_60': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_60, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_61': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_61, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_62': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_62, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_63': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_63, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_64': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_64, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_65': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_65, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_66': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_66, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_67': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_67, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_68': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_68, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_69': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_69, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_70': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_70, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_71': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_71, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_72': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_72, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_73': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_73, 
        'xǁBrunoParserǁ_extract_metadata_optimized__mutmut_74': xǁBrunoParserǁ_extract_metadata_optimized__mutmut_74
    }
    
    def _extract_metadata_optimized(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_extract_metadata_optimized__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_extract_metadata_optimized__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _extract_metadata_optimized.__signature__ = _mutmut_signature(xǁBrunoParserǁ_extract_metadata_optimized__mutmut_orig)
    xǁBrunoParserǁ_extract_metadata_optimized__mutmut_orig.__name__ = 'xǁBrunoParserǁ_extract_metadata_optimized'
    
    def xǁBrunoParserǁ_count_items__mutmut_orig(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_1(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = None
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_2(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 1
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_3(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = None
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_4(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 1
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_5(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type != "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_6(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "XXhttpXX":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_7(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "HTTP":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_8(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests = 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_9(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests -= 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_10(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 2
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_11(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type != "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_12(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "XXfolderXX":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_13(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "FOLDER":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_14(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders = 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_15(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders -= 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_16(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 2
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_17(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = None
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_18(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(None)
                    requests += sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_19(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests = sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_20(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests -= sub_requests
                    folders += sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_21(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders = sub_folders
        
        return requests, folders
    
    def xǁBrunoParserǁ_count_items__mutmut_22(self, items: List[BrunoItem]) -> tuple[int, int]:
        """Count total requests and folders recursively (legacy method)."""
        requests = 0
        folders = 0
        
        for item in items:
            if item.type == "http":
                requests += 1
            elif item.type == "folder":
                folders += 1
                if item.items:
                    sub_requests, sub_folders = self._count_items(item.items)
                    requests += sub_requests
                    folders -= sub_folders
        
        return requests, folders
    
    xǁBrunoParserǁ_count_items__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_count_items__mutmut_1': xǁBrunoParserǁ_count_items__mutmut_1, 
        'xǁBrunoParserǁ_count_items__mutmut_2': xǁBrunoParserǁ_count_items__mutmut_2, 
        'xǁBrunoParserǁ_count_items__mutmut_3': xǁBrunoParserǁ_count_items__mutmut_3, 
        'xǁBrunoParserǁ_count_items__mutmut_4': xǁBrunoParserǁ_count_items__mutmut_4, 
        'xǁBrunoParserǁ_count_items__mutmut_5': xǁBrunoParserǁ_count_items__mutmut_5, 
        'xǁBrunoParserǁ_count_items__mutmut_6': xǁBrunoParserǁ_count_items__mutmut_6, 
        'xǁBrunoParserǁ_count_items__mutmut_7': xǁBrunoParserǁ_count_items__mutmut_7, 
        'xǁBrunoParserǁ_count_items__mutmut_8': xǁBrunoParserǁ_count_items__mutmut_8, 
        'xǁBrunoParserǁ_count_items__mutmut_9': xǁBrunoParserǁ_count_items__mutmut_9, 
        'xǁBrunoParserǁ_count_items__mutmut_10': xǁBrunoParserǁ_count_items__mutmut_10, 
        'xǁBrunoParserǁ_count_items__mutmut_11': xǁBrunoParserǁ_count_items__mutmut_11, 
        'xǁBrunoParserǁ_count_items__mutmut_12': xǁBrunoParserǁ_count_items__mutmut_12, 
        'xǁBrunoParserǁ_count_items__mutmut_13': xǁBrunoParserǁ_count_items__mutmut_13, 
        'xǁBrunoParserǁ_count_items__mutmut_14': xǁBrunoParserǁ_count_items__mutmut_14, 
        'xǁBrunoParserǁ_count_items__mutmut_15': xǁBrunoParserǁ_count_items__mutmut_15, 
        'xǁBrunoParserǁ_count_items__mutmut_16': xǁBrunoParserǁ_count_items__mutmut_16, 
        'xǁBrunoParserǁ_count_items__mutmut_17': xǁBrunoParserǁ_count_items__mutmut_17, 
        'xǁBrunoParserǁ_count_items__mutmut_18': xǁBrunoParserǁ_count_items__mutmut_18, 
        'xǁBrunoParserǁ_count_items__mutmut_19': xǁBrunoParserǁ_count_items__mutmut_19, 
        'xǁBrunoParserǁ_count_items__mutmut_20': xǁBrunoParserǁ_count_items__mutmut_20, 
        'xǁBrunoParserǁ_count_items__mutmut_21': xǁBrunoParserǁ_count_items__mutmut_21, 
        'xǁBrunoParserǁ_count_items__mutmut_22': xǁBrunoParserǁ_count_items__mutmut_22
    }
    
    def _count_items(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_count_items__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_count_items__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _count_items.__signature__ = _mutmut_signature(xǁBrunoParserǁ_count_items__mutmut_orig)
    xǁBrunoParserǁ_count_items__mutmut_orig.__name__ = 'xǁBrunoParserǁ_count_items'
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_orig(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_1(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = None
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_2(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" or item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_3(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type != "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_4(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "XXhttpXX" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_5(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "HTTP" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_6(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(None)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_7(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" or item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_8(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type != "folder" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_9(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "XXfolderXX" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_10(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "FOLDER" and item.items:
                endpoints.extend(self._extract_endpoints(item.items))
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_11(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(None)
        
        return endpoints
    
    def xǁBrunoParserǁ_extract_endpoints__mutmut_12(self, items: List[BrunoItem]) -> List[str]:
        """Extract all endpoint URLs from items."""
        endpoints = []
        
        for item in items:
            if item.type == "http" and item.request:
                endpoints.append(item.request.url)
            elif item.type == "folder" and item.items:
                endpoints.extend(self._extract_endpoints(None))
        
        return endpoints
    
    xǁBrunoParserǁ_extract_endpoints__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_extract_endpoints__mutmut_1': xǁBrunoParserǁ_extract_endpoints__mutmut_1, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_2': xǁBrunoParserǁ_extract_endpoints__mutmut_2, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_3': xǁBrunoParserǁ_extract_endpoints__mutmut_3, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_4': xǁBrunoParserǁ_extract_endpoints__mutmut_4, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_5': xǁBrunoParserǁ_extract_endpoints__mutmut_5, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_6': xǁBrunoParserǁ_extract_endpoints__mutmut_6, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_7': xǁBrunoParserǁ_extract_endpoints__mutmut_7, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_8': xǁBrunoParserǁ_extract_endpoints__mutmut_8, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_9': xǁBrunoParserǁ_extract_endpoints__mutmut_9, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_10': xǁBrunoParserǁ_extract_endpoints__mutmut_10, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_11': xǁBrunoParserǁ_extract_endpoints__mutmut_11, 
        'xǁBrunoParserǁ_extract_endpoints__mutmut_12': xǁBrunoParserǁ_extract_endpoints__mutmut_12
    }
    
    def _extract_endpoints(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_extract_endpoints__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_extract_endpoints__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _extract_endpoints.__signature__ = _mutmut_signature(xǁBrunoParserǁ_extract_endpoints__mutmut_orig)
    xǁBrunoParserǁ_extract_endpoints__mutmut_orig.__name__ = 'xǁBrunoParserǁ_extract_endpoints'
    
    def xǁBrunoParserǁ_extract_methods__mutmut_orig(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_1(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = None
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_2(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" or item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_3(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type != "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_4(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "XXhttpXX" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_5(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "HTTP" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_6(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(None)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_7(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" or item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_8(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type != "folder" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_9(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "XXfolderXX" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_10(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "FOLDER" and item.items:
                methods.extend(self._extract_methods(item.items))
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_11(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(None)
        
        return methods
    
    def xǁBrunoParserǁ_extract_methods__mutmut_12(self, items: List[BrunoItem]) -> List[str]:
        """Extract all HTTP methods from items."""
        methods = []
        
        for item in items:
            if item.type == "http" and item.request:
                methods.append(item.request.method)
            elif item.type == "folder" and item.items:
                methods.extend(self._extract_methods(None))
        
        return methods
    
    xǁBrunoParserǁ_extract_methods__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_extract_methods__mutmut_1': xǁBrunoParserǁ_extract_methods__mutmut_1, 
        'xǁBrunoParserǁ_extract_methods__mutmut_2': xǁBrunoParserǁ_extract_methods__mutmut_2, 
        'xǁBrunoParserǁ_extract_methods__mutmut_3': xǁBrunoParserǁ_extract_methods__mutmut_3, 
        'xǁBrunoParserǁ_extract_methods__mutmut_4': xǁBrunoParserǁ_extract_methods__mutmut_4, 
        'xǁBrunoParserǁ_extract_methods__mutmut_5': xǁBrunoParserǁ_extract_methods__mutmut_5, 
        'xǁBrunoParserǁ_extract_methods__mutmut_6': xǁBrunoParserǁ_extract_methods__mutmut_6, 
        'xǁBrunoParserǁ_extract_methods__mutmut_7': xǁBrunoParserǁ_extract_methods__mutmut_7, 
        'xǁBrunoParserǁ_extract_methods__mutmut_8': xǁBrunoParserǁ_extract_methods__mutmut_8, 
        'xǁBrunoParserǁ_extract_methods__mutmut_9': xǁBrunoParserǁ_extract_methods__mutmut_9, 
        'xǁBrunoParserǁ_extract_methods__mutmut_10': xǁBrunoParserǁ_extract_methods__mutmut_10, 
        'xǁBrunoParserǁ_extract_methods__mutmut_11': xǁBrunoParserǁ_extract_methods__mutmut_11, 
        'xǁBrunoParserǁ_extract_methods__mutmut_12': xǁBrunoParserǁ_extract_methods__mutmut_12
    }
    
    def _extract_methods(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_extract_methods__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_extract_methods__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _extract_methods.__signature__ = _mutmut_signature(xǁBrunoParserǁ_extract_methods__mutmut_orig)
    xǁBrunoParserǁ_extract_methods__mutmut_orig.__name__ = 'xǁBrunoParserǁ_extract_methods'
    
    def xǁBrunoParserǁ_check_authentication__mutmut_orig(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_1(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode == "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_2(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "XXnoneXX":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_3(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "NONE":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_4(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return False
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_5(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" or item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_6(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type != "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_7(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "XXhttpXX" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_8(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "HTTP" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_9(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode == "none":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_10(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "XXnoneXX":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_11(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "NONE":
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_12(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return False
        
        return False
    
    def xǁBrunoParserǁ_check_authentication__mutmut_13(self, collection: BrunoCollection) -> bool:
        """Check if the collection has authentication configured."""
        # Check root auth
        if collection.root.request.auth.mode != "none":
            return True
        
        # Check individual requests
        for item in collection.items:
            if item.type == "http" and item.request:
                if item.request.auth.mode != "none":
                    return True
        
        return True
    
    xǁBrunoParserǁ_check_authentication__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_check_authentication__mutmut_1': xǁBrunoParserǁ_check_authentication__mutmut_1, 
        'xǁBrunoParserǁ_check_authentication__mutmut_2': xǁBrunoParserǁ_check_authentication__mutmut_2, 
        'xǁBrunoParserǁ_check_authentication__mutmut_3': xǁBrunoParserǁ_check_authentication__mutmut_3, 
        'xǁBrunoParserǁ_check_authentication__mutmut_4': xǁBrunoParserǁ_check_authentication__mutmut_4, 
        'xǁBrunoParserǁ_check_authentication__mutmut_5': xǁBrunoParserǁ_check_authentication__mutmut_5, 
        'xǁBrunoParserǁ_check_authentication__mutmut_6': xǁBrunoParserǁ_check_authentication__mutmut_6, 
        'xǁBrunoParserǁ_check_authentication__mutmut_7': xǁBrunoParserǁ_check_authentication__mutmut_7, 
        'xǁBrunoParserǁ_check_authentication__mutmut_8': xǁBrunoParserǁ_check_authentication__mutmut_8, 
        'xǁBrunoParserǁ_check_authentication__mutmut_9': xǁBrunoParserǁ_check_authentication__mutmut_9, 
        'xǁBrunoParserǁ_check_authentication__mutmut_10': xǁBrunoParserǁ_check_authentication__mutmut_10, 
        'xǁBrunoParserǁ_check_authentication__mutmut_11': xǁBrunoParserǁ_check_authentication__mutmut_11, 
        'xǁBrunoParserǁ_check_authentication__mutmut_12': xǁBrunoParserǁ_check_authentication__mutmut_12, 
        'xǁBrunoParserǁ_check_authentication__mutmut_13': xǁBrunoParserǁ_check_authentication__mutmut_13
    }
    
    def _check_authentication(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_check_authentication__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_check_authentication__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_authentication.__signature__ = _mutmut_signature(xǁBrunoParserǁ_check_authentication__mutmut_orig)
    xǁBrunoParserǁ_check_authentication__mutmut_orig.__name__ = 'xǁBrunoParserǁ_check_authentication'
    
    def xǁBrunoParserǁ_check_tests__mutmut_orig(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_1(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" or item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_2(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type != "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_3(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "XXhttpXX" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_4(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "HTTP" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_5(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests and item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_6(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return False
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_7(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" or item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_8(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type != "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_9(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "XXfolderXX" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_10(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "FOLDER" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_11(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(None):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_12(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return False
        
        return False
    
    def xǁBrunoParserǁ_check_tests__mutmut_13(self, items: List[BrunoItem]) -> bool:
        """Check if any items have tests/assertions."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.tests or item.request.assertions:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_tests(item.items):
                    return True
        
        return True
    
    xǁBrunoParserǁ_check_tests__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_check_tests__mutmut_1': xǁBrunoParserǁ_check_tests__mutmut_1, 
        'xǁBrunoParserǁ_check_tests__mutmut_2': xǁBrunoParserǁ_check_tests__mutmut_2, 
        'xǁBrunoParserǁ_check_tests__mutmut_3': xǁBrunoParserǁ_check_tests__mutmut_3, 
        'xǁBrunoParserǁ_check_tests__mutmut_4': xǁBrunoParserǁ_check_tests__mutmut_4, 
        'xǁBrunoParserǁ_check_tests__mutmut_5': xǁBrunoParserǁ_check_tests__mutmut_5, 
        'xǁBrunoParserǁ_check_tests__mutmut_6': xǁBrunoParserǁ_check_tests__mutmut_6, 
        'xǁBrunoParserǁ_check_tests__mutmut_7': xǁBrunoParserǁ_check_tests__mutmut_7, 
        'xǁBrunoParserǁ_check_tests__mutmut_8': xǁBrunoParserǁ_check_tests__mutmut_8, 
        'xǁBrunoParserǁ_check_tests__mutmut_9': xǁBrunoParserǁ_check_tests__mutmut_9, 
        'xǁBrunoParserǁ_check_tests__mutmut_10': xǁBrunoParserǁ_check_tests__mutmut_10, 
        'xǁBrunoParserǁ_check_tests__mutmut_11': xǁBrunoParserǁ_check_tests__mutmut_11, 
        'xǁBrunoParserǁ_check_tests__mutmut_12': xǁBrunoParserǁ_check_tests__mutmut_12, 
        'xǁBrunoParserǁ_check_tests__mutmut_13': xǁBrunoParserǁ_check_tests__mutmut_13
    }
    
    def _check_tests(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_check_tests__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_check_tests__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_tests.__signature__ = _mutmut_signature(xǁBrunoParserǁ_check_tests__mutmut_orig)
    xǁBrunoParserǁ_check_tests__mutmut_orig.__name__ = 'xǁBrunoParserǁ_check_tests'
    
    def xǁBrunoParserǁ_check_documentation__mutmut_orig(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_1(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" or item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_2(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type != "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_3(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "XXhttpXX" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_4(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "HTTP" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_5(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return False
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_6(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" or item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_7(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type != "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_8(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "XXfolderXX" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_9(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "FOLDER" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_10(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(None):
                    return True
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_11(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return False
        
        return False
    
    def xǁBrunoParserǁ_check_documentation__mutmut_12(self, items: List[BrunoItem]) -> bool:
        """Check if any items have documentation."""
        for item in items:
            if item.type == "http" and item.request:
                if item.request.docs:
                    return True
            elif item.type == "folder" and item.items:
                if self._check_documentation(item.items):
                    return True
        
        return True
    
    xǁBrunoParserǁ_check_documentation__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBrunoParserǁ_check_documentation__mutmut_1': xǁBrunoParserǁ_check_documentation__mutmut_1, 
        'xǁBrunoParserǁ_check_documentation__mutmut_2': xǁBrunoParserǁ_check_documentation__mutmut_2, 
        'xǁBrunoParserǁ_check_documentation__mutmut_3': xǁBrunoParserǁ_check_documentation__mutmut_3, 
        'xǁBrunoParserǁ_check_documentation__mutmut_4': xǁBrunoParserǁ_check_documentation__mutmut_4, 
        'xǁBrunoParserǁ_check_documentation__mutmut_5': xǁBrunoParserǁ_check_documentation__mutmut_5, 
        'xǁBrunoParserǁ_check_documentation__mutmut_6': xǁBrunoParserǁ_check_documentation__mutmut_6, 
        'xǁBrunoParserǁ_check_documentation__mutmut_7': xǁBrunoParserǁ_check_documentation__mutmut_7, 
        'xǁBrunoParserǁ_check_documentation__mutmut_8': xǁBrunoParserǁ_check_documentation__mutmut_8, 
        'xǁBrunoParserǁ_check_documentation__mutmut_9': xǁBrunoParserǁ_check_documentation__mutmut_9, 
        'xǁBrunoParserǁ_check_documentation__mutmut_10': xǁBrunoParserǁ_check_documentation__mutmut_10, 
        'xǁBrunoParserǁ_check_documentation__mutmut_11': xǁBrunoParserǁ_check_documentation__mutmut_11, 
        'xǁBrunoParserǁ_check_documentation__mutmut_12': xǁBrunoParserǁ_check_documentation__mutmut_12
    }
    
    def _check_documentation(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBrunoParserǁ_check_documentation__mutmut_orig"), object.__getattribute__(self, "xǁBrunoParserǁ_check_documentation__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _check_documentation.__signature__ = _mutmut_signature(xǁBrunoParserǁ_check_documentation__mutmut_orig)
    xǁBrunoParserǁ_check_documentation__mutmut_orig.__name__ = 'xǁBrunoParserǁ_check_documentation'


__all__ = ["BrunoParser"]
